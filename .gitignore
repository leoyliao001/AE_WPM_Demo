# Python
__pycache__/
*.py[cod]

.env
.venv/
venv/

# Node / frontend — dependencies & build artifacts (do NOT commit)
node_modules/
frontend/node_modules/
frontend/node_modules.*/
dist/
frontend/dist/
.vite/
frontend/.vite/
frontend/.vite-cache/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage/
.nyc_output/

# Run output from the MSSQL ops scripts - regenerated on every run, machine-specific
backend/scripts/*_log.txt

# Local dev database - not versioned; bootstrap with `migrate` (+ optional
# `loaddata scripts/api_data_dump.json`). Production uses MSSQL.
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm

# Env / local overrides
.env.*
!.env.example
*.local

# IDE
.idea/
.vscode/
*.swp

# OS
Thumbs.db
.DS_Store

# Local tooling (do not commit)
.tools/

# Local CA bundle (generated from corporate cert, do not commit)
combined-ca.pem
