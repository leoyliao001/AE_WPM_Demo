from django.urls import path

urlpatterns = [
    # path("milestones/", milestones, name="project-milestones"),
    # path("milestones/<str:section>/", milestone_detail, name="project-milestone-detail"),
]
