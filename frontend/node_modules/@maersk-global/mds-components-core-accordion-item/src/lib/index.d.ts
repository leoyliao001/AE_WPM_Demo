import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import { Fit } from '@maersk-global/mds-shared-types';
import { IMcAccordionItem, ExpandIconPosition } from './types';
import '@maersk-global/mds-components-core-button';
import '@maersk-global/mds-components-core-icon';
export type { IMcAccordionItem, ExpandIconPosition } from './types';
/**
 * Purpose: A customizable item for the mc-accordion component that can be expanded or collapsed to show or hide content.
 * Category: Content Display
 * When to use: FAQs, Nested content, Disclosure sections
 * When not to use: Always expanded content, Simple lists
 * Works with components: mc-avatar, mc-button-group, mc-button, mc-card, mc-checkbox, mc-drawer, mc-input, mc-list, mc-notification, mc-radio, mc-segmented-control, mc-select, mc-switch, mc-table, mc-textarea
 * Alternative component names: Accordion, Collapsible Panel, Expandable Section
 *
 * @element `mc-accordion-item`
 *
 * @slot `heading` - The heading HTML to use for the accordion item heading
 * @slot `body` - The body HTML to use for the accordion item content
 * @slot `icon` - The icon HTML
 * @slot `trailingicon` - The trailing icon HTML
 *
 * @csspart `heading` - for styling the heading text
 * @csspart `body` - for styling the body content
 * @csspart `icon` - for changing visuals of icon
 *
 */
export declare class McAccordionItem extends LitElement implements IMcAccordionItem {
    private readonly uniqueId;
    id: string;
    heading?: string;
    body?: string;
    expanded: boolean;
    expandiconposition: ExpandIconPosition;
    height: number | 'auto';
    fit: Fit;
    protected get accordionItemId(): string;
    icon?: string;
    trailingicon?: string;
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    protected renderIcon(icon: string): TemplateResult | null;
    private renderHeading;
    private renderBody;
    private handleToggle;
}
//# sourceMappingURL=index.d.ts.map