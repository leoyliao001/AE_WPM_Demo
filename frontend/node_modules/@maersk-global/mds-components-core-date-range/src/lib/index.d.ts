import { LitElement, TemplateResult, CSSResultArray, PropertyValues } from 'lit';
import { CellCustomizationPayload, Fit, LabelPosition, Orientation, Position, Variant } from '@maersk-global/mds-shared-types';
import { IDateRangeValue, IMcDateRange } from './types';
import { Max, Min, Size } from '@maersk-global/mds-components-core-input/src/lib/types';
import '@maersk-global/mds-components-core-input-date';
import '@maersk-global/mds-components-core-label';
export type { IMcDateRange } from './types';
declare const McDateRange_base: (new (...args: any[]) => import("@maersk-global/mds-components-utils").FormFieldInterface) & typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: Display a date range picker with two connected date inputs and a calendar pop-up
 * Category: Forms, Actions
 * When to use: When you need to select a range of dates
 * When not to use: When you only need to select a single date
 * Works with components: mc-accordion-item, mc-card, mc-drawer, mc-modal, mc-popover, mc-tooltip
 * Alternative component names: Date Range Picker, Date Range Selector, Date Range Input
 *
 * @element `mc-date-range`
 *
 * @event {InputEvent} input - Fired when the character is entered.
 * @event {CustomEvent<string>} inputdateselected - Fired only when date is selected in the calendar.
 * @event {FocusEvent} focus - Fired when an input in mc-date-range is focused.
 * @event {FocusEvent} blur - Fired when an input in mc-date-range is going out of focus.
 * @event {MouseEvent} click - Fired on mc-date-range click.
 * @event {KeyboardEvent} keydown - Fired when a key on keydown is pressed.
 *
 * @slot `from` - Only utilize the slot designated for the 'from' if customization on its corresponding mc-input-date is required.
 * @slot `to` - Only utilize the slot designated for the 'to' if customization on its corresponding mc-input-date is required.
 * @slot `legend` - The legend HTML to use for the mc-date-range.
 */
export declare class McDateRange extends McDateRange_base implements IMcDateRange {
    private readonly internals;
    private firstUpdate;
    private fromInput?;
    private toInput?;
    private legendElements;
    private hasSlotLegend;
    private get legendVisible();
    private from?;
    private to?;
    protected controlType: string;
    static get formAssociated(): boolean;
    get validity(): ValidityState;
    autolayoutdisabled: boolean;
    autocomplete: string;
    calendarposition: Position;
    clearbutton: boolean;
    keepclearbuttonvisible: boolean;
    customize?: CellCustomizationPayload[];
    customstyles?: string;
    fit: Fit;
    format: string;
    fromlabel?: string | null;
    hiddenlabel: boolean;
    hiddenlegend: boolean;
    icon: string;
    labelposition: LabelPosition;
    legend?: string | null;
    loading: boolean;
    locale: string;
    max?: Max;
    min?: Min;
    name: string;
    nextlabel: string;
    orientation: Orientation;
    placeholder: string;
    previouslabel: string;
    readonly: boolean;
    required: boolean;
    showadjacentmonthdays: boolean;
    showweeknumbers: boolean;
    size: Size;
    startofweek: number;
    tolabel?: string | null;
    value?: IDateRangeValue | null;
    variant: Variant;
    width: string;
    yearcap: number;
    static get styles(): CSSResultArray;
    constructor();
    firstUpdated(changedProperties: PropertyValues): void;
    updated(changedProperties: PropertyValues): void;
    disconnectedCallback(): void;
    render(): TemplateResult;
    private onInput;
    private updateValue;
    private onFromDateBlurred;
    private onToDateBlurred;
    private onFromDateSelected;
    private onToDateSelected;
    private validateAndSetValue;
    private onLegendSlotChange;
}
//# sourceMappingURL=index.d.ts.map