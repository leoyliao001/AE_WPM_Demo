import { CSSResultArray, LitElement, TemplateResult } from 'lit';
import { Fit, IMcSwitch } from './types';
import '@maersk-global/mds-components-core-label';
export type { IMcSwitch } from './types';
declare const McSwitch_base: (new (...args: any[]) => import("@maersk-global/mds-components-utils").CheckableInterface) & (new (...args: any[]) => import("@maersk-global/mds-components-utils").FormFieldInterface) & typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: Display a switch with optional labels, hints, and error messages.
 * Category: Forms, Actions
 * When to use: Forms, Surveys, User preferences
 * When not to use: Decorative purposes, Non-interactive elements
 * Works with components: mc-switch-group, mc-card, mc-modal, mc-drawer, mc-notification
 * Alternative component names: Toggle Switch, Toggle Button, On/Off Switch
 *
 * @element `mc-switch`
 *
 * @event {MouseEvent} click - Fired when mc-switch is clicked.
 * @event {CustomEvent<boolean>} change - Fired when mc-switch in checked/unckecked.
 * @event {FocusEvent} focus - Fired when mc-switch is focused.
 * @event {FocusEvent} blur - Fired when mc-switch is going out of focus.
 *
 * @slot `label` - The label HTML to use for the mc-switch.
 *
 * @csspart `label-container` - for changing visuals of label container
 * @csspart `label` - for changing visuals of label
 * @csspart `checkmark` - for changing visuals of checkmark
 *
 */
export declare class McSwitch extends McSwitch_base implements IMcSwitch {
    private button?;
    protected controlType: string;
    protected focused: boolean;
    fit: Fit;
    hiddenlabel: boolean;
    label: string;
    readonly: boolean;
    name: string;
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    private renderSwitch;
    private handleInput;
    private stopPropagationOfClick;
    handleClick(): void;
    /**
     * Sets focus
     */
    focus(): void;
    protected onInputFocus(): void;
    /**
     * Sets focus out
     */
    blur(): void;
    protected onInputBlur(): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-switch': McSwitch;
    }
}
//# sourceMappingURL=index.d.ts.map