import { CSSResultArray, LitElement, TemplateResult } from 'lit';
import { IMcCheckbox, Fit, AriaRoles } from './types';
import '@maersk-global/mds-components-core-label';
import '@maersk-global/mds-components-core-hint';
import '@maersk-global/mds-components-core-error';
export type { IMcCheckbox } from './types';
declare const McCheckbox_base: (new (...args: any[]) => import("@maersk-global/mds-components-utils").CheckableInterface) & (new (...args: any[]) => import("@maersk-global/mds-components-utils").FormFieldInterface) & typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: Display a checkbox with optional labels, hints, and error messages.
 * Category: Forms, Actions
 * When to use: Forms, Surveys, User preferences
 * When not to use: Decorative purposes, Non-interactive elements
 * Works with components: mc-checkbox-group, mc-card, mc-modal, mc-drawer, mc-notification
 * Alternative component names: Checkbox, Tickbox, Selection Box
 *
 * @element `mc-checkbox`
 *
 * @event {MouseEvent} click - Fired when mc-checkbox is clicked.
 * @event {CustomEvent<boolean>} change - Fired when mc-checkbox in checked/unckecked.
 * @event {FocusEvent} focus - Fired when mc-checkbox is focused.
 * @event {FocusEvent} blur - Fired when mc-checkbox is going out of focus.
 *
 * @slot `label` - The label HTML to use for the mc-checkbox.
 * @slot `hint` - The hint HTML to use for the mc-checkbox.
 * @slot `errormessage` - The errormessage HTML to use for the mc-checkbox.
 *
 * @csspart `label-container` - for changing visuals of label container
 * @csspart `label` - for changing visuals of label
 * @csspart `checkmark` - for changing visuals of checkmark
 *
 */
export declare class McCheckbox extends McCheckbox_base implements IMcCheckbox {
    private input?;
    protected controlType: string;
    protected focused: boolean;
    ariarole: AriaRoles | undefined;
    errormessage?: string;
    fit: Fit;
    hiddenlabel: boolean;
    hint?: string;
    icon?: string;
    indeterminate: boolean;
    invalid: boolean;
    label: string;
    readonly: boolean;
    tabindex?: number;
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    private renderCheckbox;
    private renderCheckmark;
    private handleInput;
    private handleChange;
    /** Focuses the checkbox. */
    click(): void;
    /** Sets focus */
    focus(options?: FocusOptions): void;
    protected onInputFocus(): void;
    /** Sets focus out */
    blur(): void;
    protected onInputBlur(): void;
    private stopPropagationOfClick;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-checkbox': McCheckbox;
    }
}
//# sourceMappingURL=index.d.ts.map