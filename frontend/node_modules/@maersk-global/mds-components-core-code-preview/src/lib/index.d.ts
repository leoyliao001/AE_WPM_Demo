import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import { Fit } from '@maersk-global/mds-shared-types';
import { IMcCodePreview, Variant, Appearance, IMcCode } from './types';
import '@maersk-global/mds-components-core-button';
import '@maersk-global/mds-components-core-tab';
import '@maersk-global/mds-components-core-tab-bar';
import '@maersk-global/mds-components-core-menu';
import '@maersk-global/mds-components-core-list';
import '@maersk-global/mds-components-core-list-item';
import '@maersk-global/mds-components-core-tooltip';
import { IMcMenu } from '@maersk-global/mds-components-core-menu/types';
/**
 * Purpose: Component to display formatted code snippets with syntax highlighting, copy-to-clipboard, and multiple code variant support.
 * Category: Content Display, Data Display
 * When to use: Use when you need to present code examples, API payloads, or configuration snippets in a readable, interactive format.
 * When not to use: Not for displaying large files, unformatted text, or non-code content.
 * Works with components: mc-button, mc-tab-bar, mc-menu, mc-list, mc-list-item, mc-tooltip
 * Alternative component names: Code Block, Code Viewer, Code Example, Code Snippet
 * Extra notes: Supports multiple code variants (tab-bar or select), copy-to-clipboard, and theme adaptation (light/dark).
 *
 * @element `mc-code-preview`
 *
 * @slot heading - Slot for a custom heading above the code panel.
 * @slot footer - Slot for a custom footer below the code panel.
 *
 * @csspart code-panel - The code display panel.
 * @csspart header-panel - The header area containing controls and heading.
 * @csspart footer-panel - The footer area for additional info or actions.
 */
export declare class McCodePreview extends LitElement implements IMcCodePreview {
    protected mcMenu?: IMcMenu;
    private currentElementIndex;
    private showCodePanel;
    private copyLabel;
    private hasFooterSlot;
    private footerSlotElements;
    heading: string | null;
    footer: string | null;
    hidetoggle: boolean;
    variant: Variant;
    fit: Fit;
    appearance: Appearance;
    code: IMcCode[];
    constructor();
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    private renderHeader;
    private renderHeaderStart;
    private renderHeaderEnd;
    private renderPanel;
    private renderPreview;
    private renderFooter;
    private handleCopy;
    private handleToggle;
    private onFooterSlotChange;
    private highlightCode;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-code-preview': McCodePreview;
    }
}
//# sourceMappingURL=index.d.ts.map