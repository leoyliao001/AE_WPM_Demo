import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import { McDrawer } from '@maersk-global/mds-components-core-drawer';
import '@maersk-global/mds-components-core-button';
import '@maersk-global/mds-components-core-drawer';
declare const McSideBar_base: typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IResponsive);
/**
 * Purpose: A sidebar component that can be toggled open or closed, providing easy access to navigation links or other important content.
 * Category: Layout, Navigation, Content Display
 * When to use: When you need a collapsible sidebar for navigation or additional content.
 * When not to use: For static content that doesn't require a toggleable interface.
 * Works with components: mc-button, mc-card
 * Alternative component names: Side Navigation, Collapsible Menu, Sidebar Menu, Navigation Drawer, Slide-out Menu
 *
 * @element `mc-side-bar`
 *
 * @slot - The default slot for the `sidebar` content. Can be used to pass links list, banners, etc.
 *
 * @csspart `button` - for changing visuals of button.
 * @csspart `icon` - for changing visuals of icon
 * @csspart `text-and-icon` - for changing visuals of text and icons container
 */
export declare class McSideBar extends McSideBar_base {
    private category;
    private key;
    private scrollHandler;
    private leaveTimeout;
    protected mcDrawer?: McDrawer;
    protected mcSideBar?: HTMLElement;
    protected mcButton?: HTMLElement;
    open: boolean;
    static get styles(): CSSResultArray;
    constructor();
    connectedCallback(): void;
    disconnectedCallback(): void;
    render(): TemplateResult;
    private renderSideBar;
    private toggleNav;
    private handleScroll;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-side-bar': McSideBar;
    }
}
export {};
//# sourceMappingURL=index.d.ts.map