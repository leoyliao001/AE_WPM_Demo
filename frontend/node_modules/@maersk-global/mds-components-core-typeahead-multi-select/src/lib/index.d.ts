import { TemplateResult, CSSResultArray } from 'lit';
import { McTypeahead } from '@maersk-global/mds-components-core-typeahead';
import { IMcTypeaheadMultiSelect, IMcTypeaheadMultiSelectData } from './types';
import '@maersk-global/mds-components-core-tag';
import '@maersk-global/mds-components-core-button';
import { IMcSelectOption } from '@maersk-global/mds-components-core-typeahead/types';
/**
 * Purpose: A typeahead multi-select (autocomplete) component that provides real-time suggestions as users type into an input field, and allows selection of multiple options that will be displayed as tags.
 * Category: Forms, Actions
 * When to use: When you need to provide users with suggestions based on their input.
 * When not to use: When you have a fixed set of options that do not change based on user input.
 * Works with components: mc-option, mc-accordion-item, mc-card, mc-dialog, mc-drawer, mc-modal, mc-popover, mc-step-indicator-item, mc-table, mc-tabs
 * Alternative component names: Autocomplete, Search Suggestions, Predictive Search, Smart Search, Dropdown Search, Filtered Select
 *
 * @element `mc-typeahead-multi-select`
 *
 * @event {InputEvent} input - Fired when the character is entered.
 * @event {FocusEvent} focus - Fired when mc-input is focused.
 * @event {FocusEvent} blur - Fired when mc-input is going out of focus.
 * @event {MouseEvent} click - Fired on mc-input click.
 * @event {KeyboardEvent} keydown - Fired when a key on keydown is pressed.
 * @event {CustomEvent} clearbuttonclick - Fires when the clear button is pressed.
 * @event {CustomEvent<string>} search - Fires when user starts typing (after specified number of minchar and debounce).
 * @event {CustomEvent<IMcTypeaheadOptionSelectedDetail>} optionselected - Fires when an option is selected.
 * @event {CustomEvent} listscroll - Fires when user scrolls to the bottom of the list.
 *
 * @slot `label` - The label HTML to use for the mc-input.
 * @slot `hint` - The hint HTML to use for the mc-input.
 * @slot `errormessage` - The errormessage HTML to use for the mc-input.
 *
 * @csspart `label-container` - for changing visuals of label container
 * @csspart `label` - for changing visuals of label
 * @csspart `input` - for changing visuals of input field
 * @csspart `icon` - for changing visuals of icons
 */
export declare class McTypeaheadMultiSelect extends McTypeahead implements IMcTypeaheadMultiSelect {
    private enterKeyDisabled;
    protected selectedOptions?: IMcSelectOption[] | null;
    selecteddata: IMcTypeaheadMultiSelectData[];
    hiddentags: boolean;
    clearalllabel: string;
    freetexttagging: boolean;
    readonly: boolean;
    private readonly selectedSummaryId;
    static get styles(): CSSResultArray;
    constructor();
    private getSelectedSummaryText;
    private ensureInputDescribedBySummary;
    willUpdate(changedProperties: Map<string, unknown>): void;
    protected renderExtraContent(): TemplateResult;
    protected handleKeyDown(event: KeyboardEvent): void;
    protected handleInputChange(): void;
    protected onInputFocus(): void;
    protected onOptionSelected(el: CustomEvent): void;
    removeSelectedOption(optionToRemove: IMcTypeaheadMultiSelectData): void;
    clearAllSelectedOptions(): void;
    protected filterOptions(): void;
    protected resetOptionsVisibility(): void;
    private addSelectedOption;
    private removeSelectedOptionByData;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-typeahead-multi-select': McTypeaheadMultiSelect;
    }
}
//# sourceMappingURL=index.d.ts.map