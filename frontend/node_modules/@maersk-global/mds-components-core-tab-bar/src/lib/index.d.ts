import { CSSResultArray, LitElement, PropertyValues, TemplateResult } from 'lit';
import { Fit, IMcTabBar, Variant } from './types';
import '@maersk-global/mds-components-core-button';
export type { IMcTabBar } from './types';
/**
 * Purpose: A tabbed interface that allows users to navigate between different views or sections of content within the same page.
 * Category: Content Display, Navigation, Layout
 * When to use: When you need to display multiple views or sections of content in a compact space.
 * When not to use: For single-view interfaces or when a simple dropdown menu suffices.
 * Works with components: mc-tab
 * Alternative component names: Tabbed Navigation, Tabbed Interface, Tab Control, Tab Panel, Tab View
 *
 * @element `mc-tab-bar`
 *
 * @event {CustomEvent<number>} tabchange - Emitted when the any of the tabs is selected.
 *
 * @slot `tab` - The mc-tab element to use as a tab.
 * @slot `panel` - The panel HTML to use for the corresponding mc-tab.
 *
 * @csspart `tabs` - for changing visuals of the tab buttons container.
 * @csspart `panels` - for changing visuals of the panels container.
 */
export declare class McTabBar extends LitElement implements IMcTabBar {
    private static instanceCounter;
    private instanceId;
    private transitionType;
    private currentFocusIndex;
    private tabs?;
    private panels?;
    private tabsBox?;
    private tabsHost?;
    private scrollButtonRight?;
    private scrollButtonLeft?;
    private renderScrollButtons;
    private tabsClientWidth;
    private tabsScrollWidth;
    private currentScrollPosition;
    private tabObserver?;
    private tabObserverEnabled;
    currentindex: number;
    fit: Fit;
    variant: Variant;
    static get styles(): CSSResultArray;
    constructor();
    connectedCallback(): void;
    protected render(): TemplateResult;
    private renderScrollButtonTemplate;
    private handleScrollButtonClick;
    protected firstUpdated(changedProperties: PropertyValues): void;
    protected updated(changedProperties: PropertyValues): void;
    disconnectedCallback(): void;
    private getTabs;
    private getPanels;
    private initializeScrollButtons;
    private initializeTabElements;
    private initializePanelElements;
    private initializeTabIntersectionObserver;
    private setCurrentActiveStatus;
    private setTabWidths;
    private setTabActiveStatus;
    private setPanelActiveStatus;
    private setActiveStatus;
    private setTabIndexes;
    private setTabIndex;
    private handleTabClick;
    private handleTabKeyDown;
    private handleTabKeyUp;
    private focusOnNextFocusableTab;
    private focusOnPriorFocusableTab;
    private focusOnLastFocusableTab;
    private focusOnFirstFocusableTab;
    private handleCurrentIndexChanged;
    private handleTabSlotChange;
    private handlePanelSlotChange;
    private addEventListeners;
    private addScrollEventListeners;
    private removeScrollEventListeners;
    private removeTabIntersectionObserver;
    private scrollToCurrentIndex;
    private scrollTabBar;
    private updateScroll;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-tab-bar': McTabBar;
    }
}
//# sourceMappingURL=index.d.ts.map