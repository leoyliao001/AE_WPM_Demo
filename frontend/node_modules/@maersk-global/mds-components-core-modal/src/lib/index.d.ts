import { CSSResultArray, PropertyValues, TemplateResult } from 'lit';
import { IMcModal, Padding } from './types';
import '@maersk-global/mds-components-core-button';
import { McDialogBase } from '@maersk-global/mds-components-core-dialog';
export type { IMcModal } from './types';
/**
 * Purpose: Modal dialog to display content that requires user interaction
 * Category: Content Display, Forms, Actions
 * When to use: When you need to capture user input or display important information.
 * When not to use: When you need to display a large amount of content or complex layouts.
 * Works with components: mc-button, mc-checkbox, mc-date-range, mc-input-date, mc-input-time, mc-input, mc-multi-select, mc-number-stepper, mc-radio, mc-select-native, mc-select, mc-switch, mc-textarea, mc-typeahead-multi-select, mc-typeahead
 * Alternative component names: Dialog, Lightbox, Popup, Alert, Confirmation
 *
 * @element `mc-modal`
 *
 * @event {CustomEvent<McModalAction>} closed -  The closed event gets dispatched once the modal is closed.
 * @event {CustomEvent<McModalAction>} closing - The closing event gets dispatched once the modal is closing.
 * @event {CustomEvent} opened -  The opened event gets dispatched once the modal is opened.
 * @event {CustomEvent} opening - The opening event gets dispatched once the modal is opening.
 * @event {MouseEvent} click - The click event gets dispatched when clicked on the primaryAction/secondaryAction button within a modal.
 *
 * @slot `default` - The body HTML to use for the modal.
 * @slot `primaryAction` - The primaryAction HTML to use for the modal. Could be used for i.e. custom confirm/cancel buttons.
 * @slot `secondaryAction` - The secondaryAction HTML to use for the modal. Could be used for i.e. custom confirm/cancel buttons.
 * @slot `footer` - A slot for additional content to be placed in the footer. Could be used for i.e. instructions or disclaimer
 */
export declare class McModal extends McDialogBase implements IMcModal {
    backdropcloseactiondisabled: boolean;
    entercloseactiondisabled: boolean;
    hiddenclose: boolean;
    padding: Padding;
    static get styles(): CSSResultArray;
    protected getClasses(): Record<string, boolean>;
    protected renderFooter(): TemplateResult;
    protected handleKeyDown(event: KeyboardEvent): void;
    willUpdate(changedProperties: PropertyValues): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-modal': McModal;
    }
}
//# sourceMappingURL=index.d.ts.map