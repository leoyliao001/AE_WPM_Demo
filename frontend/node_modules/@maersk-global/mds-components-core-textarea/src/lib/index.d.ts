import { LitElement, CSSResultArray, TemplateResult } from 'lit';
import { Fit, IMcTextarea, LabelPosition } from './types';
import '@maersk-global/mds-components-core-error';
import '@maersk-global/mds-components-core-hint';
import '@maersk-global/mds-components-core-label';
export type { IMcTextarea } from './types';
declare const McTextarea_base: (new (...args: any[]) => import("@maersk-global/mds-components-utils").FormFieldInterface) & typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: A multi-line text input field that allows users to enter and edit larger amounts of text.
 * Category: Forms
 * When to use: When you need to capture a larger block of text from the user.
 * When not to use: For single-line text inputs or when a more compact input is needed.
 * Works with components: mc-accordion-item, mc-card, mc-dialog, mc-drawer, mc-modal, mc-popover, mc-step-indicator-item, mc-table, mc-tabs
 * Alternative component names: Text Area, Multi Line Input, Text Box
 *
 * @element `mc-textarea`
 *
 * @event {InputEvent} input - Fired when the character is entered.
 * @event {FocusEvent} focus - Fired when mc-input is focused.
 * @event {FocusEvent} blur - Fired when mc-input is going out of focus.
 * @event {MouseEvent} click - Fired on mc-textarea click.
 * @event {KeyboardEvent} keydown - Fired when a key on keydown is pressed.
 *
 * @csspart `textarea` - for changing visuals of textarea
 * @csspart `field` - for changing visuals of field container
 *
 * @slot `label` - The label HTML to use for the mc-textarea.
 * @slot `hint` - The hint HTML to use for the mc-textarea.
 * @slot `errormessage` - The errormessage HTML to use for the mc-textarea.
 */
export declare class McTextarea extends McTextarea_base implements IMcTextarea {
    private charCounter;
    private textarea?;
    protected controlType: string;
    protected focused: boolean;
    autofocus: boolean;
    cols: number;
    errormessage: string;
    fit: Fit;
    hiddenlabel: boolean;
    hint: string;
    id: string;
    private get inputId();
    invalid: boolean;
    label: string;
    labelposition: LabelPosition;
    maxlength?: number;
    minlength: number;
    name: string;
    placeholder: string;
    readonly: boolean;
    required: boolean;
    rows: number;
    value?: string | null;
    width: string;
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    protected renderInput(): TemplateResult | string;
    protected renderLabel(): TemplateResult;
    protected renderError(): TemplateResult;
    protected renderHint(): TemplateResult;
    protected renderCharCounter(shouldRenderCharCounter: boolean): TemplateResult | null;
    focus(): void;
    protected onInputFocus(): void;
    blur(): void;
    protected onInputBlur(): void;
    protected handleInputChange(): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-textarea': McTextarea;
    }
}
//# sourceMappingURL=index.d.ts.map