import type { Viewport } from '@maersk-global/mds-shared-types';
export type { Viewport } from '@maersk-global/mds-shared-types';
export interface IMcToolbar {
    /**
     * wrapbreakpoint when the start, center or end slots will be wrapped and stacked.
     * Can be: `x-small` or `small`
     * @default x-small
     * @type {Viewport}
     */
    wrapbreakpoint?: Viewport;
}
//# sourceMappingURL=types.d.ts.map