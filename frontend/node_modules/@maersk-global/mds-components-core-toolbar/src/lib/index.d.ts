import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import type { IMcToolbar, Viewport } from './types';
/**
 * Purpose: A customizable toolbar component that groups elements in a logical way and and can be shown anywhere on the page.
 * Category: Layout, Navigation, Forms
 * When to use it: When you need a container for grouping related actions and content in a toolbar layout.
 * When not to use it: When you need a simple button or a non-toolbar related layout.
 * Works with components: mc-avatar, mc-button, mc-checkbox, mc-date-range, mc-file-upload, mc-input-date, mc-input-time, mc-input, mc-link-button, mc-menu, mc-multi-select, mc-number-stepper, mc-pagination, mc-radio, mc-select-native, mc-select, mc-select, mc-switch, mc-tag, mc-typeahead-multi-select, mc-typeahead
 * Alternative component names: Toolbar, Action Bar, Navigation Bar
 *
 * @element `mc-toolbar`
 *
 * @slot `start` - elements will be placed at the start of the toolbar.
 * @slot `center` - elements will be placed at the center of the toolbar.
 * @slot `end` - elements will be placed at the end of the toolbar.
 *
 * @csspart toolbar - The toolbar container
 */
export declare class McToolbar extends LitElement implements IMcToolbar {
    private toolbar;
    wrapbreakpoint: Viewport;
    private toolbarItems;
    private slotOrder;
    private keyDownCounter;
    private readonly directFocusableTags;
    private readonly shadowHostTags;
    private readonly componentsWithHiddenPopover;
    private readonly allowedKeyboardInInnerComponentSelectors;
    private _allowedKeyboardCombinedSelector;
    private get allowedKeyboardCombinedSelector();
    private get directFocusableSelector();
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    connectedCallback(): void;
    disconnectedCallback(): void;
    private onSlotChange;
    private getToolbarItems;
    private handleFocusIn;
    private handleFocusOut;
    private handleKeyDown;
    private focusItemAt;
    private findIndexFromPath;
    private findPopoverHostForList;
    private isNavigatingWithArrowsInComponent;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-toolbar': McToolbar;
    }
}
//# sourceMappingURL=index.d.ts.map