import { CSSResultArray, LitElement, PropertyValues, TemplateResult } from 'lit';
import { Fit, Orientation } from '@maersk-global/mds-shared-types';
import { IMcStepIndicator } from './types';
import '@maersk-global/mds-components-core-step-indicator-item';
export type { IMcStepIndicator } from './types';
/**
 * Purpose: A component for displaying a step indicator for a process or workflow.
 * Category: Forms, Navigation
 * When to use: When you need to visually represent the progress of a multi-step process.
 * When not to use: For single-step processes or when a simple progress bar suffices.
 * Works with components: mc-step-indicator-item
 * Alternative component names: Step Progress Indicator, Stepper, Progress Tracker
 *
 * @element `mc-step-indicator`
 *
 * @slot - The default slot where you can pass single step items
 */
export declare class McStepIndicator extends LitElement implements IMcStepIndicator {
    private stepIndicatorItems?;
    private slottedElements?;
    autolayoutdisabled: boolean;
    alignitemsdisabled: boolean;
    currentindex: number;
    fit: Fit;
    labels: Array<string>;
    orientation: Orientation;
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    private renderSteps;
    protected firstUpdated(changedProperties: PropertyValues): Promise<void>;
    protected updated(changedProperties: PropertyValues): void;
    private initializeLabels;
    private getOnlyStepIndicatorItems;
    private isStepIndicatorItem;
    private updateLabelsProps;
    private getStateName;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-step-indicator': McStepIndicator;
    }
}
//# sourceMappingURL=index.d.ts.map