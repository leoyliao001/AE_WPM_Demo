import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import { Appearance, State } from './types';
import { Fit, Orientation } from '@maersk-global/mds-shared-types';
import '@maersk-global/mds-components-core-icon';
export type { IMcStepIndicatorItem } from './types';
/**
 * Purpose: A component for displaying a step indicator for a process or workflow.
 * Category: Forms, Navigation
 * When to use: When you need to visually represent the progress of a multi-step process.
 * When not to use: For single-step processes or when a simple progress bar suffices.
 * Works with components: mc-step-indicator
 * Alternative component names: Step Progress Indicator, Stepper, Progress Tracker
 *
 * @element `mc-step-indicator-item`
 *
 * @slot - The default slot for the `step-indicator-item`. Used to display custom text/icon.
 * @slot icon - The `icon` slot where an `mc-icon` or any other forms of graphics go.
 *
 * @csspart `step` - for changing visuals of step item
 * @csspart `marker` - for changing visuals of marker inside the step
 * @csspart `label` - for changing visuals of label inside the step
 * @csspart `icon` - for changing visuals of icon
 */
export declare class McStepIndicatorItem extends LitElement {
    private hasSlotIcon;
    autolayoutdisabled: boolean;
    alignitemsdisabled: boolean;
    fit: Fit | 'x-large';
    icon: string;
    label: string;
    orientation: Orientation;
    state: State;
    appearance: Appearance;
    private iconSlotElements;
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    private renderIconOrCheckmark;
    private renderCheckmark;
    protected hasIcon(): boolean;
    private onIconSlotChange;
}
//# sourceMappingURL=index.d.ts.map