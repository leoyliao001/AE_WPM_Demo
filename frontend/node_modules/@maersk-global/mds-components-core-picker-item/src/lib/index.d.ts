import { TemplateResult, CSSResultArray } from 'lit';
import { IMcPickerItem, PickerItemWidth } from './types';
import { McListItem } from '@maersk-global/mds-components-core-list-item';
export type { IMcPickerItem } from './types';
/**
 * Purpose: To represent an individual item in the mc-picker.
 * Category: Forms
 * When to use: When you need to display an item in a picker.
 * When not to use: When you have a small number of items to choose from.
 * Works with components: mc-picker
 * Alternative component names: Picker Item, Scrollable Item, Wheel Item, Scroll Picker Item
 *
 * @element `mc-picker-item`
 *
 * @slot icon - The `icon` slot where an `mc-icon` or any other forms of graphics go.
 * @slot trailingicon - The `trailingicon` slot where an `mc-icon` or any other forms of graphics go.
 *
 * @csspart `button` - for changing visuals of button
 * @csspart `icon` - for changing visuals of icon
 *
 */
export declare class McPickerItem extends McListItem implements IMcPickerItem {
    width?: PickerItemWidth;
    static get styles(): CSSResultArray;
    constructor();
    render(): TemplateResult;
}
//# sourceMappingURL=index.d.ts.map