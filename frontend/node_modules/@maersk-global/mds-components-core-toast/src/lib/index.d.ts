import { LitElement, TemplateResult, CSSResultArray, PropertyValues } from 'lit';
import { IMcToast, Fit, Position, ToastAppearance, Width } from './types';
import '@maersk-global/mds-components-core-notification';
export type { IMcToast } from './types';
/**
 * Purpose: A toast notification component for displaying brief messages to the user.
 * Category: Layout, Actions
 * When to use: When you need to provide feedback to the user without interrupting their workflow.
 * When not to use: When you need to capture user input or require a more complex interaction.
 * Works with components: mc-button, mc-icon, mc-notification
 * Alternative component names: Toast Notification, Snackbar, Alert
 *
 * @element `mc-toast`
 *
 * @event {CustomEvent<string>} close - Emitted when the toast is closed.
 *
 * @slot - The HTML content of the mc-toast.
 * @slot `trigger` - The trigger element which triggers the toast.
 */
export declare class McToast extends LitElement implements IMcToast {
    private timerId;
    private start;
    private remaining;
    private animeDuration;
    private mcClosableComponent;
    private mcClosableComponentCloned;
    private allowedClosableComponents;
    private closeEventDispatched;
    private content;
    private trigger?;
    fit: Fit;
    appearance: ToastAppearance;
    position: Position;
    width: Width;
    duration: number;
    open: boolean;
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    connectedCallback(): void;
    protected firstUpdated(changedProperties: PropertyValues): void;
    protected willUpdate(changedProperties: Map<PropertyKey, unknown>): void;
    private onTriggerSlotChange;
    private onSlotChange;
    show(): void;
    hide(): void;
    private pause;
    private resume;
    private animation;
    private dispatchCloseEvent;
    private attachEventsToClosableComponent;
    private createToastContainer;
    private containerInDom;
    private destroyToastContainer;
    private cloneClosableComponent;
}
//# sourceMappingURL=index.d.ts.map