import type { SupportedLocale } from '@maersk-global/mds-components-utils';
import type { IMcTableLabels } from './types';
/**
 * Table translations combining pagination labels with table-specific labels
 * This reuses all pagination labels (previous, next, pagesize, etc.) and adds table-specific ones
 */
export declare const translations: Record<SupportedLocale, IMcTableLabels>;
//# sourceMappingURL=translations.d.ts.map