import { ReactiveController, ReactiveControllerHost } from 'lit';
import type { Table, TableOptions, RowData, TableState, Updater } from '@tanstack/table-core';
import type { UpdateParameters } from './types';
export declare function doUpdate<T>(value: T, updater: Updater<unknown>): T;
export declare function flexRender<TProps extends object>(Comp: ((props: TProps) => string) | string | undefined, props: TProps): string | number | null;
export declare class Controller<TData extends RowData> implements ReactiveController {
    private host;
    private options;
    table: Table<TData>;
    state: TableState;
    tableOptions: Partial<TableOptions<RowData>>;
    constructor(host: ReactiveControllerHost, options: TableOptions<TData>);
    hostConnected(): void;
    update({ state, data, columns, tableOptions }: UpdateParameters): void;
}
//# sourceMappingURL=controller.d.ts.map