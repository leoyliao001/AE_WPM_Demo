import { CSSResultArray, LitElement, PropertyValues, TemplateResult } from 'lit';
import { ColumnSorter, IMcTable, TableBorderStyle, TableColumn, TableData, TableSortDirection, TableVerticalAlign, RowExpansionPadding, SpanConfig, TablePagination, TablePaginationMode, TablePaginationAlignment, IMcTableLabels } from './types';
import { Fit } from '@maersk-global/mds-shared-types';
import type { ColumnDef, Header, Row, SortingFnOption } from '@tanstack/table-core';
import '@maersk-global/mds-components-core-checkbox';
import '@maersk-global/mds-components-core-button';
import '@maersk-global/mds-components-core-tooltip';
import '@maersk-global/mds-components-core-icon';
import '@maersk-global/mds-components-core-pagination';
export type { IMcTable } from './types';
declare const McTable_base: typeof LitElement;
/**
 * Purpose: A table to display data in rows and columns with advanced features like sorting, pagination, row selection, and expandable rows.
 * Category: Data Display, Layout
 * When to use: When you need to present tabular data with advanced interaction capabilities.
 * When not to use: For simple lists or when a grid layout is more appropriate.
 * Works with components: mc-avatar, mc-badge, mc-button, mc-checkbox, mc-icon, mc-input, mc-multi-select, mc-pagination, mc-popover, mc-select, mc-tooltip
 * Alternative component names: Data Grid, Data Table, Tabular View
 *
 * @element `mc-table`
 *
 * @event {CustomEvent<IMcTableSortChangeDetail>} sortchange - Dispatched when sorting direction on a column changes with a column id and direction as detail.
 * @event {CustomEvent<unknown[]>} selectchange - Dispatched when a row is selected/deselected with the current selection state as detail.
 * @event {CustomEvent<unknown | undefined>} expandchange - Dispatched when a row is expanded/collapsed with the current expansion state as detail.
 * @event {CustomEvent<string | undefined>} hidecolumnschange - Dispatched when a column is hid/shown with the current hidecolumns state as detail.
 * @event {CustomEvent<unknown | undefined>} paginationpagechange - Dispatched when the page changes (only when pagination is enabled) with the visible rows as detail.
 * @event {CustomEvent<unknown | undefined>} paginationpagesizechange - Dispatched when the page size changes.
 * @event {CustomEvent<IMcTableRowClickDetail>} rowclick - Dispatched when a row is clicked.
 * @event {CustomEvent<unknown>} rowmouseenter - Dispatched when the cursor enters a row.
 * @event {CustomEvent<unknown>} rowmouseleave - Dispatched when the cursor leaves a row.
 * @event {CustomEvent<unknown>} rowselected - Dispatched when a row is selected.
 * @event {CustomEvent<unknown>} rowdeselected - Dispatched when a row is deselected.
 * @event {CustomEvent<unknown>} rowexpanded - Dispatched when a row is expanded.
 * @event {CustomEvent<unknown>} rowcollapsed - Dispatched when a row is collapsed.
 * @event {CustomEvent<IMcTableColumnResizeDetail>} columnresizestart - Dispatched when user starts resizing a column.
 * @event {CustomEvent<IMcTableColumnResizeDetail>} columnresizing - Dispatched continuously (animation frame throttled) while a column is being resized.
 * @event {CustomEvent<IMcTableColumnResizeDetail>} columnresizeend - Dispatched when user ends resizing a column.
 * @event {CustomEvent<IMcTableColumnResizeDetail>} columnresizestart - Dispatched when user starts resizing a column.
 * @event {CustomEvent<IMcTableColumnResizeDetail>} columnresizing - Dispatched continuously (animation frame throttled) while a column is being resized.
 * @event {CustomEvent<IMcTableColumnResizeDetail>} columnresizeend - Dispatched when user ends resizing a column.
 *
 * @slot `{rowId}_{columnId}` - When you want to pass other mc-components or your own custom components to the table cell. The first parameter in the slot name {rowId} is a row id(the component will automatically try to access an \`id\` attribute, you can change that by passing the \`datakey\` attribute), the second {columnId} is the id of the column where custom component should be rendered, i.e. 10_status, 11_status, 12_status, etc
 * @slot `caption` - Caption to describe the table.
 * @slot `{columnId}_header` - Header slot for a column with {columnId} as an \`id\` property.
 * @slot `{columnId}_footer` - Footer slot for a column with {columnId} as an \`id\` property.
 * @slot `{rowId}_expanded` - Slot for an expandable row content.
 * @slot `state` - Slot in the table body. Can be used to display state of the table, i.e. loading, empty, error, etc.
 *
 * @csspart pagination-navigation - The pagination navigation container
 * @csspart pagination-pagesize - The pagination page size container
 * @csspart pagination-pagestatus - The pagination page status container
 * */
export declare class McTable extends McTable_base implements IMcTable {
    labels?: Partial<IMcTableLabels>;
    data: TableData;
    datakey: string;
    columns: TableColumn[];
    zebrastripes: boolean;
    truncate: boolean;
    outerborderstyle: TableBorderStyle;
    verticallinestyle: TableBorderStyle;
    horizontallinestyle: TableBorderStyle;
    fit: Fit;
    disablerowhighlightonhover: boolean;
    verticalalign?: TableVerticalAlign;
    nowrap: boolean;
    disableoverflow: boolean;
    sortdisableoninitialload: boolean;
    height: string;
    sortdefaultcolumnid: string;
    sortdefaultdirection: TableSortDirection;
    sortdisabled: boolean;
    sortmanual: boolean;
    /**
     * Pagination
     */
    pagination: TablePagination;
    paginationmode: TablePaginationMode;
    paginationscrolltotop: boolean;
    paginationloading: boolean;
    paginationalignment: TablePaginationAlignment;
    paginationtotalrecords: number;
    paginationarialabel: string;
    paginationcurrentpage: number;
    paginationhidepagesize: boolean;
    paginationpagesize: number;
    paginationpagesizeoptions: number[];
    paginationhidepagestatus: boolean;
    paginationshowfirstandlastpage: boolean;
    get currentpage(): number;
    set currentpage(value: number);
    get recordsperpage(): number;
    set recordsperpage(value: number);
    get resetscrollonpagechange(): boolean;
    set resetscrollonpagechange(value: boolean);
    /**
     * Deprecated labels
     */
    selectalllabel: string;
    selectlabel: string;
    expandtriggerlabel: string;
    /**
     * Selection
     */
    select: boolean;
    selected: TableData | string[];
    expand: boolean;
    expandopened: TableData;
    caption: string;
    footer: boolean;
    headersticky: boolean;
    headerstickyviewport: boolean;
    headerstickyoffsetselector: string;
    footersticky: boolean;
    selectsticky: boolean;
    expandsticky: boolean;
    headerhidden: boolean;
    hidecolumns: string[];
    expandpadding: RowExpansionPadding;
    stateslotheight: string;
    customstyles: string;
    disableroundedcorners: boolean;
    disableplaceholderfooters: boolean;
    selectrowdisabled: (row: any) => boolean;
    spans: SpanConfig[];
    columnresize: boolean;
    private _resizeDebounceTimer?;
    private _isResizing;
    private _preventSortingTimer?;
    private _mcPopoverActive;
    private _cellElementWithActivePopovers;
    private headerStickyOffsetObserver?;
    private headerStickyOffsetIntersectionObserver?;
    private headerStickyOffsetTargets;
    private headerStickyOffsetWindowResizeHandler?;
    private headerStickyOffsetScrollHandler?;
    private headerStickyOffsetRaf?;
    private headerStickyOffsetRetryHandle?;
    private headerStickingObserver?;
    private computedColumns;
    private hasCaptionSlot;
    private hasStateSlot;
    private headerStickyTop;
    private isHeaderSticking;
    private tableWrapper;
    private resizeObserver;
    private hadPreviousData;
    private shouldDispatchSortEvent;
    private spanCache;
    private skipRenderCache;
    private static hasShownSpanWarning;
    private _stickyRecalculationNeeded;
    private _paginationExplicitlySet;
    private _afterFirstUdpdate;
    private _sort;
    private _pagination;
    private _rowSelection;
    private _expanded;
    private _pinnedColumns;
    private _expandable;
    private _captionID;
    protected get computedLabels(): Required<IMcTableLabels>;
    private controller;
    private dispatchSelectedOrExpandedEvents;
    private get isPagiantionEnabled();
    static get styles(): CSSResultArray;
    connectedCallback(): void;
    disconnectedCallback(): void;
    render(): TemplateResult;
    private renderCustomStyles;
    private renderCaption;
    protected getCaptionId(): string | undefined;
    private onCaptionSlotChange;
    private hasCaption;
    private renderPagination;
    private paginationsOnPageChange;
    private paginationOnPageSizeChange;
    private handleHeaderStickyOffsetUpdates;
    private setupHeaderStickyOffsetObserver;
    private teardownHeaderStickyOffsetObserver;
    private setupHeaderStickingObserver;
    private teardownHeaderStickingObserver;
    /**
     * Determines if an element is currently affecting the layout at the top of the page
     * This includes both sticky elements in their sticky state and fixed elements positioned at the top
     */
    private isElementCurrentlySticky;
    /**
     * Gets the effective height of an element only if it's currently affecting the top layout
     * This includes both sticky and fixed elements. Returns 0 if the element doesn't contribute to offset.
     */
    private getStickyElementHeight;
    /**
     * Determines if any of the observed elements changed their sticky state
     * Used to optimize updates and only recalculate when necessary
     *
     * @param _targets - Elements to check for sticky state changes (currently unused)
     * @returns Always true for now, could be optimized in the future
     */
    private measureHeaderStickyOffset;
    private applyHeaderStickyOffsetFromNumber;
    private applyHeaderStickyOffset;
    private renderTableHead;
    private renderHeaderRow;
    private renderPlaceholderHeaderCell;
    private renderHeaderCell;
    private getHeaderCellZIndex;
    private handleHeaderSlotChange;
    /**
     * Returns the mc-popover element passed via the header slot
     */
    private getHeaderSlotPopover;
    private handleHeaderCellClicked;
    private renderThContent;
    private renderHeaderRowSelector;
    private renderHeaderExpandColumn;
    private renderTableBody;
    protected renderLoadingIndicator(): TemplateResult | null;
    private renderBodyRows;
    private handleStateSlotChange;
    private renderBodyRow;
    private handleSpans;
    private renderBodyCell;
    private cellClicked;
    private renderBodyCellSlot;
    private formatCellValue;
    private renderBodyRowSelector;
    private renderBodyExpansionToggle;
    private hasChildrenData;
    private renderExpandedRow;
    private renderChildRows;
    private renderSortButton;
    private getSortButtonIcon;
    private renderTfoot;
    private renderTfootRow;
    /**
     * Returns a rendering function for a table footer cell.
     * It encapsulates the colspan state to skip rendering of the cells falling within a colspan.
     */
    private getTfootCellRenderer;
    private renderTfootPlaceholderCell;
    protected firstUpdated(_changedProperties: PropertyValues<this>): void;
    protected updated(changedProperties: PropertyValues<this>): void;
    protected willUpdate(_changedProperties: PropertyValues<this>): void;
    protected handleColumnsChanged(): void;
    private getColumnsToCompute;
    private getPinnedColumns;
    private handleExpandedRowSlotChange;
    protected handleDataChanged(): void;
    protected handleRowClick(e: MouseEvent, row: Row<unknown>): void;
    protected createComputedColumns(columns: TableColumn[]): ColumnDef<unknown, unknown>[];
    protected createComputedColumn(column: TableColumn): ColumnDef<unknown, unknown> & {
        header?: unknown;
    };
    protected getWidthValues(width: string): {
        value: number | undefined;
        unit: string | undefined;
    };
    protected getColumnSorter(sorter: ColumnSorter): SortingFnOption<unknown>;
    protected calculateColumnSize(widthValue: number | undefined, sizeUnit: string | undefined): number | undefined;
    protected createComputedColumnsFromData(): ColumnDef<unknown, unknown>[];
    protected setDefaultSortingState(): void;
    private setupSimpleScrollDetection;
    private reduceArrayIntoBoolObject;
    private getAlignmentClass;
    private getVerticalAlignmentClass;
    private shouldApplyTabularFiguresClass;
    private getStickyStyles;
    private getDefaultCellStyles;
    private getCellStyles;
    private applyStickyHeadersTopOffset;
    private updateCellStylesOnResize;
    private initializeResizeObserver;
    private enableRowSelection;
    private areAllRowsDisabled;
    private getDisabledRowsSelection;
    private handleSelectionToggleAll;
    private getCellWidthValue;
    private dispatchCustomEvent;
    private hasSlot;
    /**
     * Returns the pagination page count
     * @returns {number}
     */
    getPageCount(): number;
    /**
     * Returns visible rows, depending if pagination is enabled or not.
     * @returns {Array<any>}
     */
    getVisibleRows(): Array<Record<string, string | number | boolean | object>>;
    /**
     * Resets the row selection to empty state
     */
    resetRowSelection(): void;
    /**
     * Resets sorting to empty state
     */
    resetSorting(): void;
    /**
     * Returns the selected row datakeys
     * @returns {Array<number | string>}
     */
    getSelectedRowDataKeys(): string[];
    /**
     * Returns header information
     * @param {string} columnId
     * @returns {Header<unknown, unknown> | undefined}
     */
    getColumnHeader(columnId: string): Header<unknown, unknown> | undefined;
    /**
     * Returns data for a column
     * @param {string} columnId
     * @returns {Array<Record<string, string | number | boolean | object>>}
     */
    getColumnData(columnId: string): Array<Record<string, string | number | boolean | object>>;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-table': McTable;
    }
}
//# sourceMappingURL=index.d.ts.map