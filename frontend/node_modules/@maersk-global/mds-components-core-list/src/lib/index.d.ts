import { CSSResultArray, LitElement, TemplateResult, PropertyValues } from 'lit';
import type { McListValueType, Fit, Orientation, IMcListFilterType } from '@maersk-global/mds-shared-types';
import type { IMcList, IMcListItem, ListTypes, McListRole, MouseMoveVisibleArea, McListKeyNavigationType, IMcListDataItem } from './types';
import type { Width } from '@maersk-global/mds-components-core-button/src/lib/types';
import type { IMcInput } from '@maersk-global/mds-components-core-input/src/lib/types';
import '@maersk-global/mds-components-core-option';
import '@maersk-global/mds-components-core-list-item';
import '@maersk-global/mds-components-core-input';
export type { IMcList, IMcListDataItem } from './types';
/**
 * Purpose: Display a list of items for user selection
 * Category: Content Display, Navigation
 * When to use: When you need to present a list of choices to the user or allow to display navigation menus
 * When not to use: When you have a single option or a different selection method.
 * Works with components: mc-accordion-item, mc-card, mc-drawer, mc-dropdown, mc-list-item, mc-menu, mc-modal, mc-side-nav, mc-tab, mc-top-bar
 * Alternative component names: Option List, Selection List, Menu List, Navigation Menu
 *
 * @element `mc-list`
 *
 * @event {CustomEvent<IMcListChangeDetail>} listchange - Emitted when a list item is interacted.
 * @event {CustomEvent<IMcListItemsLoadedDetail>} listitemsloaded - Emitted when the list items are loaded.
 * @event {CustomEvent<IMcListFocusChangeDetail>} focuschange - Emitted when a list item receives focus.
 * @event {Event} scroll - Dispatched when the list is scrolled.
 *
 * @slot - The default slot where the content of the list goes, including list items or dividers.
 *
 * @csspart `list` - for changing visuals of list container
 */
export declare class McList extends LitElement implements IMcList {
    protected scrollToSelectedValue: boolean;
    /** When true, scroll selected item into view when clicked/selected by user */
    protected scrollSelectedIntoView: boolean;
    protected labelSelector: string;
    protected smoothScroll: boolean;
    protected listSearchValue?: string;
    items?: Array<IMcListItem | IMcInput>;
    protected allItems?: IMcListItem[];
    private headingGroups;
    protected currentItem?: IMcListItem;
    focusedItem?: IMcListItem | null;
    protected lastFocusedItem?: HTMLElement | IMcListItem | null;
    protected hoveredItem?: IMcListItem | null;
    protected keyboardNavigationActive: boolean;
    mousemovevisiblearea: MouseMoveVisibleArea | null;
    protected slottedListItems?: Array<IMcListItem>;
    data?: IMcListDataItem[] | null;
    list?: HTMLElement;
    listSearchInput: HTMLInputElement;
    listsearch: boolean;
    listsearchplaceholder: string;
    filtertype: IMcListFilterType;
    customfilter: ((text: string, value: string) => string[]) | undefined;
    matchlabelonly: boolean;
    disablehandlemousemove: boolean;
    disablecircularkeynav: boolean;
    disablesetfocus: boolean;
    scrolltofocused: boolean;
    hasactionslots: boolean;
    scrollablecontainer?: HTMLElement;
    fit: Fit;
    keynavigationtype: McListKeyNavigationType;
    noborder: boolean;
    orientation: Orientation;
    role: McListRole;
    type: ListTypes;
    value?: McListValueType | McListValueType[];
    width: Width;
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    private renderList;
    protected renderItemsFromData(): TemplateResult | (TemplateResult<1> | null)[] | null;
    protected renderOptionSearch(): TemplateResult | null;
    protected firstUpdated(changedProperties: PropertyValues): void;
    protected willUpdate(changedProperties: PropertyValues): void;
    protected updated(changedProperties: PropertyValues): void;
    private shouldKeyCauseKeyboardNav;
    protected handleListChange(item: IMcListItem | null | undefined): void;
    private getItemFromEventTarget;
    private handleClick;
    private handleFocusIn;
    private handleFocusOut;
    handleKeyDown(event: KeyboardEvent): void;
    private handleMouseOut;
    private handleMouseMove;
    private handleSlotChange;
    private setItems;
    private getSelectedValues;
    private markAsSelected;
    private passOnListCommonPropsToListItems;
    private setAriaAttributes;
    private getAllItems;
    private isListItem;
    private closestMcListItem;
    private buildListGroupMap;
    /**
     * Sets the focusedItem and adjusts tabindex on all items accordingly.
     */
    setFocusedItem(focusedItem?: IMcListItem | IMcInput, focus?: boolean): void;
    private updateActionsSlotTabIndex;
    private getItemsOfSuppliedValue;
    private normalizeMultipleValue;
    private scrollListIntoView;
    /**
     * Clears the list search input and resets all items to visible
     */
    clearListSearch(): void;
    /**
     * Clears the currently focused item to reset navigation state
     */
    clearFocusedItem(): void;
    private handleListSearch;
    private updateGroupHeadings;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-list': McList;
    }
}
//# sourceMappingURL=index.d.ts.map