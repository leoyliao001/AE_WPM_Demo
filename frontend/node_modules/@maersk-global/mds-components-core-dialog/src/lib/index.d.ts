import { TemplateResult, CSSResultArray } from 'lit';
import { IMcDialog } from './types';
import { McDialogBase } from './base';
export * from './base';
/**
 * Purpose: A dialog is a type of modal window that appears in front of app content to provide critical information or ask for a decision.
 * Category: Actions, Content Display
 * When to use: When you need to display important information or ask for user input.
 * When not to use: When you need to display non-critical information or when a simple alert would suffice.
 * Works with components: mc-button, mc-input, mc-select
 * Alternative component names: Modal, Popup, Lightbox
 *
 * @element `mc-dialog`
 *
 * @event {CustomEvent<McDialogAction>} closed -  The closed event gets dispatched once the dialog is closed.
 * @event {CustomEvent<McDialogAction>} closing - The closing event gets dispatched once the dialog is closing.
 * @event {CustomEvent} opened -  The opened event gets dispatched once the dialog is opened.
 * @event {CustomEvent} opening - The opening event gets dispatched once the dialog is opening.
 * @event {MouseEvent} click - The click event gets dispatched when clicked on the primaryAction/secondaryAction button within a dialog.
 *
 * @slot `default` - The body HTML to use for the dialog.
 * @slot `primaryAction` - The primaryAction HTML to use for the dialog. Could be used for i.e. custom confirm/cancel buttons.
 * @slot `secondaryAction` - The secondaryAction HTML to use for the dialog. Could be used for i.e. custom confirm/cancel buttons.
 *
 * @csspart `dialog` - for changing the whole dialog element
 * @csspart `header` - for changing the header element
 * @csspart `body-wrapper` - for changing the body wrapper
 * @csspart `footer` - for changing the footer element
 */
export declare class McDialog extends McDialogBase implements IMcDialog {
    static get styles(): CSSResultArray;
    constructor();
    protected renderFooter(): TemplateResult;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-dialog': McDialog;
    }
}
//# sourceMappingURL=index.d.ts.map