import { TemplateResult, CSSResultArray, PropertyValues } from 'lit';
import { McList } from '@maersk-global/mds-components-core-list';
import { PickerItemWidth } from '@maersk-global/mds-components-core-picker-item/src/lib/types';
import { IMcListItem } from '@maersk-global/mds-components-core-list-item/src/lib/types';
import { IMcPicker } from './types';
export type { IMcPickerSelectedDetail, IMcPicker } from './types';
export type { IMcListDataItem } from '@maersk-global/mds-components-core-list';
/**
 * Purpose: To allow users to select a value from a list by scrolling through options.
 * Category: Forms
 * When to use: When you need a compact way to select an item from a list.
 * When not to use: When you have a small number of items to choose from.
 * Works with components: mc-button, mc-picker-item
 * Alternative component names: Picker, Scrollable Selector, Wheel Selector, Scroll Picker
 *
 * @element `mc-picker`
 *
 * @event {CustomEvent<IMcPickerSelectedDetail>} pickerselected - Emitted upon an item scrolls to the middle.
 *
 * @slot - The default slot where the picker items go.
 *
 * @csspart `container` - for changing visuals of the selection area.
 *
 */
export declare class McPicker extends McList implements IMcPicker {
    private debouncedSetValue;
    private dispatchedItem?;
    private intersectionObserver?;
    private itemHeight;
    private selectedItem?;
    private selectionWindow?;
    private viewport?;
    focused: boolean;
    preventinitialeventdispatch: boolean;
    itemwidth?: PickerItemWidth;
    static get styles(): CSSResultArray;
    constructor();
    protected firstUpdated(changedProperties: PropertyValues): void;
    protected updated(changedProperties: PropertyValues): void;
    protected renderItemsFromData(): TemplateResult | (TemplateResult<1> | null)[] | null;
    render(): TemplateResult;
    private enableSmoothScroll;
    onPickerItemClick(event: {
        target: IMcListItem | McList;
    }): void;
    private onPickerItemFocus;
    private onPickerItemBlur;
    handleKeyDown(event: KeyboardEvent): void;
    focusWith(preventEventDispatch?: boolean): void;
    show(): void;
    hide(): void;
    private enableSelectionOnScroll;
    private onIntersection;
    private setItemHeight;
    private setValue;
    private scrollToView;
    private dispatch;
    private setSelectedItem;
}
//# sourceMappingURL=index.d.ts.map