import { LitElement, TemplateResult, CSSResultArray, PropertyValues } from 'lit';
import type { Fit } from '@maersk-global/mds-shared-types';
import { IMcSelectNative, LabelPosition, McSelectNativeOptions } from './types';
import '@maersk-global/mds-components-core-error';
import '@maersk-global/mds-components-core-hint';
import '@maersk-global/mds-components-core-icon';
import '@maersk-global/mds-components-core-label';
export type { IMcSelectNative } from './types';
declare const McSelectNative_base: (new (...args: any[]) => import("@maersk-global/mds-components-utils").FormFieldInterface) & typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: A native select component for selecting one option from a list.
 * Category: Forms
 * When to use: When you need to present a list of options for the user to choose from.
 * When not to use: For single input fields or when space is not a concern.
 * Works with components: mc-accordion-item, mc-card, mc-dialog, mc-drawer, mc-modal, mc-popover, mc-step-indicator-item, mc-table, mc-tabs
 * Alternative component names: Dropdown, ComboBox, Select Box, Picklist

* @element `mc-select-native`
 *
 * @event {CustomEvent<IMcSelectNativeChangeDetail>} change - Emitted when the selected status changes.
 *
 * @slot `label` - The label HTML to use for the mc-select-native.
 * @slot `hint` - The hint HTML to use for the select.
 * @slot `errormessage` - The errormessage HTML to use for the select.
 *
 * @csspart `label-container` - for changing visuals of label container
 * @csspart `label` - for changing visuals of label
 *
 */
export declare class McSelectNative extends McSelectNative_base implements IMcSelectNative {
    private isFirstUpdate;
    private _value?;
    private get isPlaceholderActive();
    private select?;
    protected focused: boolean;
    errormessage?: string;
    fit: Fit;
    hiddenlabel: boolean;
    hint?: string;
    id: string;
    private get inputId();
    invalid: boolean;
    label: string;
    labelposition: LabelPosition;
    options?: McSelectNativeOptions;
    placeholder: string;
    required: boolean;
    selectedindex?: number[];
    set value(newValue: McSelectNativeOptions | null | undefined);
    get value(): McSelectNativeOptions | null | undefined;
    variant: 'default' | 'vanity' | 'multiple';
    width: string;
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    protected renderField(): TemplateResult;
    protected renderLabel(): TemplateResult;
    protected renderError(): TemplateResult;
    protected renderHint(): TemplateResult;
    private renderSelectOptions;
    private isOptionSelected;
    private renderPlaceholder;
    private renderIcon;
    private handleOnChange;
    private getSelected;
    focus(): void;
    protected onInputFocus(): void;
    blur(): void;
    protected onInputBlur(): void;
    firstUpdated(changedProperties: PropertyValues): void;
    willUpdate(changedProperties: PropertyValues): void;
    private initializeElements;
    /**
     * Returns true if the value is valid, otherwise false.
     * In Vue2 sometimes the new value is being supplied with invalid values.
     * Remove this function if Vue2 in our organisation is deprecated.
     * Not having this logic in place, would break vee-validate, or
     * it won't be possible to set the initial value on the select component.
     */
    private static isValueValidInVue2;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-select-native': McSelectNative;
    }
}
//# sourceMappingURL=index.d.ts.map