import { LitElement, TemplateResult, CSSResultArray, PropertyValues } from 'lit';
import { TooltipAppearance, IMcTooltip, TooltipPosition, TooltipWidth } from './types';
import type { Fit, Trigger } from '@maersk-global/mds-shared-types';
import '@maersk-global/mds-components-core-popover';
export type { IMcTooltip } from './types';
/**
 * Purpose: A tooltip component for displaying informative text when users hover over, focus on, or tap an element.
 * Category: Forms, Actions
 * When to use: When you need to provide additional information about a UI element on hover, focus, or tap.
 * When not to use: When the information is critical and must be always visible, consider using a different UI pattern.
 * Works with components: mc-button, mc-icon, mc-input, mc-select, mc-checkbox
 * Alternative component names: Info Tooltip, Help Tooltip, Hover Text, Info Bubble, Hint
 *
 * @element `mc-tooltip`
 *
 * @event {CustomEvent} show - Dispatched when tooltip opens
 * @event {CustomEvent} hide - Dispatched when tooltip hides
 *
 * @csspart `popover-container` - for changing visuals of the tooltip content area.
 * @csspart `popover-arrow` - for changing visuals of the tooltip arrow.
 *
 * @slot - The HTML content of the mc-tooltip.
 * @slot `trigger` - The trigger element which toggles the tooltip. Not used when customtriggerelement property is set.
 */
export declare class McTooltip extends LitElement implements IMcTooltip {
    appearance: TooltipAppearance;
    fit: Fit;
    open: boolean;
    position: TooltipPosition;
    width: TooltipWidth;
    dontflip: boolean;
    dontshift: boolean;
    opendelay: number;
    zindex?: number;
    customtriggerelement?: string | HTMLElement;
    trigger: Trigger;
    private contentSlot;
    private hasContentSlot;
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    private computeWidth;
    protected firstUpdated(changedProperties: PropertyValues): void;
    protected onContentSlotChange(): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-tooltip': McTooltip;
    }
}
//# sourceMappingURL=index.d.ts.map