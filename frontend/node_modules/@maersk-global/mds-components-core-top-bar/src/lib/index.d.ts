import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import type { IMcTopBar, Target, TopBarLogoSize } from './types';
/**
 * Purpose: A top bar component for displaying a product name, logo, and additional actions.
 * Category: Layout, Navigation
 * When to use: When you need a consistent header area for your application with branding and navigation elements.
 * When not to use: n/a
 * Works with components: mc-button, mc-icon, mc-link, mc-list
 * Alternative component names: App Bar, Header Bar, Navigation Bar, Masthead, Banner
 *
 * @element `mc-top-bar`
 *
 * @slot `default` - Slot where you can drop your mega menu, search bar, or any other component if needed.
 * @slot `actions` - Slot where you can drop your extra icons with actions.
 * @slot `link` - Slot where you can drop a router link to the home page in a SPA application.
 *
 */
export declare class McTopBar extends LitElement implements IMcTopBar {
    logosize: TopBarLogoSize;
    product: string;
    productshort: string;
    href: string;
    rel: string;
    target: Target | undefined;
    private productNameCharactersLimit;
    private productShortNameCharactersLimit;
    private defaultSlot;
    private hasDefaultSlot;
    private actionsSlot;
    private hasActionsSlot;
    private get productName();
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    private renderLogoAndProduct;
    private renderProducts;
    private onDefaultSlotChange;
    private onActionsSlotChange;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-top-bar': McTopBar;
    }
}
//# sourceMappingURL=index.d.ts.map