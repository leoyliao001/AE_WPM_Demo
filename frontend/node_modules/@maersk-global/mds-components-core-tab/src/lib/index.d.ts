import { TemplateResult, CSSResultArray, LitElement, PropertyValues } from 'lit';
import { Fit, IMcTab } from './types';
import { Width } from '@maersk-global/mds-components-core-button/src/lib/types';
export type { IMcTab } from './types';
declare const McTab_base: typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: A tabbed interface that allows users to navigate between different views or sections of content within the same page.
 * Category: Content Display, Navigation, Layout
 * When to use: When you need to display multiple views or sections of content in a compact space.
 * When not to use: For single-view interfaces or when a simple dropdown menu suffices.
 * Works with components: mc-tab-bar
 * Alternative component names: Tabbed Navigation, Tabbed Interface, Tab Control, Tab Panel, Tab View
 *
 * @element `mc-tab`
 *
 * @event {CustomEvent} disabledchange - Fired when disabled status of a tab changes.
 *
 * @slot - The default slot where main label goes.
 * @slot `prefix` - The prefix HTML to use for the mc-tab.
 * @slot `suffix` - The suffix HTML to use for the mc-tab.
 * @slot `badge` - Used if you want to attach badge to the mc-avatar.
 *
 * @csspart `button` - for changing visuals of button.
 * @csspart `icon` - for changing visuals of icon
 * @csspart `text-and-icon` - for changing visuals of text and icons container
 */
export declare class McTab extends McTab_base implements IMcTab {
    private button?;
    index?: number;
    haseventlisteners: boolean;
    id: string;
    label?: string;
    icon: string;
    trailingicon: string;
    fit: Fit;
    active: boolean;
    tabindex?: number;
    width: Width;
    static get styles(): CSSResultArray;
    focus(): void;
    blur(): void;
    protected render(): TemplateResult;
    protected firstUpdated(changedProperties: PropertyValues): void;
    protected updated(changedProperties: PropertyValues): void;
    private initializeElements;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-tab': McTab;
    }
}
//# sourceMappingURL=index.d.ts.map