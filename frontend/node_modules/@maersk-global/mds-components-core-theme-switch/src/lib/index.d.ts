import { TemplateResult, CSSResultArray, LitElement, PropertyValues } from 'lit';
import { Fit } from '@maersk-global/mds-shared-types';
import { IMcThemeSwitch, ThemeOptions } from './types';
import '@maersk-global/mds-components-core-button';
/**
 * Purpose: A button that allows the user to switch between light and dark themes.
 * Category: Layout, Content Display
 * When to use: When you need to provide a way for users to toggle between light and dark themes.
 * When not to use: When the application only supports a single theme.
 * Works with components: mc-top-bar
 * Alternative component names: Theme Toggle, Dark Mode Switch, Light/Dark Mode Toggle
 * Extra notes: Implementation additional step: this component requires that the application has two stylesheets linked in the document head with the following attributes: <link rel="stylesheet" href="path/to/light-theme.css" media="prefers-color-scheme: light"> or <link rel="stylesheet" href="path/to/dark-theme.css" media="prefers-color-scheme: dark">
 *
 * @element `mc-theme-switch`
 *
 * @event {CustomEvent<IMcThemeSwitchChangeEvent>} change - Fired when the theme is changed.
 */
export declare class McThemeSwitch extends LitElement implements IMcThemeSwitch {
    private LSThemeCategory;
    private LSThemeKey;
    theme: ThemeOptions;
    fit: Fit;
    private styleLight;
    private styleDark;
    private browserScheme;
    private nextTheme;
    private icon;
    constructor();
    connectedCallback(): void;
    disconnectedCallback(): void;
    static get styles(): CSSResultArray;
    private onSystemColorSchemeChange;
    willUpdate(_changedProperties: PropertyValues): void;
    private get label();
    render(): TemplateResult;
    /**
     * Changes the theme of the component on the click of the button.
     * @event change
     */
    changeTheme(): void;
    private fireChangeEvent;
    private switchTheme;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-theme-switch': McThemeSwitch;
    }
}
//# sourceMappingURL=index.d.ts.map