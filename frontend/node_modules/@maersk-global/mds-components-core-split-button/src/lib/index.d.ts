import { TemplateResult, CSSResultArray } from 'lit';
import { IMcSplitButton } from './types';
import { McButton } from '@maersk-global/mds-components-core-button';
import '@maersk-global/mds-components-core-list';
import '@maersk-global/mds-components-core-list-item';
import '@maersk-global/mds-components-core-menu';
/**
 * Purpose: Integrates a main action button (`mc-button`) with a dropdown menu (`mc-menu`), allowing users to easily perform the main action and choose from additional options, all within a single, space-saving component.
 * Category: Actions, Navigation
 * When to use: Use when you want to present a main action (e.g., "Save", "Download") and offer additional options via a dropdown, reducing clutter and grouping related actions together.
 * When not to use: Avoid when only one action is needed, or when all actions should be equally visible and accessible as separate buttons.
 * Works with components: mc-list, mc-list-item
 * Alternative component names: Dropdown Button, Action Menu Button
 *
 * @element `mc-split-button`
 *
 * @event {MouseEvent} `click` - Emitted when the button is clicked
 * @event {FocusEvent} `focus` - Emitted when the button receives focus
 * @event {FocusEvent} `blur` - Emitted when the button loses focus
 *
 * @slot - The default slot for the button label. Used to display custom text/HTML content in the button.
 * @slot `icon` - The icon HTML to use for the button.
 * @slot `menu` - Content for the dropdown menu. Pass <mc-list> with <mc-list-item> elements and bind events directly to mc-list (e.g., @listchange).
 *
 * @csspart `button` - for changing visuals of the main button
 * @csspart `split-button-menu` - for changing visuals of the menu button
 * @csspart `icon` - for changing visuals of the main button icon
 * @csspart `split-button-icon` - for changing visuals of the menu button icon (chevron)
 * @csspart `text-and-icon` - for changing visuals of the text and icons container
 *
 */
export declare class McSplitButton extends McButton implements IMcSplitButton {
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    protected renderMenu(): TemplateResult;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-split-button': McSplitButton;
    }
}
//# sourceMappingURL=index.d.ts.map