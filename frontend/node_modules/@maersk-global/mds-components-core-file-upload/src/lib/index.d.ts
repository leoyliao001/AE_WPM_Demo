import { CSSResultArray, PropertyValues, TemplateResult } from 'lit';
import { Fit } from '@maersk-global/mds-shared-types';
import { McTextAndIcon } from '@maersk-global/mds-components-core-text-and-icon';
import { FileUploadVariant, FileStatus, IMcFileUpload } from './types';
import '@maersk-global/mds-components-core-button';
import '@maersk-global/mds-components-core-error';
import '@maersk-global/mds-components-core-hint';
import '@maersk-global/mds-components-core-icon';
export type { IMcFileUpload } from './types';
declare const McFileUpload_base: (new (...args: any[]) => import("@maersk-global/mds-components-utils").FormFieldInterface) & typeof McTextAndIcon & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: Component for uploading files, supporting single and multiple file selection, drag-and-drop functionality, and file status indicators.
 * Category: Forms
 * When to use: When you need to allow users to upload files.
 * When not to use: When you don't need file uploads.
 * Works with components: mc-card, mc-drawer, mc-modal
 * Alternative component names: File Upload, File Picker
 *
 * @element `mc-file-upload`
 *
 * @event {CustomEvent<McFileUploadInputDetail>} input - Fired when single/multiple files are selected, or removed.
 * @event {CustomEvent<McFileUploadInputDetail>} change - Fired when single/multiple files are selected.
 *
 * @slot `legend` - The legend HTML to use for the mc-file-upload.
 * @slot `icon` - The icon HTML
 * @slot `trailingicon` - The trailing icon HTML
 * @slot `intermediate` - The intermediate icon HTML
 * @slot `file-url-${index}` - The file URL HTML, which is displayed in the file list.
 * @slot `hint` - The hint HTML to use for the mc-file-upload.
 * @slot `hint-${fileName}` - The hint HTML to use for file in the mc-file-upload.
 * @slot `errormessage` - The errormessage HTML to use for the mc-file-upload.
 * @slot `error-${fileName}` - The errormessage HTML to use for file in the mc-file-upload.
 *
 * @csspart `button` - for changing visuals of button.
 * @csspart `icon` - for changing visuals of icon
 * @csspart `text-and-icon` - for changing visuals of text and icons container
 * @csspart `drag-drop-area` - for changing visuals of drag-drop area, when drag-drop variant is selected.
 */
export declare class McFileUpload extends McFileUpload_base implements IMcFileUpload {
    protected controlType: string;
    icon: string;
    trailingicon: string;
    private fileInput?;
    private legendElements;
    private hasSlotLegend;
    private draggedOver;
    private get legendVisible();
    private hintElements;
    private hasSlotHint;
    private get hintVisible();
    private errorElements;
    private hasSlotError;
    private get errorVisible();
    accept: string;
    /**
     * @deprecated Use `label` instead.
     */
    dragdroplabel: string;
    errormessage?: string;
    files?: FileList;
    filesstatus?: FileStatus[];
    fit: Fit;
    hiddenlegend: boolean;
    hint?: string;
    allowduplicatefilenames?: boolean;
    invalid: boolean;
    legend?: string | null;
    loading: boolean;
    multiple: boolean;
    tabindex?: number;
    value?: string;
    variant?: FileUploadVariant;
    hiddenfilelist?: boolean;
    static get styles(): CSSResultArray;
    constructor();
    firstUpdated(changedProperties: PropertyValues): Promise<void>;
    willUpdate(changedProperties: PropertyValues): void;
    protected renderHint(id: string, hint?: string): TemplateResult;
    protected renderErrorMessage(id: string, error?: string, invalid?: boolean): TemplateResult;
    protected renderFileStatusIcon(fileName: string, index: number, fileStatus?: FileStatus): TemplateResult;
    render(): TemplateResult;
    private onSelectorClick;
    private onFileRemove;
    private handleInput;
    private handleDragOver;
    private handleFileChange;
    private isFileAccepted;
    private handleDrop;
    private convertArrayToFileList;
    private updateFiles;
    private ensureFilenameIsUnique;
    private dispatchInputEvent;
    private dispatchChangeEvent;
    private onLegendSlotChange;
    private onHintSlotChange;
    private onErrorSlotChange;
}
//# sourceMappingURL=index.d.ts.map