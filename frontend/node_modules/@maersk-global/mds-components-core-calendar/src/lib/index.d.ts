import { CSSResultArray, LitElement, PropertyValues, TemplateResult } from 'lit';
import { Dayjs } from 'dayjs';
import type { IMcCalendar, CalendarCell } from './types';
import type { CellCustomizationPayload } from '@maersk-global/mds-shared-types';
import { McButton } from '@maersk-global/mds-components-core-button';
import type { McCalendarMonthYearPicker } from './components/mc-calendar-month-year-picker';
import './components/mc-calendar-month-year-picker';
import '@maersk-global/mds-components-core-button';
export type { IMcCalendar } from './types';
/**
 * A calendar component that allows users to select dates and view date-related information.
 * Category: Forms, Actions
 * Purpose: Display calendar interface for date selection and navigation.
 * When to use: Date selection, Scheduling, Booking systems
 * When not to use: Displaying static dates, Non-interactive date views
 * Works with components: mc-card, mc-drawer, mc-input-date, mc-modal, mc-popover
 * Alternative component names: Date Picker, Date Selector, Calendar Widget
 *
 * @element `mc-calendar`
 *
 * @event {CustomEvent<IMcCalendarDateSelectedDetail>} dateselected - Fired when the date is selected.
 *
 * @slot `footer` - Footer slot.
 *
 * @csspart `calendar` - for changing visuals of the calendar.
 * @csspart `button` - used together with `customstyles` for changing visuals of button inside the calender
 */
export declare class McCalendar extends LitElement implements IMcCalendar {
    set activedate(value: string | null);
    get activedate(): string | null;
    customstyles?: string;
    format: string;
    locale: string;
    nextlabel: string;
    previouslabel: string;
    value: string;
    dayperiod: 'long' | 'short' | 'narrow';
    showadjacentmonthdays: boolean;
    showweeknumbers: boolean;
    noshadow: boolean;
    noborder: boolean;
    yearcap: number;
    get min(): Dayjs | string | null;
    set min(date: Dayjs | string | null);
    get max(): Dayjs | string | null;
    set max(date: Dayjs | string | null);
    set customize(data: CellCustomizationPayload[] | undefined);
    get customize(): CellCustomizationPayload[] | undefined;
    set startofweek(day: number);
    get startofweek(): number;
    private _customize?;
    private _max;
    private _min;
    private hasFooterSlot;
    private activeDate?;
    private cachedWeekdays;
    private cachedWeeks;
    private firstDayOfWeek;
    private firstWeekOffset;
    private focusableDate?;
    private periodButtonText;
    private selectedDate;
    private todayValue;
    private weekdays;
    private showMonthYearPicker;
    private weeks;
    private eventReferences?;
    private get flattenedWeeks();
    protected previousButtonElement?: McButton;
    protected nextButtonElement?: McButton;
    protected monthYearPicker?: McCalendarMonthYearPicker;
    protected currentPeriodElement?: McButton;
    constructor();
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    focus(): void;
    private renderTemplateStyles;
    /**
     * Renders calendar header
     * @returns {TemplateResult}
     */
    private renderHeader;
    /**
     * Renders calendar body
     * @returns {TemplateResult}
     */
    private renderBody;
    private renderWeekNumbersHeader;
    private renderWeeks;
    private renderWeekNumber;
    /**
     * Renders calendar cell
     * @returns {TemplateResult}
     */
    protected renderCell(cell: CalendarCell): TemplateResult;
    private renderFooterSlot;
    private renderMonthYearPicker;
    firstUpdated(): void;
    protected willUpdate(changedProperties: PropertyValues): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    private addEventListeners;
    private removeEventListeners;
    /**
     * Handles `keydown` event.
     * Makes sure the focus is trapped within the calendar when pressing `Tab`.
     * @param {KeyboardEvent} event
     */
    private onKeyDown;
    /**
     * Focuses the next enabled button to trap focus.
     * @param {KeyboardEvent} event
     * @param {KeyboardEvent} first When true, will focus the first enabled button, last otherwise.
     */
    private focusNextEnabledButton;
    /**
     * Handles `keyup` event.
     * Moves the focus around the calendar days
     * @param {KeyboardEvent} event
     */
    private onKeyUp;
    /**
     * Focuses the next enabled day depending on the delta.
     * If there is no date available it will try to focus the first day on the next month view.
     * @param {numeber} delta how many days to add to the current focusableDate.
     */
    private focusNextDay;
    /**
     * Focuses the previous enabled day depending on the delta.
     * If there is no date available it will try to focus the last day on the previous month view.
     * @param {numeber} delta how many days to substract from the current focusableDate.
     */
    private focusPreviousDay;
    /**
     * Gets the next date that is enabled.
     * @param {Dayjs} currentDay baseline dates, from which the search will start
     * @param {numeber} delta how many days to add to the current focusableDate.
     * @returns {Dayjs}
     */
    private getNextAvailableDay;
    /**
     * Gets the next date that is enabled.
     * @param {Dayjs} currentDay baseline dates, from which the search will start
     * @param {numeber} delta how many days to add to the current focusableDate.
     * @returns {Dayjs}
     */
    private getPreviousEnabledDay;
    /**
     * Sets the current month to the previous one.
     */
    private previousClicked;
    /**
     * Sets the current month to the next one.
     */
    private nextClicked;
    /**
     * Creates weekdays array.
     * @returns {Weekday[]}
     */
    private createWeekdays;
    /**
     * Creates week cells array.
     * @param {Dayjs} activeDate
     * @param {Number} firstWeekOffset
     * @param {CellCustomizationPayload[]} customize
     * @returns {Week[]}
     */
    private createWeekCells;
    private getIndicatorAppearanceVariable;
    private handleMinMaxSet;
    selectDate(date: Dayjs | string, emitEvent?: boolean): void;
    private adjustActiveDateIfNeeded;
    /**
     * Aligns focusable, selected and active dates
     * @param {Dayjs | Date | string} date new date to set
     */
    private alignInternalDates;
    /**
     * Get a cache key
     * @param {Dayjs} activeDate
     * @param {number | string} extension
     * @returns {string}
     */
    private getCacheKey;
    /**
     * Focuses an element corresponding to the supplied date.
     * @param {Dayjs} date
     */
    private focusDate;
    /**
     * Checks if the provided date is the same as the currently selected one.
     * @param {Dayjs} date
     * @returns {boolean}
     */
    private isSelected;
    /**
     * Checks if two dates are the same excluding the hour/date/minute
     * @param {Dayjs} date1
     * @param {Dayjs} date2
     * @returns {boolean}
     */
    private isSameDate;
    /**
     * Determines the `tabindex` attribute of a cell. If the cell is supposed to be focusable by `Tab`, returns 0.
     * @param {CalendarCell} cell
     * @returns {number}
     */
    private getTabIndex;
    /**
     * Computes date customizations(enabled & customClasses)
     * @param {Dayjs} date
     * @param {CellCustomizationPayload[]} customize
     * @returns {DateCustomizations}
     */
    private computeDateCustomizations;
    private extractCustomClasses;
    /**
     * Computes values needed to correctly display calendar view
     */
    private computeCalendarViewValues;
    /**
     * Gets the first date of the currently displayed month
     * @returns {Dayjs}
     */
    private getCurrentMonthFirstEnabledDay;
    /**
     * Gets last date of the currently displayed month
     * @returns {Dayjs}
     */
    private getCurrentMonthLastEnabledDay;
    /**
     * Gets the Intl formatter used to format dates in locales
     * @param {Intl.DayjsFormatOptions} options
     * @returns {(date: Date) => string}
     */
    private getIntlFormatter;
    /**
     * Gets the days that are in the allowed range.
     * @returns {CalendarCell[]}
     */
    private getEnabledDays;
    /**
     * Gets the day to focus when initially Tabbing into the calendar. Today by default.
     * @returns {Dayjs}
     */
    private getEnabledFocusableDay;
    /**
     * Gets the locale month and year to display in the calendar header.
     * @returns {string}
     */
    private getPeriodTitle;
    /**
     * Gets the appropriate weekday display text based on dayperiod setting.
     * @param {Weekday} day The weekday object containing long and current format
     * @returns {string}
     */
    private getWeekdayDisplayText;
    /**
     * Gets the number of days to offset depending on the start of the week.
     * @returns {number}
     */
    private getFirstWeekOffSet;
    /**
     * Aligns focusable and active dates to have matching months.
     * Used in PageUp/PageDown handlers.
     */
    private alignFocusableAndActiveDates;
    private toggleMonthYearPicker;
    private openMonthYearPicker;
    private closeMonthYearPicker;
    private handleMonthYearClosed;
    private handleMonthYearSelected;
    private getNewDateFromMonthYearEvent;
    private handleMonthYearPickerFocusout;
    private handleFooterSlotChange;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-calendar': McCalendar;
    }
}
//# sourceMappingURL=index.d.ts.map