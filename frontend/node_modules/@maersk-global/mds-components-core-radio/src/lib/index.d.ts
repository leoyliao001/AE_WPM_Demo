import { CSSResultArray, LitElement, TemplateResult, PropertyValues } from 'lit';
import { Fit, IMcRadio } from './types';
import '@maersk-global/mds-components-core-label';
export type { IMcRadio } from './types';
declare const McRadio_base: (new (...args: any[]) => import("@maersk-global/mds-components-utils").FormFieldInterface) & typeof LitElement & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IDisabledState);
/**
 * Purpose: Display a radio with optional labels, hints, and error messages.
 * Category: Forms, Actions
 * When to use: Forms, Surveys, User preferences
 * When not to use: Decorative purposes, Non-interactive elements
 * Works with components: mc-radio-group, mc-card, mc-modal, mc-drawer, mc-notification
 * Alternative component names: Radio Button, Option Button, Selection Button
 *
 * @element `mc-radio`
 *
 * @event {MouseEvent} click - Fired when mc-radio is clicked.
 * @event {CustomEvent<boolean>} change - Fired when mc-radio in checked/unckecked.
 * @event {FocusEvent} focus - Fired when mc-radio is focused.
 * @event {FocusEvent} blur - Fired when mc-radio is going out of focus.
 *
 * @slot `label` - The label HTML to use for the mc-radio.
 *
 * @csspart `label-container` - for changing visuals of label container
 * @csspart `label` - for changing visuals of label
 * @csspart `checkmark` - for changing visuals of checkmark
 *
 */
export declare class McRadio extends McRadio_base implements IMcRadio {
    private _selectionController?;
    private _checked;
    protected controlType: string;
    protected focused: boolean;
    input?: HTMLInputElement | null;
    fit: Fit;
    hiddenlabel: boolean;
    label: string;
    readonly: boolean;
    tabindex: number;
    value: string;
    /**
     * Gets the state of the radio.
     * @returns {boolean}
     */
    get checked(): boolean;
    set checked(isChecked: boolean);
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    firstUpdated(changedProperties: PropertyValues): void;
    disconnectedCallback(): void;
    private handleInput;
    focus(): void;
    protected onInputFocus(): void;
    blur(): void;
    protected onInputBlur(): void;
    click(): void;
    private preventClick;
    private onInputClick;
    changeHandler(): void;
    private registerSingleSelectionController;
    private unregisterSingleSelectionController;
    private dispatchChangeIfChecked;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-radio': McRadio;
    }
}
//# sourceMappingURL=index.d.ts.map