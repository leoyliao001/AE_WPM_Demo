import { LitElement, TemplateResult, CSSResultArray, PropertyValues } from 'lit';
import { Fit } from '@maersk-global/mds-shared-types';
import { IMcAccordion, ExpandIconPosition } from './types';
/**
 * Purpose: Display collapsible content panels for better organization and navigation.
 * Category: Content Display
 * When to use: FAQs, Nested content, Disclosure sections
 * When not to use: Always expanded content, Simple lists
 * Works with components: mc-accordion-item
 * Alternative component names: Accordion, Collapsible Panel, Expandable Section
 *
 * @element `mc-accordion`
 *
 * @event {CustomEvent<IMcAccordionChangeDetail>} `change` - Emitted when an accordion item's expanded state changes.
 *
 * @slot - The default slot where the accordion items go.
 *
 * @csspart `container` - The main container for the accordion.
 */
export declare class McAccordion extends LitElement implements IMcAccordion {
    fit: Fit;
    expandiconposition: ExpandIconPosition;
    singleexpand: boolean;
    withborder: boolean;
    private accordionItems;
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    protected updated(changedProperties: PropertyValues): void;
    private handleSlotChange;
    private updateAccordionItemsFit;
    private handleAccordionItemToggle;
    connectedCallback(): void;
    disconnectedCallback(): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-accordion': McAccordion;
    }
}
//# sourceMappingURL=index.d.ts.map