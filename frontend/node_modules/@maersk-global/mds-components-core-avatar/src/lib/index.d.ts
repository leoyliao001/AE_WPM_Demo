import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import { IMcAvatar, FitExtended, AvatarAppearance } from './types';
import '@maersk-global/mds-components-core-tooltip';
import '@maersk-global/mds-components-core-button';
export type { IMcAvatar } from './types';
/**
 * Purpose:
 * Category:
 * When to use:
 * When not to use:
 * Works with components:
 * Alternative component names:
 *
 */
/**
 * Purpose: Display user avatars with optional tooltips and badges
 * Category: Content Display
 * When to use: User profiles, Comment sections, Team member lists
 * When not to use: Decorative images, Large hero images
 * Works with components: mc-accordion-item, mc-card
 * Alternative component names: User Avatar, Profile Picture, User Icon
 *
 * @element `mc-avatar`
 *
 * @event {MouseEvent} `click` - Emitted when the avatar is clicked.

 * @slot `info` - The info HTML to use for the mc-avatar.
 * @slot `badge` - Used if you want to attach badge to the mc-avatar.
 *
 * @csspart `avatar` - for changing visuals of image/initials
 * @csspart `tooltip-container` - for changing visuals of tootlip used in avatar
 * @csspart `tooltip-arrow` - for changing visuals of tootltip's arrow
 *
 */
export declare class McAvatar extends LitElement implements IMcAvatar {
    private emptyImage;
    appearance: AvatarAppearance;
    fit: FitExtended;
    hiddentooltip: boolean;
    clickable: boolean;
    imagesrc: string;
    info: string | undefined;
    initials: string;
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    private renderAvatarWithTooltip;
    private renderClickableAvatar;
    private renderBaseAvatar;
    private renderAvatarImage;
    private _onTriggerClick;
    private _onTriggerKeyDown;
    private _dispatchHostClick;
}
//# sourceMappingURL=index.d.ts.map