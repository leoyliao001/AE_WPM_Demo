import { TemplateResult, LitElement, PropertyValues } from 'lit';
import { IMcMenu } from './types';
import { IMcPopover } from '@maersk-global/mds-components-core-popover/src/lib/types';
import { Position, Trigger, Fit } from '@maersk-global/mds-shared-types';
import '@maersk-global/mds-components-core-popover';
export type { IMcMenu } from './types';
/**
 * Purpose: Dropdown menu for navigation and actions
 * Category: Navigation, Actions
 * When to use: When you need to provide a list of actions or links in a compact space.
 * When not to use: When you need to display a large amount of content or complex layouts.
 * Works with components: mc-button, mc-list, mc-popover, mc-top-bar
 * Alternative component names: Dropdown, Context Menu, Action Menu, Options Menu, Flyout Menu
 *
 * @element `mc-menu`
 *
 * @slot - The default slot where the content of the list goes, including list items or dividers.
 * @slot trigger - The trigger slot where the element which triggers the menu must go.
 */
export declare class McMenu extends LitElement implements IMcMenu {
    private mcList?;
    private triggerElements?;
    protected mcPopover?: IMcPopover;
    arrow: boolean;
    fit: Fit;
    maxwidth?: string;
    maxheight?: string;
    open: boolean;
    disablecloseonselect: boolean;
    position: Position;
    trigger: Trigger;
    contextmenuonside: boolean;
    zindex?: number;
    firstUpdated(changedProperties: PropertyValues): void;
    updated(changedProperties: PropertyValues): void;
    protected render(): TemplateResult;
    private handlePopoverShow;
    private handlePopoverHide;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-menu': McMenu;
    }
}
//# sourceMappingURL=index.d.ts.map