import { CSSResultArray, PropertyValues, TemplateResult } from 'lit';
import { IMcInputDate } from './types';
import { CellCustomizationPayload, Position } from '@maersk-global/mds-shared-types';
import '@maersk-global/mds-components-core-calendar';
import '@maersk-global/mds-components-core-popover';
import '@maersk-global/mds-components-core-drawer';
import { McInput } from '@maersk-global/mds-components-core-input';
export { INPUT_DATE_DEFAULTS } from './types';
export type { IMcInputDate } from './types';
declare const McInputDate_base: typeof McInput & (new (...args: any[]) => import("@maersk-global/mds-components-utils").IResponsive);
/**
 * Purpose: Component for user date input with various customization options.
 * Category: Forms
 * When to use: When you need to collect user date input in a form.
 * When not to use: When you need to display non-input content.
 * Works with components: mc-accordion-item, mc-card, mc-dialog, mc-drawer, mc-modal, mc-popover, mc-step-indicator-item, mc-table, mc-tabs
 * Alternative component names: Date Input, Form Date Input, Date Field, Date Picker
 *
 * @element `mc-input-date`
 *
 * @event {InputEvent} input - Fired when the character is entered.
 * @event {CustomEvent<string>} inputdateselected - Fired only when date is selected in the calendar.
 * @event {FocusEvent} focus - Fired when mc-input-date is focused.
 * @event {FocusEvent} blur - Fired when mc-input-date is going out of focus.
 * @event {MouseEvent} click - Fired on mc-input-date click.
 * @event {KeyboardEvent} keydown - Fired when a key on keydown is pressed.
 * @event {CustomEvent} clearbuttonclick - Fires when the clear button is pressed.
 * @event {CustomEvent} trailingiconclick - Fires when the trailing icon (calendar) is clicked.
 *
 * @slot `label` - The label HTML to use for the mc-input-date.
 * @slot `hint` - The hint HTML to use for the mc-input-date.
 * @slot `errormessage` - The errormessage HTML to use for the mc-input-date.
 * @slot `footer` - Calendar footer slot.
 *
 * @csspart `label-container` - for changing visuals of label container
 * @csspart `label` - for changing visuals of label
 * @csspart `input` - for changing visuals of input field
 * @csspart `icon` - for changing visuals of icons
 */
export declare class McInputDate extends McInputDate_base implements IMcInputDate {
    private readonly internals;
    private selectedDate;
    static get formAssociated(): boolean;
    get validity(): ValidityState;
    private actionKeys;
    protected isTrailingIconFocused: boolean;
    activedate: string | null;
    customize?: CellCustomizationPayload[];
    customstyles?: string;
    format: string;
    locale: string;
    nextlabel: string;
    dayperiod: 'long' | 'short' | 'narrow';
    placeholder: string;
    calendarposition: Position;
    open?: boolean;
    previouslabel: string;
    startofweek: number;
    showadjacentmonthdays: boolean;
    showweeknumbers: boolean;
    usemask: boolean;
    yearcap: number;
    private mcCalendar?;
    private mcPopover?;
    private mcDrawer?;
    constructor();
    static get styles(): CSSResultArray;
    render(): TemplateResult;
    private renderDrawer;
    private renderPopover;
    protected renderCalendar(): TemplateResult | undefined;
    connectedCallback(): void;
    firstUpdated(changedProperties: PropertyValues): Promise<void>;
    willUpdate(changedProperties: PropertyValues): void;
    protected initializeMaskController(): void;
    private onCalendarShown;
    private onCalendarHidden;
    private handleInputInteraction;
    private handleTrailingIconClick;
    private handleKeyDown;
    private focusCalendar;
    private handleCalendarKeyDown;
    /**
     * Handles `dateselected` event.
     * Sets the value to the selected date and closes the calendar.
     * @param {CustomEvent} event
     */
    private onDateSelected;
    /** Extends the McInput handleInputChange to assign selected date when user types it in  */
    protected handleInputChange(): void;
    private setSelectedDate;
    private getMaskFromFormat;
    private closeCalendar;
    private validate;
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-input-date': McInputDate;
    }
}
//# sourceMappingURL=index.d.ts.map