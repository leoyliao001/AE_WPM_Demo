import type { BrandCode, EnvironmentCode } from "../../config";

export interface IGNSettings {
  ASSETS_ENV: EnvironmentCode;
  brand: BrandCode;
  version: string;
  disableOpacityChange: boolean;
  beta: boolean;
}

export type IGNConfig = {
  headerId: string;
  brandCode: BrandCode;
  assetsLocationMap: Record<string, string>;
  settings: IGNSettings;
};

declare global {
  interface Window {
    projectServices: IGNSettings;
    forceIGN: boolean;
  }
}
