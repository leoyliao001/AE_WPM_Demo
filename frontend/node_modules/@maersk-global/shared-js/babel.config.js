/**
 * This babel config sets how this module will be transpiled when rollup does its build
 * The target is then overridden in Jest so that the regeneratorRuntime is not required
 * Please don't mess with this file unless you really know what you're doing
 */
module.exports = {
  presets: ["@babel/preset-env", "@babel/preset-typescript"],
  plugins: ["babel-plugin-transform-async-to-promises"], // Means we won't need regenerator-runtime
  env: {
    test: {
      presets: [
        [
          "@babel/preset-env",
          {
            targets: {
              node: "current" // When running in test mode (Jest) it should behave like NodeJS
            }
          }
        ]
      ]
    }
  }
};
