/**
 * Generates a key for local storage based on the key and category
 * @param key key name
 * @param category category for the key
 * @returns [category]key or just key
 */
export declare function makeKey(key: string, category?: string): string;
