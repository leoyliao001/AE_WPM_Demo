import type { BrandCode, GlobalBaseConfig } from "../../config";
type AttachElementToHeadConfig = {
    elemType: any;
    rel?: string;
    link: string;
    innerText?: string;
    media?: string;
    property?: string;
    content?: string;
    name?: string;
};
export declare function attachElementToHead({ elemType, rel, link, innerText, media, property, content, name }: AttachElementToHeadConfig): void;
export declare function getBrandCode(AO: GlobalBaseConfig["AO"], DEFAULT_BRAND_CODE: BrandCode): BrandCode;
export {};
