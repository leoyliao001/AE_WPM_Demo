import { LitElement, TemplateResult, CSSResultArray } from 'lit';
import { Fit, Orientation, Target, CardVariant, ContentAlignment, ImageScaleStrength, IMcCard } from './types';
export type { IMcCard } from './types';
/**
 * A customizable card component that displays content in a structured format with optional images, headers, footers, and action buttons.
 * Category: Content Display
 * Purpose: Display content in a structured format.
 * When to use: Content grouping, Highlighting information, Call-to-action sections, User profiles, Product listings, News lists
 * When not to use: Decorative purposes, Full-page layouts, Large data set
 * Works with components: mc-drawer, mc-modal, mc-popover
 * Alternative component names: Content Card, Info Card, Action Card
 *
 * @element `mc-card`
 *
 * @slot - The default slot for the body content.
 * @slot `subheading` - For adding tags / links etc.
 * @slot `actions` -  For adding links / buttons etc.
 * @slot `image` - Image.
 * @slot `footer` - Footer.
 *
 * @csspart `container` - for changing visuals of container div
 * @csspart `image-container` - for changing visuals of image wrapper div
 * @csspart `image-inner` - for changing visuals of inner image div
 * @csspart `content-container` - for changing visuals of content wrapper div
 * @csspart `content-inner` - for changing visuals of inner content div
 * @csspart `header-container` - for changing visuals of header wrapper div
 * @csspart `body-container` - for changing visuals of body wrapper div
 * @csspart `footer-container` - for changing visuals of footer wrapper div
 * @csspart `actions-container` - for changing visuals of action buttons wrapper div
 *
 */
export declare class McCard extends LitElement implements IMcCard {
    hasbodyslot: boolean;
    private bodySlotNodes;
    hasactionsslot: boolean;
    private actionsSlotNodes;
    hasimageslot: boolean;
    private imageSlotNodes;
    hasfooterslot: boolean;
    private footerSlotNodes;
    hassubheadingslot: boolean;
    private subheadingSlotNodes;
    variant: CardVariant;
    orientation: Orientation;
    fit: Fit;
    heading?: string;
    subheading?: string;
    body?: string;
    footer?: string;
    contentalignment: ContentAlignment;
    padding?: string;
    image?: string;
    imagepercent?: number;
    imagescalestrength: ImageScaleStrength;
    imagebackgroundcolor?: string;
    href?: string;
    rel?: string;
    target?: Target;
    clickable?: boolean;
    static get styles(): CSSResultArray;
    /**
     * Renders the component
     * @returns {TemplateResult}
     */
    render(): TemplateResult;
    /**
     * Render the image
     * @returns {TemplateResult}
     */
    protected renderImage(): TemplateResult;
    private onImageSlotChange;
    private hasImage;
    protected renderImageAsSlot(): TemplateResult;
    /**
     * Render the image as a CSS background
     * @returns {TemplateResult}
     */
    protected renderImageAsBackground(): TemplateResult;
    /**
     * Render the content
     * @returns {TemplateResult}
     */
    protected renderContent(): TemplateResult;
    /**
     * Render the heading
     * @returns {TemplateResult}
     */
    protected renderHeading(): TemplateResult;
    /**
     * Render the body
     * @returns {TemplateResult}
     */
    protected renderBody(hasBody: boolean): TemplateResult;
    private onBodySlotChange;
    private hasBody;
    /**
     * Render the footer
     * @returns {TemplateResult}
     */
    protected renderFooter(hasFooter: boolean): TemplateResult;
    private onFooterSlotChange;
    private hasFooter;
    /**
     * Render the "actions" bar
     * @returns {TemplateResult}
     */
    protected renderActions(hasActions: boolean): TemplateResult;
    private onActionsSlotChange;
    private onSubheadingSlotChange;
}
//# sourceMappingURL=index.d.ts.map