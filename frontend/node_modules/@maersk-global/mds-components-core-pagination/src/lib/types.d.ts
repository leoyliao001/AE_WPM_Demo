import type { ICommonLabels } from '@maersk-global/mds-components-utils';
export interface EllipsisVisibility {
    left: boolean;
    right: boolean;
}
export type NavButtonType = 'first' | 'prev' | 'next' | 'last';
export type PaginationVariant = 'button' | 'select' | 'input';
import type { Fit } from '@maersk-global/mds-shared-types';
export type { Fit } from '@maersk-global/mds-shared-types';
/**
 * Labels configuration for mc-pagination component
 * Extends common labels with pagination specific labels
 */
export interface IMcPaginationLabels extends ICommonLabels {
    /** aria-label (or label when showlabel is on) for Previous page button */
    previous?: string;
    /** aria-label (or label when showlabel is on) for Next page button */
    next?: string;
    /** aria-label (or label when showlabel is on) for First page button */
    first?: string;
    /** aria-label (or label when showlabel is on) for Last page button */
    last?: string;
    /** aria-label for ellipsis */
    ariaEllipsis?: string;
    /** aria-label for current page number button */
    ariaCurrentPage?: string;
    /** aria-label for page number buttons */
    ariaPage?: string;
    /** hidden label for mc-select in select variant  */
    ariaSelectLabel?: string;
    /** hidden label for mc-input in input variant  */
    ariaInputLabel?: string;
}
export interface IMcPagination {
    /**
     * Labels object for customizing component text.
     * If not provided, labels are automatically loaded based on document's lang attribute.
     */
    labels?: IMcPaginationLabels;
    /**
     * Number of total pages.
     * @deprecated Use `totalrecords` instead, number of pages displayed will be calculated based on that.
     */
    totalpages: number;
    /**
     * Total number of rows across all pages.
     * Required when `showpagestatus` is enabled to display accurate row count (e.g., "1-10 of 75 rows").
     * If not provided when `showpagestatus` is true, the status will show a simplified format without the total (e.g., "1-10").
     */
    totalrecords: number;
    /**
     * Number of the selected page.
     */
    currentpage?: number;
    /**
     * If set to `true` or the attribute is just presented without any value,
     * then the `previous` and `next` label will not appear,
     * but will be used as `aria-label` only
     * @deprecated Use `showlabels` instead.
     */
    hiddenlabels?: boolean;
    /**
     * If set to `true` or the attribute is just presented without any value,
     * then the `previous`, `next`, `first`, and `last` labels will appear,
     * By default, labels for those buttons are hidden but are used as `aria-label`
     * @default false
     */
    showlabels?: boolean;
    /**
     * Maximum number of visible pages to be displayed.
     * If the `disabledtruncation` is set to `false` the dots used to truncate the pages will also be counted.
     * When set to `0` only navigation buttons will be displayed.
     */
    visiblepages?: number;
    /**
     * Label for the `previous` button.
     * The value will also be used as aria-label on the button.
     * @deprecated Use `labels.previous` instead
     */
    previouslabel?: string;
    /**
     * Label for the `next` button.
     * The value will also be used as aria-label on the button.
     * @deprecated Use `labels.next` instead
     */
    nextlabel?: string;
    /**
     * The `aria-label` for the pagination wrapper
     */
    arialabel?: string;
    /**
     * Fit of the pagination.
     */
    fit?: Fit;
    /**
     * If set to `true` or the attribute is just presented without any value,
     * then three dots will not be used to truncate the pages and first and last page navigation will not be visible.
     */
    disabledtruncation?: boolean;
    /**
     * Variant of the pagination component.
     * - 'button': Standard pagination with numbered page buttons (1, 2, 3...), recommended to use for content pagination (e.g., search results and articles)
     * - 'select': Advanced pagination with page selector dropdown instead of numbered buttons, recommended to use for tables with less than 300 rows
     * - 'input': Minimal pagination with only navigation arrows (first/prev/next/last), recommended to use for tables with more than 300 rows
     */
    variant?: PaginationVariant;
    /**
     * If set to `true`, displays first and last navigation buttons (double arrows) in addition to prev/next buttons.
     */
    showfirstandlastpage?: boolean;
    /**
     * If set to `true`, displays a rows per page selector dropdown in the start section.
     * Can be combined with any variant.
     */
    showpagesize?: boolean;
    /**
     * If set to `true`, displays a status text showing the current range of items in the start section.
     * When `totalrecords` is provided: shows "1-10 of 75 rows"
     * When `totalrecords` is not provided: shows simplified format "1-10"
     * Can be combined with any variant.
     */
    showpagestatus?: boolean;
    /**
     * Data for the rows per page selector dropdown options as an array of numbers.
     */
    pagesizeoptions?: number[];
    /**
     * Currently selected rows per page value.
     */
    pagesize?: number;
    /**
     * Page number changed
     */
    pagechange?: (page: number) => void;
    /**
     * Rows per page value changed
     */
    pagesizechange?: (rowsPerPage: number) => void;
}
//# sourceMappingURL=types.d.ts.map