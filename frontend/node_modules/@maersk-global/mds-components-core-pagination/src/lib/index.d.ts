import { LitElement, TemplateResult, PropertyValues, CSSResultArray } from 'lit';
import { IMcPagination, Fit, PaginationVariant, IMcPaginationLabels } from './types';
import '@maersk-global/mds-components-core-button';
import '@maersk-global/mds-components-core-select';
import '@maersk-global/mds-components-core-input';
export type { IMcPagination } from './types';
declare const McPagination_base: typeof LitElement;
/**
 * Purpose: To provide navigation for paginated content.
 * Category: Navigation, Data Display
 * When to use: When you need to navigate through a set of paginated content.
 * When not to use: When you have a single page of content or don't need pagination.
 * Works with components: mc-table, mc-list, mc-list-item, mc-card, mc-drawer, mc-modal
 * Alternative component names: Pagination Control, Page Navigator, Page Selector, Page Controller, Page Switcher
 *
 * @element `mc-pagination`
 *
 * @event {CustomEvent<number>} `pagechange` - Emitted when the page is changed.
 * @event {CustomEvent<number>} `pagesizechange` - Emitted when the rows per page value is changed.
 *
 * @slot - The default slot for the router links
 *
 * @csspart navigation - The navigation container
 * @csspart pagesize - The page size container
 * @csspart pagestatus - The page status container
 */
export declare class McPagination extends McPagination_base implements IMcPagination {
    private _currentPage;
    private _totalpages;
    private _totalrecords;
    private _linkElements;
    private currentFocusIndex?;
    private hasLinkSlot;
    private linkSlotElements;
    private inputElement;
    labels?: Partial<IMcPaginationLabels>;
    variant?: PaginationVariant;
    arialabel: string;
    visiblepages: number;
    fit: Fit;
    disabledtruncation: boolean;
    hiddenlabels: boolean;
    showlabels: boolean;
    showfirstandlastpage: boolean;
    nextlabel: string;
    previouslabel: string;
    pagesize: number;
    showpagesize: boolean;
    pagesizeoptions: number[];
    showpagestatus: boolean;
    set totalrecords(value: number);
    get totalrecords(): number;
    set totalpages(pagesAmount: number);
    get totalpages(): number;
    get currentpage(): number;
    set currentpage(value: number);
    protected get computedLabels(): Required<IMcPaginationLabels>;
    static get styles(): CSSResultArray;
    protected render(): TemplateResult;
    private renderNavButton;
    private renderNavList;
    private calculateElipsisVisibilty;
    private buildEllipsis;
    private buildPageButtons;
    private buildPageButton;
    private renderPageSelector;
    protected renderInput(): TemplateResult;
    private renderPageSelectorText;
    private renderShowPageSize;
    private renderPageStatus;
    firstUpdated(): void;
    disconnectedCallback(): void;
    updated(changedProperties: PropertyValues): void;
    private handleNavButtonClick;
    private isChangingPagePossible;
    private handlePageButtonClick;
    private handleSelectChange;
    private handleInputChange;
    private handlePageSizeChange;
    private handleKeyDown;
    private handleKeyUp;
    private range;
    private onLinkSlotChange;
    private selectedData;
    private get pagesData();
    private get rowsPerPageSelectData();
}
declare global {
    interface HTMLElementTagNameMap {
        'mc-pagination': McPagination;
    }
}
//# sourceMappingURL=index.d.ts.map