import {
  t as t2
} from "./chunk-7EDGZDXL.js";
import {
  styleMap
} from "./chunk-YT5IOTNB.js";
import {
  n,
  t,
  unsafeSVG
} from "./chunk-RUUVNDB7.js";
import {
  Directive,
  PartType,
  classMap,
  directive,
  i,
  property,
  state
} from "./chunk-MNWCOL3V.js";
import {
  LitElement,
  _$LH,
  css,
  html,
  isServer,
  noChange,
  unsafeCSS
} from "./chunk-APOV36NY.js";

// node_modules/lit-html/development/directive-helpers.js
var { _ChildPart: ChildPart } = _$LH;
var ENABLE_SHADYDOM_NOPATCH = true;
var _a, _b;
var wrap = ENABLE_SHADYDOM_NOPATCH && ((_a = window.ShadyDOM) == null ? void 0 : _a.inUse) && ((_b = window.ShadyDOM) == null ? void 0 : _b.noPatch) === true ? window.ShadyDOM.wrap : (node) => node;
var isPrimitive = (value) => value === null || typeof value != "object" && typeof value != "function";
var isSingleExpression = (part) => part.strings === void 0;
var createMarker = () => document.createComment("");
var insertPart = (containerPart, refPart, part) => {
  var _a2;
  const container = wrap(containerPart._$startNode).parentNode;
  const refNode = refPart === void 0 ? containerPart._$endNode : refPart._$startNode;
  if (part === void 0) {
    const startNode = wrap(container).insertBefore(createMarker(), refNode);
    const endNode = wrap(container).insertBefore(createMarker(), refNode);
    part = new ChildPart(startNode, endNode, containerPart, containerPart.options);
  } else {
    const endNode = wrap(part._$endNode).nextSibling;
    const oldParent = part._$parent;
    const parentChanged = oldParent !== containerPart;
    if (parentChanged) {
      (_a2 = part._$reparentDisconnectables) == null ? void 0 : _a2.call(part, containerPart);
      part._$parent = containerPart;
      let newConnectionState;
      if (part._$notifyConnectionChanged !== void 0 && (newConnectionState = containerPart._$isConnected) !== oldParent._$isConnected) {
        part._$notifyConnectionChanged(newConnectionState);
      }
    }
    if (endNode !== refNode || parentChanged) {
      let start = part._$startNode;
      while (start !== endNode) {
        const n2 = wrap(start).nextSibling;
        wrap(container).insertBefore(start, refNode);
        start = n2;
      }
    }
  }
  return part;
};
var setChildPartValue = (part, value, directiveParent = part) => {
  part._$setValue(value, directiveParent);
  return part;
};
var RESET_VALUE = {};
var setCommittedValue = (part, value = RESET_VALUE) => part._$committedValue = value;
var getCommittedValue = (part) => part._$committedValue;
var removePart = (part) => {
  part._$clear();
  part._$startNode.remove();
};

// node_modules/lit-html/development/async-directive.js
var DEV_MODE = true;
var notifyChildrenConnectedChanged = (parent, isConnected) => {
  var _a2;
  const children = parent._$disconnectableChildren;
  if (children === void 0) {
    return false;
  }
  for (const obj of children) {
    (_a2 = obj["_$notifyDirectiveConnectionChanged"]) == null ? void 0 : _a2.call(obj, isConnected, false);
    notifyChildrenConnectedChanged(obj, isConnected);
  }
  return true;
};
var removeDisconnectableFromParent = (obj) => {
  let parent, children;
  do {
    if ((parent = obj._$parent) === void 0) {
      break;
    }
    children = parent._$disconnectableChildren;
    children.delete(obj);
    obj = parent;
  } while ((children == null ? void 0 : children.size) === 0);
};
var addDisconnectableToParent = (obj) => {
  for (let parent; parent = obj._$parent; obj = parent) {
    let children = parent._$disconnectableChildren;
    if (children === void 0) {
      parent._$disconnectableChildren = children = /* @__PURE__ */ new Set();
    } else if (children.has(obj)) {
      break;
    }
    children.add(obj);
    installDisconnectAPI(parent);
  }
};
function reparentDisconnectables(newParent) {
  if (this._$disconnectableChildren !== void 0) {
    removeDisconnectableFromParent(this);
    this._$parent = newParent;
    addDisconnectableToParent(this);
  } else {
    this._$parent = newParent;
  }
}
function notifyChildPartConnectedChanged(isConnected, isClearingValue = false, fromPartIndex = 0) {
  const value = this._$committedValue;
  const children = this._$disconnectableChildren;
  if (children === void 0 || children.size === 0) {
    return;
  }
  if (isClearingValue) {
    if (Array.isArray(value)) {
      for (let i4 = fromPartIndex; i4 < value.length; i4++) {
        notifyChildrenConnectedChanged(value[i4], false);
        removeDisconnectableFromParent(value[i4]);
      }
    } else if (value != null) {
      notifyChildrenConnectedChanged(value, false);
      removeDisconnectableFromParent(value);
    }
  } else {
    notifyChildrenConnectedChanged(this, isConnected);
  }
}
var installDisconnectAPI = (obj) => {
  if (obj.type == PartType.CHILD) {
    obj._$notifyConnectionChanged ?? (obj._$notifyConnectionChanged = notifyChildPartConnectedChanged);
    obj._$reparentDisconnectables ?? (obj._$reparentDisconnectables = reparentDisconnectables);
  }
};
var AsyncDirective = class extends Directive {
  constructor() {
    super(...arguments);
    this._$disconnectableChildren = void 0;
  }
  /**
   * Initialize the part with internal fields
   * @param part
   * @param parent
   * @param attributeIndex
   */
  _$initialize(part, parent, attributeIndex) {
    super._$initialize(part, parent, attributeIndex);
    addDisconnectableToParent(this);
    this.isConnected = part._$isConnected;
  }
  // This property needs to remain unminified.
  /**
   * Called from the core code when a directive is going away from a part (in
   * which case `shouldRemoveFromParent` should be true), and from the
   * `setChildrenConnected` helper function when recursively changing the
   * connection state of a tree (in which case `shouldRemoveFromParent` should
   * be false).
   *
   * @param isConnected
   * @param isClearingDirective - True when the directive itself is being
   *     removed; false when the tree is being disconnected
   * @internal
   */
  ["_$notifyDirectiveConnectionChanged"](isConnected, isClearingDirective = true) {
    var _a2, _b2;
    if (isConnected !== this.isConnected) {
      this.isConnected = isConnected;
      if (isConnected) {
        (_a2 = this.reconnected) == null ? void 0 : _a2.call(this);
      } else {
        (_b2 = this.disconnected) == null ? void 0 : _b2.call(this);
      }
    }
    if (isClearingDirective) {
      notifyChildrenConnectedChanged(this, isConnected);
      removeDisconnectableFromParent(this);
    }
  }
  /**
   * Sets the value of the directive's Part outside the normal `update`/`render`
   * lifecycle of a directive.
   *
   * This method should not be called synchronously from a directive's `update`
   * or `render`.
   *
   * @param directive The directive to update
   * @param value The value to set
   */
  setValue(value) {
    if (isSingleExpression(this.__part)) {
      this.__part._$setValue(value, this);
    } else {
      if (DEV_MODE && this.__attributeIndex === void 0) {
        throw new Error(`Expected this.__attributeIndex to be a number`);
      }
      const newValues = [...this.__part._$committedValue];
      newValues[this.__attributeIndex] = value;
      this.__part._$setValue(newValues, this, 0);
    }
  }
  /**
   * User callbacks for implementing logic to release any resources/subscriptions
   * that may have been retained by this directive. Since directives may also be
   * re-connected, `reconnected` should also be implemented to restore the
   * working state of the directive prior to the next render.
   */
  disconnected() {
  }
  reconnected() {
  }
};

// node_modules/lit-html/development/directives/private-async-helpers.js
var PseudoWeakRef = class {
  constructor(ref) {
    this._ref = ref;
  }
  /**
   * Disassociates the ref with the backing instance.
   */
  disconnect() {
    this._ref = void 0;
  }
  /**
   * Reassociates the ref with the backing instance.
   */
  reconnect(ref) {
    this._ref = ref;
  }
  /**
   * Retrieves the backing instance (will be undefined when disconnected)
   */
  deref() {
    return this._ref;
  }
};
var Pauser = class {
  constructor() {
    this._promise = void 0;
    this._resolve = void 0;
  }
  /**
   * When paused, returns a promise to be awaited; when unpaused, returns
   * undefined. Note that in the microtask between the pauser being resumed
   * an await of this promise resolving, the pauser could be paused again,
   * hence callers should check the promise in a loop when awaiting.
   * @returns A promise to be awaited when paused or undefined
   */
  get() {
    return this._promise;
  }
  /**
   * Creates a promise to be awaited
   */
  pause() {
    this._promise ?? (this._promise = new Promise((resolve) => this._resolve = resolve));
  }
  /**
   * Resolves the promise which may be awaited
   */
  resume() {
    var _a2;
    (_a2 = this._resolve) == null ? void 0 : _a2.call(this);
    this._promise = this._resolve = void 0;
  }
};

// node_modules/lit-html/development/directives/until.js
var isPromise = (x) => {
  return !isPrimitive(x) && typeof x.then === "function";
};
var _infinity = 1073741823;
var UntilDirective = class extends AsyncDirective {
  constructor() {
    super(...arguments);
    this.__lastRenderedIndex = _infinity;
    this.__values = [];
    this.__weakThis = new PseudoWeakRef(this);
    this.__pauser = new Pauser();
  }
  render(...args) {
    return args.find((x) => !isPromise(x)) ?? noChange;
  }
  update(_part, args) {
    const previousValues = this.__values;
    let previousLength = previousValues.length;
    this.__values = args;
    const weakThis = this.__weakThis;
    const pauser = this.__pauser;
    if (!this.isConnected) {
      this.disconnected();
    }
    for (let i4 = 0; i4 < args.length; i4++) {
      if (i4 > this.__lastRenderedIndex) {
        break;
      }
      const value = args[i4];
      if (!isPromise(value)) {
        this.__lastRenderedIndex = i4;
        return value;
      }
      if (i4 < previousLength && value === previousValues[i4]) {
        continue;
      }
      this.__lastRenderedIndex = _infinity;
      previousLength = 0;
      Promise.resolve(value).then(async (result) => {
        while (pauser.get()) {
          await pauser.get();
        }
        const _this = weakThis.deref();
        if (_this !== void 0) {
          const index = _this.__values.indexOf(value);
          if (index > -1 && index < _this.__lastRenderedIndex) {
            _this.__lastRenderedIndex = index;
            _this.setValue(result);
          }
        }
      });
    }
    return noChange;
  }
  disconnected() {
    this.__weakThis.disconnect();
    this.__pauser.pause();
  }
  reconnected() {
    this.__weakThis.reconnect(this);
    this.__pauser.resume();
  }
};
var until = directive(UntilDirective);

// node_modules/@maersk-global/mds-components-core-icon/index.js
var g = Object.defineProperty;
var y = Object.getOwnPropertyDescriptor;
var o = (a, n2, t3, e) => {
  for (var s = e > 1 ? void 0 : e ? y(n2, t3) : n2, r = a.length - 1, p; r >= 0; r--) (p = a[r]) && (s = (e ? p(n2, t3, s) : p(s)) || s);
  return e && s && g(n2, t3, s), s;
};
var l = css`.mc-icon{display:flex;fill:inherit}.mc-icon.size-16,.mc-icon.size-16 svg{height:16px;width:16px}.mc-icon.size-20{height:20px;width:20px}.mc-icon.size-24{height:24px;width:24px}`;
var u = [i, unsafeCSS(l)];
var z = "https://assets.maerskline.com/mds/icons/latest/svg";
var i2 = class i3 extends LitElement {
  constructor() {
    super(...arguments);
    this.convertedSize = "20";
    this.icon = "empty";
    this.size = "20";
  }
  static get styles() {
    return u;
  }
  get iconTitle() {
    return this.title || (this.icon !== "empty" ? this.icon : void 0);
  }
  willUpdate(t3) {
    super.willUpdate(t3), (t3.has("size") || t3.has("icon") || t3.has("title")) && (this.convertedSize = this.size === "16" ? "20" : this.size, this.icon && this.icon !== "empty" && (this.svg = this.renderIcon(this.convertedSize)));
  }
  render() {
    let t3 = { [`size-${this.size}`]: true }, e = { fill: this.color ? this.color : "var(--mds_brand_appearance_neutral_default_text-color)" };
    return html`<span part="icon-wrapper" class="mc-icon ${classMap(t3)}" style="${styleMap(e)}">${this.icon && this.icon !== "empty" && this.svg ? until(this.svg) : this.getEmptyIcon(this.convertedSize)}</span>`;
  }
  async renderIcon(t3) {
    var _a2, _b2, _c;
    try {
      i3.supportsDynamicImport === void 0 && (i3.supportsDynamicImport = n());
      let e, s = ((_a2 = t2) == null ? void 0 : _a2.iconsDynamicImportPath) || isServer ? (_b2 = t2) == null ? void 0 : _b2.iconsDynamicImportPath : (_c = window.MdsConfig) == null ? void 0 : _c._iconsDynamicImportPath;
      if (!i3.supportsDynamicImport || t()) e = await fetch(`${z}/${t3}px/mi-${this.icon}.svg`).then((r) => {
        if (r.ok) return r.text();
      });
      else {
        let r = null;
        s ? r = await import(
          /* @vite-ignore */
          `${s}@maersk-global/icons/js/${t3}px/mi-${this.icon}.js`
        ) : r = await import(
          /* @vite-ignore */
          `@maersk-global/icons/js/${t3}px/mi-${this.icon}.js`
        ), e = r.default;
      }
      return unsafeSVG(this.addTitleToSvg(e));
    } catch (e) {
      return console.warn(`The icons path not configured. 
        Please set MdsConfig.iconsDynamicImportPath before using mc-icon component.
        Example: MdsConfig.iconsDynamicImportPath = "/node_modules/"
        ${e}`), this.getEmptyIcon(t3);
    }
  }
  addTitleToSvg(t3) {
    return this.iconTitle ? t3.replace(/<svg/, `<svg aria-label="${this.iconTitle}"`) : t3;
  }
  getEmptyIcon(t3) {
    return unsafeSVG(`<svg
    width="${t3}"
    height="${t3}"
    viewBox="0 0 ${t3} ${t3}"
    xmlns="http://www.w3.org/2000/svg"
  ></svg>`);
  }
};
i2.supportsDynamicImport = void 0, o([state()], i2.prototype, "convertedSize", 2), o([state()], i2.prototype, "svg", 2), o([property({ type: String })], i2.prototype, "color", 2), o([property({ type: String, reflect: true })], i2.prototype, "icon", 2), o([property({ type: String })], i2.prototype, "size", 2), o([property({ type: String, reflect: true })], i2.prototype, "title", 2);
var m = i2;
customElements.get("mc-icon") || customElements.define("mc-icon", m);

export {
  isSingleExpression,
  insertPart,
  setChildPartValue,
  setCommittedValue,
  getCommittedValue,
  removePart,
  m
};
/*! Bundled license information:

lit-html/development/directive-helpers.js:
  (**
   * @license
   * Copyright 2020 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/development/async-directive.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/development/directives/private-async-helpers.js:
  (**
   * @license
   * Copyright 2021 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/development/directives/until.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
*/
//# sourceMappingURL=chunk-64OPIA7H.js.map
