import {
  i as i2,
  t
} from "./chunk-ZU2OBHAA.js";
import {
  styleMap
} from "./chunk-YT5IOTNB.js";
import {
  T,
  a3 as a,
  e,
  e3 as e2,
  m,
  p2 as p
} from "./chunk-RUUVNDB7.js";
import {
  ifDefined
} from "./chunk-OWANMJLW.js";
import {
  classMap,
  i,
  property,
  query,
  queryAssignedElements,
  state
} from "./chunk-MNWCOL3V.js";
import {
  LitElement,
  css,
  html,
  unsafeCSS
} from "./chunk-APOV36NY.js";

// node_modules/@maersk-global/mds-components-core-list/index.js
var T2 = Object.defineProperty;
var w = Object.getOwnPropertyDescriptor;
var o = (I, p2, t2, e3) => {
  for (var s = e3 > 1 ? void 0 : e3 ? w(p2, t2) : p2, i4 = I.length - 1, l; i4 >= 0; i4--) (l = I[i4]) && (s = (e3 ? l(p2, t2, s) : l(s)) || s);
  return e3 && s && T2(p2, t2, s), s;
};
var v = css`:host([orientation=horizontal]) .mc-list{display:inline-flex;flex-direction:row}:host([orientation=horizontal]) .mc-list.large,:host([orientation=horizontal]) .mc-list.medium,:host([orientation=horizontal]) .mc-list.small{padding:0}:host([width=auto]) .mc-list{min-width:unset;width:fit-content}:host([width=full-width]) .mc-list{width:100%}.mc-list{background-color:var(--mds_brand_appearance_neutral_default_background-color);border-color:var(--mds_brand_appearance_neutral_default_border-color);border-radius:var(--mds_brand_border_medium_radius);border-style:var(--mds_global_border_style);border-width:var(--mds_global_border_width);display:flex;flex-direction:column;min-width:64px;margin:0;padding:0}.mc-list ::slotted(small){color:var(--mds_brand_appearance_neutral_weak_text-color)}.mc-list.large,.mc-list.medium,.mc-list.small{padding:4px 0}.mc-list.small ::slotted(hr){margin:4px 0!important}.mc-list.small ::slotted(small){font-size:var(--mds_brand_typography_text_small_mobile_font-size)!important;margin:6px 8px 2px;font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);line-height:var(--mds_brand_typography_text_small_mobile_line-height)}@media screen and (min-width:1025px){.mc-list.small ::slotted(small){font-size:var(--mds_brand_typography_text_small_desktop_font-size);line-height:var(--mds_brand_typography_text_small_desktop_line-height)}}.mc-list.small ::slotted(small){font-style:var(--mds_brand_typography_text_small_normal_font-style);font-weight:var(--mds_brand_typography_text_small_normal_font-weight)}@media screen and (min-width:1000px){.mc-list.small ::slotted(small){font-size:var(--mds_brand_typography_text_small_desktop_font-size)!important}}.mc-list.small .list-search{padding:4px 8px 8px}.mc-list.medium ::slotted(hr){margin:4px 0!important}.mc-list.medium ::slotted(small){font-size:var(--mds_brand_typography_text_small_mobile_font-size)!important;margin:8px 12px 2px;font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);line-height:var(--mds_brand_typography_text_small_mobile_line-height)}@media screen and (min-width:1025px){.mc-list.medium ::slotted(small){font-size:var(--mds_brand_typography_text_small_desktop_font-size);line-height:var(--mds_brand_typography_text_small_desktop_line-height)}}.mc-list.medium ::slotted(small){font-style:var(--mds_brand_typography_text_small_normal_font-style);font-weight:var(--mds_brand_typography_text_small_normal_font-weight)}@media screen and (min-width:1000px){.mc-list.medium ::slotted(small){font-size:var(--mds_brand_typography_text_small_desktop_font-size)!important}}.mc-list.medium .list-search{padding:8px 12px 12px}.mc-list.large ::slotted(hr){margin:4px 0!important}.mc-list.large ::slotted(small){font-size:var(--mds_brand_typography_text_small_mobile_font-size)!important;margin:8px 16px 4px;font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);line-height:var(--mds_brand_typography_text_small_mobile_line-height)}@media screen and (min-width:1025px){.mc-list.large ::slotted(small){font-size:var(--mds_brand_typography_text_small_desktop_font-size);line-height:var(--mds_brand_typography_text_small_desktop_line-height)}}.mc-list.large ::slotted(small){font-style:var(--mds_brand_typography_text_small_normal_font-style);font-weight:var(--mds_brand_typography_text_small_normal_font-weight)}@media screen and (min-width:1000px){.mc-list.large ::slotted(small){font-size:var(--mds_brand_typography_text_small_desktop_font-size)!important}}.mc-list.large .list-search{padding:12px 16px 16px}.mc-list.no-border{border:0;border-radius:0;background-color:transparent}.mc-list .list-search::part(field){border-radius:4px}`;
var _ = [i, unsafeCSS(v)];
var g = ["mc-picker-item", "mc-option", "mc-input", "mc-list-item", "mc-button-group-item", "mc-segmented-control-item"];
var S = "mc-button";
var a2 = class extends LitElement {
  constructor() {
    super(...arguments);
    this.scrollToSelectedValue = false;
    this.scrollSelectedIntoView = false;
    this.labelSelector = ".labels";
    this.smoothScroll = false;
    this.listSearchValue = "";
    this.items = [];
    this.headingGroups = /* @__PURE__ */ new Map();
    this.keyboardNavigationActive = false;
    this.mousemovevisiblearea = null;
    this.data = null;
    this.listsearch = false;
    this.listsearchplaceholder = "Search in the list";
    this.filtertype = "contains";
    this.matchlabelonly = false;
    this.disablehandlemousemove = false;
    this.disablecircularkeynav = false;
    this.disablesetfocus = false;
    this.scrolltofocused = false;
    this.hasactionslots = false;
    this.fit = "medium";
    this.keynavigationtype = "arrow";
    this.noborder = false;
    this.orientation = "vertical";
    this.role = "list";
    this.type = "none";
    this.width = "full-width";
  }
  static get styles() {
    return _;
  }
  render() {
    return html`${this.renderList()}`;
  }
  renderList() {
    let t2 = { [`${this.fit}`]: true, "no-border": this.noborder, "smooth-scroll": this.smoothScroll };
    return html`<div part="list" class="mc-list ${classMap(t2)}" data-cy="mc-list" @click="${this.handleClick}" @keydown="${this.handleKeyDown}" @focusin="${this.handleFocusIn}" @focusout="${this.handleFocusOut}" @mouseout="${this.handleMouseOut}" @mousemove="${this.handleMouseMove}" @slotchange="${this.handleSlotChange}" @scroll="${() => {
      this.dispatchEvent(new Event("scroll"));
    }}">${this.renderOptionSearch()} ${this.renderItemsFromData()} ${!this.data || this.data.length === 0 ? html`<slot></slot>` : null}</div>`;
  }
  renderItemsFromData() {
    if (this.data && this.data.length > 0) return this.data.map((t2) => html`<mc-list-item ariarole="${ifDefined(this.role === "listbox" ? "option" : "listitem")}" .value="${t2.value}" .label="${t2.label}" ?disabled="${t2.disabled}"></mc-list-item>`);
  }
  renderOptionSearch() {
    return this.listsearch ? html`<mc-input placeholder="${this.listsearchplaceholder}" class="list-search" label="Search" part="list-search" .fit="${this.fit}" hiddenlabel .visible="${true}" .value="${this.listSearchValue}" @input="${(t2) => this.handleListSearch(t2)}"></mc-input>` : null;
  }
  firstUpdated(t2) {
    super.firstUpdated(t2), this.setItems();
  }
  willUpdate(t2) {
    super.willUpdate(t2), this.setAriaAttributes(), (t2.has("role") || t2.has("type") || t2.has("fit") || t2.has("orientation")) && this.passOnListCommonPropsToListItems(), t2.has("type") && (this.focusedItem = null, this.setAriaAttributes());
  }
  updated(t2) {
    if (super.updated(t2), t2.has("value") && t2.get("value") !== this.value) {
      let e3 = this.getItemsOfSuppliedValue();
      this.type !== "multiple" && this.type !== "typeahead" && this.items && this.value && requestAnimationFrame(() => {
        this.setFocusedItem(e3, this.scrollToSelectedValue);
      });
    }
    t2.has("listsearch") && requestAnimationFrame(() => {
      this.setItems();
    }), t2.has("data") && this.setItems();
  }
  shouldKeyCauseKeyboardNav(t2) {
    return this.keynavigationtype === "tab" ? t2 === "Tab" : this.orientation === "vertical" ? ["ArrowDown", "ArrowUp", "Home", "End"].includes(t2) : ["ArrowRight", "ArrowLeft", "Home", "End"].includes(t2);
  }
  handleListChange(t2) {
    !t2 || t2.disabled || (this.currentItem = t2, this.setFocusedItem(t2, this.scrollSelectedIntoView), this.type === "multiple" && (t2.selected = !t2.selected), this.type === "single" && (this.role === "listbox" || this.role === "list") && this.markAsSelected(t2), this.value = this.getSelectedValues(), this.dispatchEvent(new CustomEvent("listchange", { detail: { item: t2 } })));
  }
  getItemFromEventTarget(t2) {
    let e3 = t2.target;
    return e3.closest("mc-button-group-item") || e3.closest("mc-segmented-control-item") || e3.closest("mc-list-item") || e3.closest("mc-option") || e3.closest("mc-picker-item") || this.focusedItem;
  }
  handleClick(t2) {
    let e3 = t2.target;
    if (e3.closest(".list-search") || e3.classList.contains("list-search")) return;
    let i4 = this.getItemFromEventTarget(t2);
    (i4 == null ? void 0 : i4.querySelector("a")) || (i4 && !i4.href && t2.preventDefault(), this.handleListChange(i4));
  }
  handleFocusIn(t2) {
    if (!this.hasactionslots) return;
    let e3 = t2.target;
    if (this.focusedItem) {
      let s = this.focusedItem.querySelector('[slot="actions"]');
      if (s && s.contains(e3)) return;
    }
    this.lastFocusedItem ? this.setFocusedItem(this.lastFocusedItem, false) : this.items && this.items[0] && this.setFocusedItem(this.items[0]);
  }
  handleFocusOut(t2) {
    if (!this.hasactionslots) return;
    let e3 = t2.relatedTarget;
    if (e3 && this.focusedItem) {
      let s = this.focusedItem.querySelector('[slot="actions"]');
      s && s.contains(e3) && (this.focusedItem.focused = false);
    }
    this.lastFocusedItem = this.focusedItem || null;
  }
  handleKeyDown(t2) {
    var _a, _b, _c, _d;
    let e3 = t2.target, s = e3.closest("mc-list");
    if (!(s && s !== this || e3.tagName.toLowerCase() === "mc-button" && e3.closest('[slot="actions"]') && t2.key === "Tab")) {
      if (t2.key === "Escape" && this.role === "menu") {
        t2.stopPropagation();
        let l = this.closest("mc-menu"), n = (_a = l == null ? void 0 : l.shadowRoot) == null ? void 0 : _a.querySelector("mc-popover");
        if (n && n.hide) {
          n.hide();
          let h = ((_c = (_b = l == null ? void 0 : l.shadowRoot) == null ? void 0 : _b.querySelector('slot[name="trigger"]')) == null ? void 0 : _c.assignedElements()) || [];
          if (h.length > 0) {
            let d = h[0];
            (((_d = d.shadowRoot) == null ? void 0 : _d.querySelector("button")) || d).focus();
          }
        }
        return;
      }
      if (t2.key === "Enter" && e3.nodeName.toLowerCase() !== "a") {
        if (e3.closest(".list-search") || e3.classList.contains("list-search")) return;
        t2.preventDefault(), this.handleListChange(this.getItemFromEventTarget(t2));
      }
      if (t2.key === "Space") {
        let l = t2.target;
        l.closest(".list-search") || l.classList.contains("list-search") || t2.preventDefault();
      }
      if (t2.key === "Tab" && (this.type === "none" || !this.value) && this.items && !this.focusedItem && !this.hasactionslots && this.setFocusedItem(this.items[0]), this.items && this.items.length > 0 && this.shouldKeyCauseKeyboardNav(t2.key)) {
        this.keynavigationtype === "arrow" && t2.preventDefault(), this.keyboardNavigationActive = true, this.passOnListCommonPropsToListItems();
        let l = this.items.filter((h) => h.visible);
        this.hoveredItem && (this.focusedItem = this.hoveredItem, this.hoveredItem = void 0);
        let n = this.focusedItem ? l.indexOf(this.focusedItem) : 0;
        t2.key === "ArrowDown" || t2.key === "ArrowRight" || this.keynavigationtype === "tab" && t2.key === "Tab" && !t2.shiftKey ? n++ : t2.key === "ArrowUp" || t2.key === "ArrowLeft" || this.keynavigationtype === "tab" && t2.key === "Tab" && t2.shiftKey ? n-- : t2.key === "Home" ? n = 0 : t2.key === "End" && (n = l.length - 1), this.keynavigationtype === "arrow" && (n < 0 && (this.disablecircularkeynav ? n = 0 : n = l.length - 1), n > l.length - 1 && (this.disablecircularkeynav ? n = l.length - 1 : n = 0));
        let r2 = this.keynavigationtype === "tab" && n < 0 && n > l.length - 1 ? void 0 : l[n];
        this.disablesetfocus && r2 && this.list && this.scrollListIntoView(r2, n), this.setFocusedItem(r2, true);
      }
    }
  }
  handleMouseOut(t2) {
    if (!this.disablehandlemousemove) {
      let e3 = t2.relatedTarget;
      !g.includes(e3 == null ? void 0 : e3.nodeName.toLocaleLowerCase()) && this.items && (this.hoveredItem = void 0, this.focusedItem && this.setFocusedItem(void 0));
    }
  }
  handleMouseMove(t2) {
    if (!this.disablehandlemousemove) {
      let e3 = t2.target, s = this.closestMcListItem(e3);
      if (s !== this.hoveredItem && this.isListItem(s)) if (this.keyboardNavigationActive = false, this.passOnListCommonPropsToListItems(), this.focusedItem && this.focusedItem !== s && this.setFocusedItem(void 0), this.mousemovevisiblearea) {
        let { y: i4, height: l } = s.getBoundingClientRect();
        i4 > this.mousemovevisiblearea.min - 8 && i4 < this.mousemovevisiblearea.max - l / 2 && (this.hoveredItem = s);
      } else this.hoveredItem = s;
    }
  }
  handleSlotChange() {
    this.slottedListItems && (this.setItems(), this.items && this.items.length > 0 && (this.getItemsOfSuppliedValue(), this.passOnListCommonPropsToListItems(), this.buildListGroupMap()));
  }
  setItems() {
    this.allItems = this.getAllItems(), this.items = this.allItems.filter((t2) => !t2.disabled), this.listSearchInput && (this.items = [this.listSearchInput, ...this.items]), this.dispatchEvent(new CustomEvent("listitemsloaded", { detail: { items: this.items } }));
  }
  getSelectedValues() {
    let t2 = this.items.reduce((e3, s) => [...s.selected ? [s.value] : [], ...e3], []);
    if (this.type === "single" && t2 && t2.length > 0) return t2[0];
    if (this.type === "multiple") return t2;
  }
  markAsSelected(t2) {
    this.items.forEach((e3) => {
      this.role === "listbox" ? e3 === t2 && e3.value !== void 0 && e3.value !== "null" ? e3.selected = true : e3.selected = false : e3 === t2 ? e3.selected = true : e3.selected = false;
    });
  }
  passOnListCommonPropsToListItems() {
    this.allItems && this.allItems.forEach((t2) => {
      var _a;
      t2.fit = this.fit, t2.orientation = this.orientation, this.keyboardNavigationActive ? t2.setAttribute("data-keyboard-nav", "true") : t2.removeAttribute("data-keyboard-nav"), !this.role || this.role === "list" ? (this.role = "list", t2.ariarole = "listitem") : this.role === "menu" ? t2.ariarole = "menuitem" : this.role === "listbox" && (t2.ariarole = "option", this.type === "multiple" && t2.type !== "select-clear" && ((_a = t2.tagName) == null ? void 0 : _a.toLowerCase()) !== "mc-input" && (t2.type = "checkbox"));
    });
  }
  setAriaAttributes() {
    !this.type || this.type === "none" || this.type === "typeahead" ? this.removeAttribute("aria-multiselectable") : this.type && this.type === "single" ? (this.role = "listbox", this.removeAttribute("aria-multiselectable")) : (this.role = "listbox", this.setAttribute("aria-multiselectable", "true"));
  }
  getAllItems() {
    var _a, _b, _c;
    let t2 = [];
    return this.data && this.data.length > 0 && ((_b = (_a = this.shadowRoot) == null ? void 0 : _a.querySelectorAll("mc-list-item, mc-option, mc-picker-item, mc-button-group-item, mc-segmented-control-item")) == null ? void 0 : _b.forEach((s, i4) => {
      var _a2, _b2;
      this.type !== "none" && s.value === void 0 && (s.value = ((_b2 = (_a2 = this.data) == null ? void 0 : _a2[i4]) == null ? void 0 : _b2.value) ?? i4), t2.push(s);
    })), (_c = this.slottedListItems) == null ? void 0 : _c.forEach((e3, s) => {
      var _a2;
      if (this.isListItem(e3)) this.type !== "none" && e3.value === void 0 && (e3.value = s), t2.push(e3);
      else if (((_a2 = e3.tagName) == null ? void 0 : _a2.toLowerCase()) === "mc-tooltip") {
        let i4 = e3.querySelector("mc-button-group-item, mc-segmented-control-item, mc-list-item, mc-option, mc-picker-item");
        i4 && (this.type !== "none" && i4.value === void 0 && (i4.value = s), t2.push(i4));
      }
    }), t2;
  }
  isListItem(t2) {
    var _a;
    return g.includes((_a = t2 == null ? void 0 : t2.tagName) == null ? void 0 : _a.toLowerCase()) || ["listitem", "menuitem", "menuitemcheckbox", "menuitemradio"].includes((t2 == null ? void 0 : t2.getAttribute("role")) ?? "");
  }
  closestMcListItem(t2) {
    let e3 = g.find((s) => t2.closest(s));
    return t2.closest(e3);
  }
  buildListGroupMap() {
    this.headingGroups.clear();
    let t2 = Array.from(this.slottedListItems), e3 = null;
    t2.forEach((s) => {
      let i4 = s.tagName.toLocaleLowerCase();
      if (i4 === "small") {
        e3 = s, this.headingGroups.has(e3) || this.headingGroups.set(e3, []);
        return;
      }
      if ((i4 === "mc-option" || i4 === "mc-list-item") && e3) {
        let l = this.headingGroups.get(e3);
        l && l.push(s);
      }
    });
  }
  setFocusedItem(t2, e3) {
    var _a, _b;
    if ((_a = this.items) == null ? void 0 : _a.forEach((s, i4) => {
      t2 && s === t2 ? (this.focusedItem = t2, s.focused = true, this.hasactionslots && this.updateActionsSlotTabIndex(), this.keynavigationtype === "arrow" && (s.tabindex = 0, e3 && !this.disablesetfocus && t2.focus({ preventScroll: !this.scrolltofocused }))) : (s.focused = false, this.keynavigationtype === "arrow" && (s.tabindex = i4 === 0 && !t2 ? 0 : -1));
    }), e3 && t2 && this.scrollSelectedIntoView && t2.scrollIntoView && t2.scrollIntoView({ behavior: "smooth", block: "nearest", inline: "center" }), e3) {
      let s = (_b = this.items) == null ? void 0 : _b.indexOf(t2);
      this.dispatchEvent(new CustomEvent("focuschange", { detail: { item: t2, index: s } }));
    }
  }
  updateActionsSlotTabIndex() {
    var _a;
    if ((_a = this.items) == null ? void 0 : _a.forEach((t2) => {
      let e3 = t2.querySelector('[slot="actions"]');
      e3 && e3.querySelectorAll(S).forEach((i4) => {
        i4.tabIndex = -1;
      });
    }), this.focusedItem) {
      let t2 = this.focusedItem.querySelector('[slot="actions"]');
      t2 && t2.querySelectorAll(S).forEach((s) => {
        s.tabIndex = 0;
      });
    }
  }
  getItemsOfSuppliedValue() {
    var _a, _b;
    if (this.allItems) {
      if (!this.value && this.value !== "") return this.allItems[0];
      if (this.type === "single") {
        let t2 = JSON.stringify(this.value), e3 = (_a = this.allItems) == null ? void 0 : _a.filter((s) => s.value !== "null" && t2 === JSON.stringify(s.value) ? (s.selected = true, true) : (s.selected = false, false));
        return e3.length !== 0 ? e3[0] : this.allItems[0];
      }
      if (this.type === "multiple") {
        let t2 = this.normalizeMultipleValue(), e3 = (_b = this.allItems) == null ? void 0 : _b.filter((i4) => t2.find((l) => JSON.stringify(l) === JSON.stringify(i4.value)) !== void 0 ? (i4.selected = true, true) : (i4.selected = false, false)), s = this.currentItem ? e3 == null ? void 0 : e3.indexOf(this.currentItem) : 0;
        return e3 && s ? e3[s] : this.allItems[0];
      }
      return this.allItems[0];
    }
  }
  normalizeMultipleValue() {
    if (Array.isArray(this.value)) return this.value;
    if (typeof this.value == "string") return this.value.split(",");
    if (typeof this.value == "number") return [`${this.value}`];
  }
  scrollListIntoView(t2, e3) {
    var _a;
    let s = this.list.getBoundingClientRect(), i4 = t2.getBoundingClientRect(), l = this.scrollablecontainer;
    if (!l) return;
    let n = l.scrollTop, r2 = l.clientHeight || 360, h = i4.height, d = i4.top - s.top, m2 = i4.bottom - s.top, b2 = n, M2 = n + r2, x = d < b2 || m2 > M2 || m2 - d > r2;
    if (e3 === 0 || e3 === ((_a = this.items) == null ? void 0 : _a.length) - 1 && !this.disablecircularkeynav) l.scrollTo({ top: e3 === 0 ? 0 : l.scrollHeight - r2, behavior: "smooth" });
    else if (x) {
      let y3;
      d < b2 ? y3 = Math.max(0, d - h / 2) : y3 = Math.max(0, d - h);
      let E = l.scrollHeight - r2;
      l.scrollTo({ top: Math.min(y3, E), behavior: "smooth" });
    }
  }
  clearListSearch() {
    var _a;
    this.listSearchInput && (this.listSearchInput.value = "", this.listSearchValue = "", (_a = this.allItems) == null ? void 0 : _a.forEach((t2) => {
      t2.visible = true;
    }), this.updateGroupHeadings());
  }
  clearFocusedItem() {
    this.focusedItem = null, this.setFocusedItem(void 0);
  }
  handleListSearch(t2) {
    var _a, _b;
    let e3 = t2.target;
    this.listSearchValue = e3.value;
    let s = e3.value.toLowerCase().trim(), i4 = (_a = this.allItems) == null ? void 0 : _a.filter((r2) => r2.tagName.toLowerCase() !== "mc-input");
    if (s === "") {
      i4 == null ? void 0 : i4.forEach((r2) => {
        r2.visible = true;
      }), this.updateGroupHeadings();
      return;
    }
    let l = i4 == null ? void 0 : i4.filter((r2) => !r2.disabled);
    (_b = i4 == null ? void 0 : i4.filter((r2) => r2.disabled)) == null ? void 0 : _b.forEach((r2) => {
      r2.visible = false;
    }), l == null ? void 0 : l.forEach((r2) => {
      var _a2, _b2;
      let h;
      if (this.matchlabelonly) h = r2.getAttribute("label") || "";
      else {
        let m2;
        m2 = r2.innerText, (!m2 || m2.trim() === "") && (m2 = (_b2 = (_a2 = r2.shadowRoot) == null ? void 0 : _a2.querySelector(this.labelSelector)) == null ? void 0 : _b2.innerText), (!m2 || m2.trim() === "") && (m2 = r2.getAttribute("label") || ""), m2 && m2.trim() !== "" && (r2._cachedLabel = m2), h = r2._cachedLabel || m2 || "";
      }
      let d = a(h, s, this.filtertype, this.customfilter);
      r2.visible = d.length > 0;
    }), this.updateGroupHeadings();
  }
  updateGroupHeadings() {
    this.headingGroups.size && this.headingGroups.forEach((t2, e3) => {
      let s = t2.some((i4) => i4.visible !== false);
      e3.toggleAttribute("hidden", !s);
    });
  }
};
o([state()], a2.prototype, "smoothScroll", 2), o([state()], a2.prototype, "listSearchValue", 2), o([state()], a2.prototype, "currentItem", 2), o([state()], a2.prototype, "focusedItem", 2), o([state()], a2.prototype, "lastFocusedItem", 2), o([state()], a2.prototype, "keyboardNavigationActive", 2), o([property({ type: Object })], a2.prototype, "mousemovevisiblearea", 2), o([queryAssignedElements({ slot: "", flatten: true })], a2.prototype, "slottedListItems", 2), o([property({ type: Array })], a2.prototype, "data", 2), o([query(".mc-list")], a2.prototype, "list", 2), o([query(".list-search")], a2.prototype, "listSearchInput", 2), o([property({ type: Boolean })], a2.prototype, "listsearch", 2), o([property({ type: String })], a2.prototype, "listsearchplaceholder", 2), o([property({ type: String })], a2.prototype, "filtertype", 2), o([property({ attribute: false })], a2.prototype, "customfilter", 2), o([property({ type: Boolean })], a2.prototype, "matchlabelonly", 2), o([property({ type: Boolean })], a2.prototype, "disablehandlemousemove", 2), o([property({ type: Boolean })], a2.prototype, "disablecircularkeynav", 2), o([property({ type: Boolean })], a2.prototype, "disablesetfocus", 2), o([property({ type: Boolean })], a2.prototype, "scrolltofocused", 2), o([property({ type: Boolean })], a2.prototype, "hasactionslots", 2), o([property({ type: Object })], a2.prototype, "scrollablecontainer", 2), o([property({ type: String })], a2.prototype, "fit", 2), o([property({ type: String })], a2.prototype, "keynavigationtype", 2), o([property({ type: Boolean, reflect: true })], a2.prototype, "noborder", 2), o([property({ type: String, attribute: true, reflect: true })], a2.prototype, "orientation", 2), o([property({ type: String, reflect: true })], a2.prototype, "role", 2), o([property({ type: String, reflect: true })], a2.prototype, "type", 2), o([property({ type: Array, reflect: true })], a2.prototype, "value", 2), o([property({ type: String, reflect: true })], a2.prototype, "width", 2);
customElements.get("mc-list") || customElements.define("mc-list", a2);

// node_modules/@floating-ui/utils/dist/floating-ui.utils.esm.js
var sides = ["top", "right", "bottom", "left"];
var alignments = ["start", "end"];
var placements = sides.reduce((acc, side) => acc.concat(side, side + "-" + alignments[0], side + "-" + alignments[1]), []);
var min = Math.min;
var max = Math.max;
var round = Math.round;
var floor = Math.floor;
var createCoords = (v2) => ({
  x: v2,
  y: v2
});
var oppositeSideMap = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
var oppositeAlignmentMap = {
  start: "end",
  end: "start"
};
function clamp(start, value, end) {
  return max(start, min(value, end));
}
function evaluate(value, param) {
  return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
  return placement.split("-")[0];
}
function getAlignment(placement) {
  return placement.split("-")[1];
}
function getOppositeAxis(axis) {
  return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
  return axis === "y" ? "height" : "width";
}
var yAxisSides = /* @__PURE__ */ new Set(["top", "bottom"]);
function getSideAxis(placement) {
  return yAxisSides.has(getSide(placement)) ? "y" : "x";
}
function getAlignmentAxis(placement) {
  return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
  if (rtl === void 0) {
    rtl = false;
  }
  const alignment = getAlignment(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const length = getAxisLength(alignmentAxis);
  let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
  if (rects.reference[length] > rects.floating[length]) {
    mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
  }
  return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
  const oppositePlacement = getOppositePlacement(placement);
  return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
}
function getOppositeAlignmentPlacement(placement) {
  return placement.replace(/start|end/g, (alignment) => oppositeAlignmentMap[alignment]);
}
var lrPlacement = ["left", "right"];
var rlPlacement = ["right", "left"];
var tbPlacement = ["top", "bottom"];
var btPlacement = ["bottom", "top"];
function getSideList(side, isStart, rtl) {
  switch (side) {
    case "top":
    case "bottom":
      if (rtl) return isStart ? rlPlacement : lrPlacement;
      return isStart ? lrPlacement : rlPlacement;
    case "left":
    case "right":
      return isStart ? tbPlacement : btPlacement;
    default:
      return [];
  }
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
  const alignment = getAlignment(placement);
  let list = getSideList(getSide(placement), direction === "start", rtl);
  if (alignment) {
    list = list.map((side) => side + "-" + alignment);
    if (flipAlignment) {
      list = list.concat(list.map(getOppositeAlignmentPlacement));
    }
  }
  return list;
}
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, (side) => oppositeSideMap[side]);
}
function expandPaddingObject(padding) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...padding
  };
}
function getPaddingObject(padding) {
  return typeof padding !== "number" ? expandPaddingObject(padding) : {
    top: padding,
    right: padding,
    bottom: padding,
    left: padding
  };
}
function rectToClientRect(rect) {
  const {
    x,
    y: y3,
    width,
    height
  } = rect;
  return {
    width,
    height,
    top: y3,
    left: x,
    right: x + width,
    bottom: y3 + height,
    x,
    y: y3
  };
}

// node_modules/@floating-ui/core/dist/floating-ui.core.esm.js
function computeCoordsFromPlacement(_ref, placement, rtl) {
  let {
    reference,
    floating
  } = _ref;
  const sideAxis = getSideAxis(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const alignLength = getAxisLength(alignmentAxis);
  const side = getSide(placement);
  const isVertical = sideAxis === "y";
  const commonX = reference.x + reference.width / 2 - floating.width / 2;
  const commonY = reference.y + reference.height / 2 - floating.height / 2;
  const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
  let coords;
  switch (side) {
    case "top":
      coords = {
        x: commonX,
        y: reference.y - floating.height
      };
      break;
    case "bottom":
      coords = {
        x: commonX,
        y: reference.y + reference.height
      };
      break;
    case "right":
      coords = {
        x: reference.x + reference.width,
        y: commonY
      };
      break;
    case "left":
      coords = {
        x: reference.x - floating.width,
        y: commonY
      };
      break;
    default:
      coords = {
        x: reference.x,
        y: reference.y
      };
  }
  switch (getAlignment(placement)) {
    case "start":
      coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
      break;
    case "end":
      coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
      break;
  }
  return coords;
}
async function detectOverflow(state2, options) {
  var _await$platform$isEle;
  if (options === void 0) {
    options = {};
  }
  const {
    x,
    y: y3,
    platform: platform2,
    rects,
    elements,
    strategy
  } = state2;
  const {
    boundary = "clippingAncestors",
    rootBoundary = "viewport",
    elementContext = "floating",
    altBoundary = false,
    padding = 0
  } = evaluate(options, state2);
  const paddingObject = getPaddingObject(padding);
  const altContext = elementContext === "floating" ? "reference" : "floating";
  const element = elements[altBoundary ? altContext : elementContext];
  const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
    element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
    boundary,
    rootBoundary,
    strategy
  }));
  const rect = elementContext === "floating" ? {
    x,
    y: y3,
    width: rects.floating.width,
    height: rects.floating.height
  } : rects.reference;
  const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
  const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  };
  const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
    elements,
    rect,
    offsetParent,
    strategy
  }) : rect);
  return {
    top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
    bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
    left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
    right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
  };
}
var computePosition = async (reference, floating, config) => {
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware = [],
    platform: platform2
  } = config;
  const validMiddleware = middleware.filter(Boolean);
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
  let rects = await platform2.getElementRects({
    reference,
    floating,
    strategy
  });
  let {
    x,
    y: y3
  } = computeCoordsFromPlacement(rects, placement, rtl);
  let statefulPlacement = placement;
  let middlewareData = {};
  let resetCount = 0;
  for (let i4 = 0; i4 < validMiddleware.length; i4++) {
    var _platform$detectOverf;
    const {
      name,
      fn
    } = validMiddleware[i4];
    const {
      x: nextX,
      y: nextY,
      data,
      reset
    } = await fn({
      x,
      y: y3,
      initialPlacement: placement,
      placement: statefulPlacement,
      strategy,
      middlewareData,
      rects,
      platform: {
        ...platform2,
        detectOverflow: (_platform$detectOverf = platform2.detectOverflow) != null ? _platform$detectOverf : detectOverflow
      },
      elements: {
        reference,
        floating
      }
    });
    x = nextX != null ? nextX : x;
    y3 = nextY != null ? nextY : y3;
    middlewareData = {
      ...middlewareData,
      [name]: {
        ...middlewareData[name],
        ...data
      }
    };
    if (reset && resetCount <= 50) {
      resetCount++;
      if (typeof reset === "object") {
        if (reset.placement) {
          statefulPlacement = reset.placement;
        }
        if (reset.rects) {
          rects = reset.rects === true ? await platform2.getElementRects({
            reference,
            floating,
            strategy
          }) : reset.rects;
        }
        ({
          x,
          y: y3
        } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
      }
      i4 = -1;
    }
  }
  return {
    x,
    y: y3,
    placement: statefulPlacement,
    strategy,
    middlewareData
  };
};
var arrow = (options) => ({
  name: "arrow",
  options,
  async fn(state2) {
    const {
      x,
      y: y3,
      placement,
      rects,
      platform: platform2,
      elements,
      middlewareData
    } = state2;
    const {
      element,
      padding = 0
    } = evaluate(options, state2) || {};
    if (element == null) {
      return {};
    }
    const paddingObject = getPaddingObject(padding);
    const coords = {
      x,
      y: y3
    };
    const axis = getAlignmentAxis(placement);
    const length = getAxisLength(axis);
    const arrowDimensions = await platform2.getDimensions(element);
    const isYAxis = axis === "y";
    const minProp = isYAxis ? "top" : "left";
    const maxProp = isYAxis ? "bottom" : "right";
    const clientProp = isYAxis ? "clientHeight" : "clientWidth";
    const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
    const startDiff = coords[axis] - rects.reference[axis];
    const arrowOffsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(element));
    let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
    if (!clientSize || !await (platform2.isElement == null ? void 0 : platform2.isElement(arrowOffsetParent))) {
      clientSize = elements.floating[clientProp] || rects.floating[length];
    }
    const centerToReference = endDiff / 2 - startDiff / 2;
    const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
    const minPadding = min(paddingObject[minProp], largestPossiblePadding);
    const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
    const min$1 = minPadding;
    const max2 = clientSize - arrowDimensions[length] - maxPadding;
    const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
    const offset3 = clamp(min$1, center, max2);
    const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset3 && rects.reference[length] / 2 - (center < min$1 ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
    const alignmentOffset = shouldAddOffset ? center < min$1 ? center - min$1 : center - max2 : 0;
    return {
      [axis]: coords[axis] + alignmentOffset,
      data: {
        [axis]: offset3,
        centerOffset: center - offset3 - alignmentOffset,
        ...shouldAddOffset && {
          alignmentOffset
        }
      },
      reset: shouldAddOffset
    };
  }
});
var flip = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "flip",
    options,
    async fn(state2) {
      var _middlewareData$arrow, _middlewareData$flip;
      const {
        placement,
        middlewareData,
        rects,
        initialPlacement,
        platform: platform2,
        elements
      } = state2;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = true,
        fallbackPlacements: specifiedFallbackPlacements,
        fallbackStrategy = "bestFit",
        fallbackAxisSideDirection = "none",
        flipAlignment = true,
        ...detectOverflowOptions
      } = evaluate(options, state2);
      if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
        return {};
      }
      const side = getSide(placement);
      const initialSideAxis = getSideAxis(initialPlacement);
      const isBasePlacement = getSide(initialPlacement) === initialPlacement;
      const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
      const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
      const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
      if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
        fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
      }
      const placements2 = [initialPlacement, ...fallbackPlacements];
      const overflow = await platform2.detectOverflow(state2, detectOverflowOptions);
      const overflows = [];
      let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
      if (checkMainAxis) {
        overflows.push(overflow[side]);
      }
      if (checkCrossAxis) {
        const sides2 = getAlignmentSides(placement, rects, rtl);
        overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
      }
      overflowsData = [...overflowsData, {
        placement,
        overflows
      }];
      if (!overflows.every((side2) => side2 <= 0)) {
        var _middlewareData$flip2, _overflowsData$filter;
        const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
        const nextPlacement = placements2[nextIndex];
        if (nextPlacement) {
          const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
          if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
          // overflows the main axis.
          overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) {
            return {
              data: {
                index: nextIndex,
                overflows: overflowsData
              },
              reset: {
                placement: nextPlacement
              }
            };
          }
        }
        let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a3, b2) => a3.overflows[1] - b2.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
        if (!resetPlacement) {
          switch (fallbackStrategy) {
            case "bestFit": {
              var _overflowsData$filter2;
              const placement2 = (_overflowsData$filter2 = overflowsData.filter((d) => {
                if (hasFallbackAxisSideDirection) {
                  const currentSideAxis = getSideAxis(d.placement);
                  return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                  // reading directions favoring greater width.
                  currentSideAxis === "y";
                }
                return true;
              }).map((d) => [d.placement, d.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a3, b2) => a3[1] - b2[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
              if (placement2) {
                resetPlacement = placement2;
              }
              break;
            }
            case "initialPlacement":
              resetPlacement = initialPlacement;
              break;
          }
        }
        if (placement !== resetPlacement) {
          return {
            reset: {
              placement: resetPlacement
            }
          };
        }
      }
      return {};
    }
  };
};
var originSides = /* @__PURE__ */ new Set(["left", "top"]);
async function convertValueToCoords(state2, options) {
  const {
    placement,
    platform: platform2,
    elements
  } = state2;
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
  const side = getSide(placement);
  const alignment = getAlignment(placement);
  const isVertical = getSideAxis(placement) === "y";
  const mainAxisMulti = originSides.has(side) ? -1 : 1;
  const crossAxisMulti = rtl && isVertical ? -1 : 1;
  const rawValue = evaluate(options, state2);
  let {
    mainAxis,
    crossAxis,
    alignmentAxis
  } = typeof rawValue === "number" ? {
    mainAxis: rawValue,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: rawValue.mainAxis || 0,
    crossAxis: rawValue.crossAxis || 0,
    alignmentAxis: rawValue.alignmentAxis
  };
  if (alignment && typeof alignmentAxis === "number") {
    crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
  }
  return isVertical ? {
    x: crossAxis * crossAxisMulti,
    y: mainAxis * mainAxisMulti
  } : {
    x: mainAxis * mainAxisMulti,
    y: crossAxis * crossAxisMulti
  };
}
var offset = function(options) {
  if (options === void 0) {
    options = 0;
  }
  return {
    name: "offset",
    options,
    async fn(state2) {
      var _middlewareData$offse, _middlewareData$arrow;
      const {
        x,
        y: y3,
        placement,
        middlewareData
      } = state2;
      const diffCoords = await convertValueToCoords(state2, options);
      if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
        return {};
      }
      return {
        x: x + diffCoords.x,
        y: y3 + diffCoords.y,
        data: {
          ...diffCoords,
          placement
        }
      };
    }
  };
};
var shift = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "shift",
    options,
    async fn(state2) {
      const {
        x,
        y: y3,
        placement,
        platform: platform2
      } = state2;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = false,
        limiter = {
          fn: (_ref) => {
            let {
              x: x2,
              y: y4
            } = _ref;
            return {
              x: x2,
              y: y4
            };
          }
        },
        ...detectOverflowOptions
      } = evaluate(options, state2);
      const coords = {
        x,
        y: y3
      };
      const overflow = await platform2.detectOverflow(state2, detectOverflowOptions);
      const crossAxis = getSideAxis(getSide(placement));
      const mainAxis = getOppositeAxis(crossAxis);
      let mainAxisCoord = coords[mainAxis];
      let crossAxisCoord = coords[crossAxis];
      if (checkMainAxis) {
        const minSide = mainAxis === "y" ? "top" : "left";
        const maxSide = mainAxis === "y" ? "bottom" : "right";
        const min2 = mainAxisCoord + overflow[minSide];
        const max2 = mainAxisCoord - overflow[maxSide];
        mainAxisCoord = clamp(min2, mainAxisCoord, max2);
      }
      if (checkCrossAxis) {
        const minSide = crossAxis === "y" ? "top" : "left";
        const maxSide = crossAxis === "y" ? "bottom" : "right";
        const min2 = crossAxisCoord + overflow[minSide];
        const max2 = crossAxisCoord - overflow[maxSide];
        crossAxisCoord = clamp(min2, crossAxisCoord, max2);
      }
      const limitedCoords = limiter.fn({
        ...state2,
        [mainAxis]: mainAxisCoord,
        [crossAxis]: crossAxisCoord
      });
      return {
        ...limitedCoords,
        data: {
          x: limitedCoords.x - x,
          y: limitedCoords.y - y3,
          enabled: {
            [mainAxis]: checkMainAxis,
            [crossAxis]: checkCrossAxis
          }
        }
      };
    }
  };
};
var limitShift = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    options,
    fn(state2) {
      const {
        x,
        y: y3,
        placement,
        rects,
        middlewareData
      } = state2;
      const {
        offset: offset3 = 0,
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = true
      } = evaluate(options, state2);
      const coords = {
        x,
        y: y3
      };
      const crossAxis = getSideAxis(placement);
      const mainAxis = getOppositeAxis(crossAxis);
      let mainAxisCoord = coords[mainAxis];
      let crossAxisCoord = coords[crossAxis];
      const rawOffset = evaluate(offset3, state2);
      const computedOffset = typeof rawOffset === "number" ? {
        mainAxis: rawOffset,
        crossAxis: 0
      } : {
        mainAxis: 0,
        crossAxis: 0,
        ...rawOffset
      };
      if (checkMainAxis) {
        const len = mainAxis === "y" ? "height" : "width";
        const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
        const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
        if (mainAxisCoord < limitMin) {
          mainAxisCoord = limitMin;
        } else if (mainAxisCoord > limitMax) {
          mainAxisCoord = limitMax;
        }
      }
      if (checkCrossAxis) {
        var _middlewareData$offse, _middlewareData$offse2;
        const len = mainAxis === "y" ? "width" : "height";
        const isOriginSide = originSides.has(getSide(placement));
        const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
        const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
        if (crossAxisCoord < limitMin) {
          crossAxisCoord = limitMin;
        } else if (crossAxisCoord > limitMax) {
          crossAxisCoord = limitMax;
        }
      }
      return {
        [mainAxis]: mainAxisCoord,
        [crossAxis]: crossAxisCoord
      };
    }
  };
};
var size = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "size",
    options,
    async fn(state2) {
      var _state$middlewareData, _state$middlewareData2;
      const {
        placement,
        rects,
        platform: platform2,
        elements
      } = state2;
      const {
        apply = () => {
        },
        ...detectOverflowOptions
      } = evaluate(options, state2);
      const overflow = await platform2.detectOverflow(state2, detectOverflowOptions);
      const side = getSide(placement);
      const alignment = getAlignment(placement);
      const isYAxis = getSideAxis(placement) === "y";
      const {
        width,
        height
      } = rects.floating;
      let heightSide;
      let widthSide;
      if (side === "top" || side === "bottom") {
        heightSide = side;
        widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
      } else {
        widthSide = side;
        heightSide = alignment === "end" ? "top" : "bottom";
      }
      const maximumClippingHeight = height - overflow.top - overflow.bottom;
      const maximumClippingWidth = width - overflow.left - overflow.right;
      const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
      const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
      const noShift = !state2.middlewareData.shift;
      let availableHeight = overflowAvailableHeight;
      let availableWidth = overflowAvailableWidth;
      if ((_state$middlewareData = state2.middlewareData.shift) != null && _state$middlewareData.enabled.x) {
        availableWidth = maximumClippingWidth;
      }
      if ((_state$middlewareData2 = state2.middlewareData.shift) != null && _state$middlewareData2.enabled.y) {
        availableHeight = maximumClippingHeight;
      }
      if (noShift && !alignment) {
        const xMin = max(overflow.left, 0);
        const xMax = max(overflow.right, 0);
        const yMin = max(overflow.top, 0);
        const yMax = max(overflow.bottom, 0);
        if (isYAxis) {
          availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
        } else {
          availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
        }
      }
      await apply({
        ...state2,
        availableWidth,
        availableHeight
      });
      const nextDimensions = await platform2.getDimensions(elements.floating);
      if (width !== nextDimensions.width || height !== nextDimensions.height) {
        return {
          reset: {
            rects: true
          }
        };
      }
      return {};
    }
  };
};

// node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.esm.js
function hasWindow() {
  return typeof window !== "undefined";
}
function getNodeName(node) {
  if (isNode(node)) {
    return (node.nodeName || "").toLowerCase();
  }
  return "#document";
}
function getWindow(node) {
  var _node$ownerDocument;
  return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node) {
  var _ref;
  return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Node || value instanceof getWindow(value).Node;
}
function isElement(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Element || value instanceof getWindow(value).Element;
}
function isHTMLElement(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function isShadowRoot(value) {
  if (!hasWindow() || typeof ShadowRoot === "undefined") {
    return false;
  }
  return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
}
var invalidOverflowDisplayValues = /* @__PURE__ */ new Set(["inline", "contents"]);
function isOverflowElement(element) {
  const {
    overflow,
    overflowX,
    overflowY,
    display
  } = getComputedStyle2(element);
  return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && !invalidOverflowDisplayValues.has(display);
}
var tableElements = /* @__PURE__ */ new Set(["table", "td", "th"]);
function isTableElement(element) {
  return tableElements.has(getNodeName(element));
}
var topLayerSelectors = [":popover-open", ":modal"];
function isTopLayer(element) {
  return topLayerSelectors.some((selector) => {
    try {
      return element.matches(selector);
    } catch (_e) {
      return false;
    }
  });
}
var transformProperties = ["transform", "translate", "scale", "rotate", "perspective"];
var willChangeValues = ["transform", "translate", "scale", "rotate", "perspective", "filter"];
var containValues = ["paint", "layout", "strict", "content"];
function isContainingBlock(elementOrCss) {
  const webkit = isWebKit();
  const css2 = isElement(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
  return transformProperties.some((value) => css2[value] ? css2[value] !== "none" : false) || (css2.containerType ? css2.containerType !== "normal" : false) || !webkit && (css2.backdropFilter ? css2.backdropFilter !== "none" : false) || !webkit && (css2.filter ? css2.filter !== "none" : false) || willChangeValues.some((value) => (css2.willChange || "").includes(value)) || containValues.some((value) => (css2.contain || "").includes(value));
}
function getContainingBlock(element) {
  let currentNode = getParentNode(element);
  while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
    if (isContainingBlock(currentNode)) {
      return currentNode;
    } else if (isTopLayer(currentNode)) {
      return null;
    }
    currentNode = getParentNode(currentNode);
  }
  return null;
}
function isWebKit() {
  if (typeof CSS === "undefined" || !CSS.supports) return false;
  return CSS.supports("-webkit-backdrop-filter", "none");
}
var lastTraversableNodeNames = /* @__PURE__ */ new Set(["html", "body", "#document"]);
function isLastTraversableNode(node) {
  return lastTraversableNodeNames.has(getNodeName(node));
}
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}
function getNodeScroll(element) {
  if (isElement(element)) {
    return {
      scrollLeft: element.scrollLeft,
      scrollTop: element.scrollTop
    };
  }
  return {
    scrollLeft: element.scrollX,
    scrollTop: element.scrollY
  };
}
function getParentNode(node) {
  if (getNodeName(node) === "html") {
    return node;
  }
  const result = (
    // Step into the shadow DOM of the parent of a slotted node.
    node.assignedSlot || // DOM Element detected.
    node.parentNode || // ShadowRoot detected.
    isShadowRoot(node) && node.host || // Fallback.
    getDocumentElement(node)
  );
  return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node) {
  const parentNode = getParentNode(node);
  if (isLastTraversableNode(parentNode)) {
    return node.ownerDocument ? node.ownerDocument.body : node.body;
  }
  if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
    return parentNode;
  }
  return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node, list, traverseIframes) {
  var _node$ownerDocument2;
  if (list === void 0) {
    list = [];
  }
  if (traverseIframes === void 0) {
    traverseIframes = true;
  }
  const scrollableAncestor = getNearestOverflowAncestor(node);
  const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
  const win = getWindow(scrollableAncestor);
  if (isBody) {
    const frameElement = getFrameElement(win);
    return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
  }
  return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
}
function getFrameElement(win) {
  return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}

// node_modules/@floating-ui/dom/dist/floating-ui.dom.esm.js
function getCssDimensions(element) {
  const css2 = getComputedStyle2(element);
  let width = parseFloat(css2.width) || 0;
  let height = parseFloat(css2.height) || 0;
  const hasOffset = isHTMLElement(element);
  const offsetWidth = hasOffset ? element.offsetWidth : width;
  const offsetHeight = hasOffset ? element.offsetHeight : height;
  const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
  if (shouldFallback) {
    width = offsetWidth;
    height = offsetHeight;
  }
  return {
    width,
    height,
    $: shouldFallback
  };
}
function unwrapElement(element) {
  return !isElement(element) ? element.contextElement : element;
}
function getScale(element) {
  const domElement = unwrapElement(element);
  if (!isHTMLElement(domElement)) {
    return createCoords(1);
  }
  const rect = domElement.getBoundingClientRect();
  const {
    width,
    height,
    $
  } = getCssDimensions(domElement);
  let x = ($ ? round(rect.width) : rect.width) / width;
  let y3 = ($ ? round(rect.height) : rect.height) / height;
  if (!x || !Number.isFinite(x)) {
    x = 1;
  }
  if (!y3 || !Number.isFinite(y3)) {
    y3 = 1;
  }
  return {
    x,
    y: y3
  };
}
var noOffsets = createCoords(0);
function getVisualOffsets(element) {
  const win = getWindow(element);
  if (!isWebKit() || !win.visualViewport) {
    return noOffsets;
  }
  return {
    x: win.visualViewport.offsetLeft,
    y: win.visualViewport.offsetTop
  };
}
function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) {
    return false;
  }
  return isFixed;
}
function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  const clientRect = element.getBoundingClientRect();
  const domElement = unwrapElement(element);
  let scale = createCoords(1);
  if (includeScale) {
    if (offsetParent) {
      if (isElement(offsetParent)) {
        scale = getScale(offsetParent);
      }
    } else {
      scale = getScale(element);
    }
  }
  const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
  let x = (clientRect.left + visualOffsets.x) / scale.x;
  let y3 = (clientRect.top + visualOffsets.y) / scale.y;
  let width = clientRect.width / scale.x;
  let height = clientRect.height / scale.y;
  if (domElement) {
    const win = getWindow(domElement);
    const offsetWin = offsetParent && isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
    let currentWin = win;
    let currentIFrame = getFrameElement(currentWin);
    while (currentIFrame && offsetParent && offsetWin !== currentWin) {
      const iframeScale = getScale(currentIFrame);
      const iframeRect = currentIFrame.getBoundingClientRect();
      const css2 = getComputedStyle2(currentIFrame);
      const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css2.paddingLeft)) * iframeScale.x;
      const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css2.paddingTop)) * iframeScale.y;
      x *= iframeScale.x;
      y3 *= iframeScale.y;
      width *= iframeScale.x;
      height *= iframeScale.y;
      x += left;
      y3 += top;
      currentWin = getWindow(currentIFrame);
      currentIFrame = getFrameElement(currentWin);
    }
  }
  return rectToClientRect({
    width,
    height,
    x,
    y: y3
  });
}
function getWindowScrollBarX(element, rect) {
  const leftScroll = getNodeScroll(element).scrollLeft;
  if (!rect) {
    return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
  }
  return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll) {
  const htmlRect = documentElement.getBoundingClientRect();
  const x = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
  const y3 = htmlRect.top + scroll.scrollTop;
  return {
    x,
    y: y3
  };
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
  let {
    elements,
    rect,
    offsetParent,
    strategy
  } = _ref;
  const isFixed = strategy === "fixed";
  const documentElement = getDocumentElement(offsetParent);
  const topLayer = elements ? isTopLayer(elements.floating) : false;
  if (offsetParent === documentElement || topLayer && isFixed) {
    return rect;
  }
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  let scale = createCoords(1);
  const offsets = createCoords(0);
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      const offsetRect = getBoundingClientRect(offsetParent);
      scale = getScale(offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    }
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
  return {
    width: rect.width * scale.x,
    height: rect.height * scale.y,
    x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
    y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
  };
}
function getClientRects(element) {
  return Array.from(element.getClientRects());
}
function getDocumentRect(element) {
  const html2 = getDocumentElement(element);
  const scroll = getNodeScroll(element);
  const body = element.ownerDocument.body;
  const width = max(html2.scrollWidth, html2.clientWidth, body.scrollWidth, body.clientWidth);
  const height = max(html2.scrollHeight, html2.clientHeight, body.scrollHeight, body.clientHeight);
  let x = -scroll.scrollLeft + getWindowScrollBarX(element);
  const y3 = -scroll.scrollTop;
  if (getComputedStyle2(body).direction === "rtl") {
    x += max(html2.clientWidth, body.clientWidth) - width;
  }
  return {
    width,
    height,
    x,
    y: y3
  };
}
var SCROLLBAR_MAX = 25;
function getViewportRect(element, strategy) {
  const win = getWindow(element);
  const html2 = getDocumentElement(element);
  const visualViewport = win.visualViewport;
  let width = html2.clientWidth;
  let height = html2.clientHeight;
  let x = 0;
  let y3 = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    const visualViewportBased = isWebKit();
    if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
      x = visualViewport.offsetLeft;
      y3 = visualViewport.offsetTop;
    }
  }
  const windowScrollbarX = getWindowScrollBarX(html2);
  if (windowScrollbarX <= 0) {
    const doc = html2.ownerDocument;
    const body = doc.body;
    const bodyStyles = getComputedStyle(body);
    const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
    const clippingStableScrollbarWidth = Math.abs(html2.clientWidth - body.clientWidth - bodyMarginInline);
    if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) {
      width -= clippingStableScrollbarWidth;
    }
  } else if (windowScrollbarX <= SCROLLBAR_MAX) {
    width += windowScrollbarX;
  }
  return {
    width,
    height,
    x,
    y: y3
  };
}
var absoluteOrFixed = /* @__PURE__ */ new Set(["absolute", "fixed"]);
function getInnerBoundingClientRect(element, strategy) {
  const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
  const top = clientRect.top + element.clientTop;
  const left = clientRect.left + element.clientLeft;
  const scale = isHTMLElement(element) ? getScale(element) : createCoords(1);
  const width = element.clientWidth * scale.x;
  const height = element.clientHeight * scale.y;
  const x = left * scale.x;
  const y3 = top * scale.y;
  return {
    width,
    height,
    x,
    y: y3
  };
}
function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
  let rect;
  if (clippingAncestor === "viewport") {
    rect = getViewportRect(element, strategy);
  } else if (clippingAncestor === "document") {
    rect = getDocumentRect(getDocumentElement(element));
  } else if (isElement(clippingAncestor)) {
    rect = getInnerBoundingClientRect(clippingAncestor, strategy);
  } else {
    const visualOffsets = getVisualOffsets(element);
    rect = {
      x: clippingAncestor.x - visualOffsets.x,
      y: clippingAncestor.y - visualOffsets.y,
      width: clippingAncestor.width,
      height: clippingAncestor.height
    };
  }
  return rectToClientRect(rect);
}
function hasFixedPositionAncestor(element, stopNode) {
  const parentNode = getParentNode(element);
  if (parentNode === stopNode || !isElement(parentNode) || isLastTraversableNode(parentNode)) {
    return false;
  }
  return getComputedStyle2(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
}
function getClippingElementAncestors(element, cache) {
  const cachedResult = cache.get(element);
  if (cachedResult) {
    return cachedResult;
  }
  let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
  let currentContainingBlockComputedStyle = null;
  const elementIsFixed = getComputedStyle2(element).position === "fixed";
  let currentNode = elementIsFixed ? getParentNode(element) : element;
  while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
    const computedStyle = getComputedStyle2(currentNode);
    const currentNodeIsContaining = isContainingBlock(currentNode);
    if (!currentNodeIsContaining && computedStyle.position === "fixed") {
      currentContainingBlockComputedStyle = null;
    }
    const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && absoluteOrFixed.has(currentContainingBlockComputedStyle.position) || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode);
    if (shouldDropCurrentNode) {
      result = result.filter((ancestor) => ancestor !== currentNode);
    } else {
      currentContainingBlockComputedStyle = computedStyle;
    }
    currentNode = getParentNode(currentNode);
  }
  cache.set(element, result);
  return result;
}
function getClippingRect(_ref) {
  let {
    element,
    boundary,
    rootBoundary,
    strategy
  } = _ref;
  const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
  const clippingAncestors = [...elementClippingAncestors, rootBoundary];
  const firstClippingAncestor = clippingAncestors[0];
  const clippingRect = clippingAncestors.reduce((accRect, clippingAncestor) => {
    const rect = getClientRectFromClippingAncestor(element, clippingAncestor, strategy);
    accRect.top = max(rect.top, accRect.top);
    accRect.right = min(rect.right, accRect.right);
    accRect.bottom = min(rect.bottom, accRect.bottom);
    accRect.left = max(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromClippingAncestor(element, firstClippingAncestor, strategy));
  return {
    width: clippingRect.right - clippingRect.left,
    height: clippingRect.bottom - clippingRect.top,
    x: clippingRect.left,
    y: clippingRect.top
  };
}
function getDimensions(element) {
  const {
    width,
    height
  } = getCssDimensions(element);
  return {
    width,
    height
  };
}
function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  const documentElement = getDocumentElement(offsetParent);
  const isFixed = strategy === "fixed";
  const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const offsets = createCoords(0);
  function setLeftRTLScrollbarOffset() {
    offsets.x = getWindowScrollBarX(documentElement);
  }
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isOffsetParentAnElement) {
      const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    } else if (documentElement) {
      setLeftRTLScrollbarOffset();
    }
  }
  if (isFixed && !isOffsetParentAnElement && documentElement) {
    setLeftRTLScrollbarOffset();
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
  const x = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
  const y3 = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
  return {
    x,
    y: y3,
    width: rect.width,
    height: rect.height
  };
}
function isStaticPositioned(element) {
  return getComputedStyle2(element).position === "static";
}
function getTrueOffsetParent(element, polyfill) {
  if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
    return null;
  }
  if (polyfill) {
    return polyfill(element);
  }
  let rawOffsetParent = element.offsetParent;
  if (getDocumentElement(element) === rawOffsetParent) {
    rawOffsetParent = rawOffsetParent.ownerDocument.body;
  }
  return rawOffsetParent;
}
function getOffsetParent(element, polyfill) {
  const win = getWindow(element);
  if (isTopLayer(element)) {
    return win;
  }
  if (!isHTMLElement(element)) {
    let svgOffsetParent = getParentNode(element);
    while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
      if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
        return svgOffsetParent;
      }
      svgOffsetParent = getParentNode(svgOffsetParent);
    }
    return win;
  }
  let offsetParent = getTrueOffsetParent(element, polyfill);
  while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
    offsetParent = getTrueOffsetParent(offsetParent, polyfill);
  }
  if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
    return win;
  }
  return offsetParent || getContainingBlock(element) || win;
}
var getElementRects = async function(data) {
  const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
  const getDimensionsFn = this.getDimensions;
  const floatingDimensions = await getDimensionsFn(data.floating);
  return {
    reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
    floating: {
      x: 0,
      y: 0,
      width: floatingDimensions.width,
      height: floatingDimensions.height
    }
  };
};
function isRTL(element) {
  return getComputedStyle2(element).direction === "rtl";
}
var platform = {
  convertOffsetParentRelativeRectToViewportRelativeRect,
  getDocumentElement,
  getClippingRect,
  getOffsetParent,
  getElementRects,
  getClientRects,
  getDimensions,
  getScale,
  isElement,
  isRTL
};
function rectsAreEqual(a3, b2) {
  return a3.x === b2.x && a3.y === b2.y && a3.width === b2.width && a3.height === b2.height;
}
function observeMove(element, onMove) {
  let io = null;
  let timeoutId;
  const root = getDocumentElement(element);
  function cleanup() {
    var _io;
    clearTimeout(timeoutId);
    (_io = io) == null || _io.disconnect();
    io = null;
  }
  function refresh(skip, threshold) {
    if (skip === void 0) {
      skip = false;
    }
    if (threshold === void 0) {
      threshold = 1;
    }
    cleanup();
    const elementRectForRootMargin = element.getBoundingClientRect();
    const {
      left,
      top,
      width,
      height
    } = elementRectForRootMargin;
    if (!skip) {
      onMove();
    }
    if (!width || !height) {
      return;
    }
    const insetTop = floor(top);
    const insetRight = floor(root.clientWidth - (left + width));
    const insetBottom = floor(root.clientHeight - (top + height));
    const insetLeft = floor(left);
    const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
    const options = {
      rootMargin,
      threshold: max(0, min(1, threshold)) || 1
    };
    let isFirstUpdate = true;
    function handleObserve(entries) {
      const ratio = entries[0].intersectionRatio;
      if (ratio !== threshold) {
        if (!isFirstUpdate) {
          return refresh();
        }
        if (!ratio) {
          timeoutId = setTimeout(() => {
            refresh(false, 1e-7);
          }, 1e3);
        } else {
          refresh(false, ratio);
        }
      }
      if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) {
        refresh();
      }
      isFirstUpdate = false;
    }
    try {
      io = new IntersectionObserver(handleObserve, {
        ...options,
        // Handle <iframe>s
        root: root.ownerDocument
      });
    } catch (_e) {
      io = new IntersectionObserver(handleObserve, options);
    }
    io.observe(element);
  }
  refresh(true);
  return cleanup;
}
function autoUpdate(reference, floating, update, options) {
  if (options === void 0) {
    options = {};
  }
  const {
    ancestorScroll = true,
    ancestorResize = true,
    elementResize = typeof ResizeObserver === "function",
    layoutShift = typeof IntersectionObserver === "function",
    animationFrame = false
  } = options;
  const referenceEl = unwrapElement(reference);
  const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...getOverflowAncestors(floating)] : [];
  ancestors.forEach((ancestor) => {
    ancestorScroll && ancestor.addEventListener("scroll", update, {
      passive: true
    });
    ancestorResize && ancestor.addEventListener("resize", update);
  });
  const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
  let reobserveFrame = -1;
  let resizeObserver = null;
  if (elementResize) {
    resizeObserver = new ResizeObserver((_ref) => {
      let [firstEntry] = _ref;
      if (firstEntry && firstEntry.target === referenceEl && resizeObserver) {
        resizeObserver.unobserve(floating);
        cancelAnimationFrame(reobserveFrame);
        reobserveFrame = requestAnimationFrame(() => {
          var _resizeObserver;
          (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
        });
      }
      update();
    });
    if (referenceEl && !animationFrame) {
      resizeObserver.observe(referenceEl);
    }
    resizeObserver.observe(floating);
  }
  let frameId;
  let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
  if (animationFrame) {
    frameLoop();
  }
  function frameLoop() {
    const nextRefRect = getBoundingClientRect(reference);
    if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
      update();
    }
    prevRefRect = nextRefRect;
    frameId = requestAnimationFrame(frameLoop);
  }
  update();
  return () => {
    var _resizeObserver2;
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.removeEventListener("scroll", update);
      ancestorResize && ancestor.removeEventListener("resize", update);
    });
    cleanupIo == null || cleanupIo();
    (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
    resizeObserver = null;
    if (animationFrame) {
      cancelAnimationFrame(frameId);
    }
  };
}
var detectOverflow2 = detectOverflow;
var offset2 = offset;
var shift2 = shift;
var flip2 = flip;
var size2 = size;
var arrow2 = arrow;
var limitShift2 = limitShift;
var computePosition2 = (reference, floating, options) => {
  const cache = /* @__PURE__ */ new Map();
  const mergedOptions = {
    platform,
    ...options
  };
  const platformWithCache = {
    ...mergedOptions.platform,
    _c: cache
  };
  return computePosition(reference, floating, {
    ...mergedOptions,
    platform: platformWithCache
  });
};

// node_modules/@maersk-global/mds-components-core-popover/index.js
var R = Object.defineProperty;
var M = Object.getOwnPropertyDescriptor;
var r = (h, l, e3, t2) => {
  for (var n = t2 > 1 ? void 0 : t2 ? M(l, e3) : l, s = h.length - 1, a3; s >= 0; s--) (a3 = h[s]) && (n = (t2 ? a3(l, e3, n) : a3(n)) || n);
  return t2 && n && R(l, e3, n), n;
};
var g2 = css`.mc-popover .content{display:block;left:0;max-width:calc(100vw - 10px);position:fixed;top:0;width:max-content;z-index:99999;border-radius:var(--mds_brand_border_medium_radius);box-shadow:var(--mds_brand_appearance_shadow_low_first-layer_offset-x) var(--mds_brand_appearance_shadow_low_first-layer_offset-y) var(--mds_brand_appearance_shadow_low_first-layer_blur-radius) var(--mds_brand_appearance_shadow_low_first-layer_spread-radius) var(--mds_brand_appearance_shadow_low_first-layer_color),var(--mds_brand_appearance_shadow_low_second-layer_offset-x) var(--mds_brand_appearance_shadow_low_second-layer_offset-y) var(--mds_brand_appearance_shadow_low_second-layer_blur-radius) var(--mds_brand_appearance_shadow_low_second-layer_spread-radius) var(--mds_brand_appearance_shadow_low_second-layer_color),var(--mds_brand_appearance_shadow_low_third-layer_offset-x) var(--mds_brand_appearance_shadow_low_third-layer_offset-y) var(--mds_brand_appearance_shadow_low_third-layer_blur-radius) var(--mds_brand_appearance_shadow_low_third-layer_spread-radius) var(--mds_brand_appearance_shadow_low_third-layer_color)}.mc-popover .content .arrow,.mc-popover .content .container{background:var(--mds_brand_appearance_neutral_default_background-color)}.mc-popover .content,.mc-popover .content .arrow{border-color:var(--mds_brand_appearance_neutral_default_border-color);border-style:var(--mds_global_border_style);border-width:var(--mds_global_border_width)}.mc-popover .content .arrow{position:absolute;transform:rotate(45deg);z-index:-1}.mc-popover .content .container{border-radius:calc(var(--mds_brand_border_medium_radius) - 1px);color:var(--mds_brand_appearance_neutral_default_on-background-color);overflow:auto}.mc-popover .content .container .body-slot-wrapper{height:100%;width:100%}.mc-popover .content .container footer,.mc-popover .content .container header{display:none}@media only screen and (max-width:640px){.mc-popover.modal-on-x-small-screen .content .container header{align-items:center;display:flex;justify-content:space-between;padding:var(--mds_foundations_modal_heading_padding-top) var(--mds_foundations_modal_heading_padding-right) var(--mds_foundations_modal_heading_padding-bottom) var(--mds_foundations_modal_heading_padding-left);width:100%}.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_title,.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_title::slotted(*){margin:var(--mds_foundations_modal_heading_title_margin-top)0 0 0;font-family:var(--mds_brand_typography_headline_font-family),var(--mds_brand_typography_headline_font-family-fallback);font-size:var(--mds_brand_typography_headline_medium_mobile_font-size);line-height:var(--mds_brand_typography_headline_medium_mobile_line-height)}}@media only screen and (max-width:640px) and (min-width:1025px){.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_title,.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_title::slotted(*){font-size:var(--mds_brand_typography_headline_medium_desktop_font-size);line-height:var(--mds_brand_typography_headline_medium_desktop_line-height)}}@media only screen and (max-width:640px){.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_title,.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_title::slotted(*){text-transform:var(--mds_brand_typography_headline_medium_text-transform);font-style:var(--mds_brand_typography_headline_medium_font-style);font-weight:var(--mds_brand_typography_headline_medium_font-weight)}.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_close-button::part(button){background-color:var(--mds_foundations_modal_close_background-color)}.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_close-button::part(button):active,.mc-popover.modal-on-x-small-screen .content .container header .mds-modal_close-button::part(button):hover{background-color:var(--mds_foundations_modal_close_active_background-color)}.mc-popover.modal-on-x-small-screen .content .container footer{align-items:center;background-color:var(--mds_foundations_modal_footer_background-color);display:flex;gap:var(--mds_foundations_modal_footer_gap);justify-content:flex-end;margin-top:auto;padding:var(--mds_foundations_modal_footer_padding-top) var(--mds_foundations_modal_footer_padding-right) var(--mds_foundations_modal_footer_padding-bottom) var(--mds_foundations_modal_footer_padding-left);width:100%}.mc-popover.modal-on-x-small-screen .content .container footer.--empty{display:none}}@media only screen and (max-width:640px) and (max-width:640px){.mc-popover.modal-on-x-small-screen .content .container{align-items:center;background-color:var(--mds_foundations_modal_background-color);border-radius:0;display:flex;flex-direction:column;height:100%;justify-content:flex-start;left:0;position:fixed;top:0;touch-action:none;width:100%;z-index:var(--mds_foundations_modal_z-index)}.mc-popover.modal-on-x-small-screen .content .container .mds-modal_body{overflow:hidden;padding:var(--mds_foundations_modal_body_padding-top) var(--mds_foundations_modal_body_padding-right) var(--mds_foundations_modal_body_padding-bottom) var(--mds_foundations_modal_body_padding-left);width:100%}}.mc-popover.small .arrow{height:7px;width:7px}.mc-popover.small{font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);font-size:var(--mds_brand_typography_text_small_mobile_font-size);line-height:var(--mds_brand_typography_text_small_mobile_line-height)}@media screen and (min-width:1025px){.mc-popover.small{font-size:var(--mds_brand_typography_text_small_desktop_font-size);line-height:var(--mds_brand_typography_text_small_desktop_line-height)}}.mc-popover.small{font-style:var(--mds_brand_typography_text_small_normal_font-style);font-weight:var(--mds_brand_typography_text_small_normal_font-weight)}.mc-popover.medium .arrow{height:9px;width:9px}.mc-popover.medium{font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);font-size:var(--mds_brand_typography_text_medium_mobile_font-size);line-height:var(--mds_brand_typography_text_medium_mobile_line-height)}@media screen and (min-width:1025px){.mc-popover.medium{font-size:var(--mds_brand_typography_text_medium_desktop_font-size);line-height:var(--mds_brand_typography_text_medium_desktop_line-height)}}.mc-popover.medium{font-style:var(--mds_brand_typography_text_medium_normal_font-style);font-weight:var(--mds_brand_typography_text_medium_normal_font-weight)}.mc-popover.large .arrow{height:11px;width:11px}.mc-popover.large{font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);font-size:var(--mds_brand_typography_text_medium_mobile_font-size);line-height:var(--mds_brand_typography_text_medium_mobile_line-height)}@media screen and (min-width:1025px){.mc-popover.large{font-size:var(--mds_brand_typography_text_medium_desktop_font-size);line-height:var(--mds_brand_typography_text_medium_desktop_line-height)}}.mc-popover.large{font-style:var(--mds_brand_typography_text_medium_normal_font-style);font-weight:var(--mds_brand_typography_text_medium_normal_font-weight)}.mc-popover:not(.open) .content{display:none}.mc-popover .content[data-position^=top] .arrow{border-top:0;border-left:0}.mc-popover .content[data-position^=bottom] .arrow{border-bottom:0;border-right:0}.mc-popover .content[data-position^=left] .arrow{border-bottom:0;border-left:0}.mc-popover .content[data-position^=right] .arrow{border-top:0;border-right:0}`;
var f = [i, unsafeCSS(g2)];
var y = 50;
var j = 50;
var o2 = class extends p(LitElement) {
  constructor() {
    super(...arguments);
    this.latestClickTarget = null;
    this.latestOverflow = 0;
    this.latestMouseOverElement = null;
    this.eventReferences = {};
    this.trigger = "click focus";
    this.open = false;
    this.position = "top-left";
    this.arrow = false;
    this.actionkeys = ["Enter", "Space"];
    this.preventcloseonblur = false;
    this.fit = "medium";
    this.width = "auto";
    this.modalmode = "none";
    this.dontflip = false;
    this.dontshift = false;
    this.dontadjustheight = false;
    this.opendelay = y;
    this.contextmenuonside = false;
    this.detectOverflowMiddleware = { name: "detectOverflowMiddleware", fn: async (e3) => {
      let { top: t2, bottom: n } = await detectOverflow2(e3, { padding: 8 });
      if (t2 > 0) {
        this.latestOverflow = t2;
        let s = e3.rects.floating.height - t2;
        return this.container && (this.container.style.maxHeight = `${s}px`), {};
      }
      if (n > 0) {
        this.latestOverflow = n;
        let s = e3.rects.floating.height - n;
        return this.container && (this.container.style.maxHeight = `${s}px`), {};
      }
      return this.latestOverflow > 0 && t2 < 0 && n < 0 && this.container && (this.container.style.maxHeight = "none"), {};
    } };
  }
  static get styles() {
    return f;
  }
  render() {
    let e3 = { [this.fit]: true, open: this.open, "modal-on-x-small-screen": this.modalmode === "x-small-screen" }, t2 = { maxHeight: this.maxheight ? `calc(${this.maxheight} - var(--mds_global_border_width) * 4)` : "none", maxWidth: this.maxwidth || "calc(100vw - 10px)" }, n = this.zindex ? { zIndex: this.zindex } : {};
    return html`<div class="mc-popover ${classMap(e3)}"><slot name="trigger" @slotchange="${this.onTriggerChange}"></slot><div part="content" data-cy="content" class="content" aria-describedby="popover-content-slot" style="${styleMap(n)}"><div part="container" class="container">${this.renderHeading()}<div class="body-slot-wrapper mds-modal_body" style="${styleMap(t2)}"><slot @slotchange="${this.onContentChange}" id="popover-content-slot"></slot></div>${this.renderFooter()}</div>${this.renderArrow()}</div></div>`;
  }
  renderHeading() {
    return this.modalmode === "none" ? null : html`<header><slot class="mds-modal_title" name="heading"></slot><mc-button class="mds-modal_close-button" label="Close" hiddenlabel appearance="neutral" variant="plain" icon="times" fit="${this.fit}" data-cy="close" @click="${this.hide}" disablediconslot disabledlabelslot></mc-button></header>`;
  }
  renderFooter() {
    let e3 = { "--empty": !this.footerSlottedElements || this.footerSlottedElements.length === 0 };
    return this.modalmode === "none" ? null : html`<footer class="${classMap(e3)}"><slot @slotchange="${this.updateFooter}" name="footer"></slot></footer>`;
  }
  updateFooter() {
    var _a;
    this.footerSlottedElements = (_a = this.footerSlot) == null ? void 0 : _a.assignedElements({ flatten: true });
  }
  renderArrow() {
    return this.arrow ? html`<div part="arrow" class="arrow" role="presentation"></div>` : html``;
  }
  async connectedCallback() {
    super.connectedCallback(), this.eventReferences && (this.eventReferences.onDocumentMousedown = this.onDocumentMousedown.bind(this), this.eventReferences.onTriggerMousedown = this.onTriggerMousedown.bind(this), this.eventReferences.onFocusIn = this.onFocusIn.bind(this), this.eventReferences.onKeyDown = this.onKeyDown.bind(this), this.eventReferences.onMouseOver = this.onMouseOver.bind(this), this.eventReferences.onMouseOut = this.onMouseOut.bind(this), this.eventReferences.onContextMenu = this.onContextMenu.bind(this), this.eventReferences.onBlur = this.onBlur.bind(this), this.eventReferences.onScroll = this.onScroll.bind(this), this.eventReferences.onDocumentScroll = this.onDocumentScroll.bind(this)), await super.updateComplete, this.addEventListeners();
  }
  disconnectedCallback() {
    this.removeEventListeners(), this.stop(), this.latestClickTarget = null, this.latestMouseOverElement = null, super.disconnectedCallback();
  }
  async stop() {
    return new Promise((e3) => {
      this.cleanup ? (this.cleanup(), this.cleanup = void 0, requestAnimationFrame(() => e3())) : e3();
    });
  }
  async firstUpdated(e3) {
    super.firstUpdated(e3), this.onContentChange(), this.setTriggerElement(), await this.updateComplete, this.updateFooter();
  }
  async updated(e3) {
    super.updated(e3), e3.has("trigger") && this.trigger && (this.addRemoveTriggerEventListeners(), this.removeEventListeners(), this.addEventListeners()), e3.has("customtriggerelement") && this.customtriggerelement && (this.setTriggerElement(), this.removeEventListeners(), this.addEventListeners()), e3.has("open") && this.open !== e3.get("open") && (this.modalmode === "x-small-screen" && this.togglePageLevelScrollingInFullScreenModal(this.open), this.open ? (this.triggerElement || this.setTriggerElement(), !this.cleanup && this.triggerElement && this.contentWrapper && (this.cleanup = autoUpdate(this.triggerElement, this.contentWrapper, () => {
      requestAnimationFrame(() => this.reposition());
    }))) : await this.stop()), this.reposition();
  }
  onTriggerChange() {
    this.setTriggerElement();
  }
  onContentChange() {
    var _a;
    let t2 = ((_a = this.shadowRoot) == null ? void 0 : _a.querySelector("#popover-content-slot")).assignedElements({ flatten: true });
    this.contentElements = this.getAllChildren(t2), this.reposition();
  }
  onFocusIn() {
    this.open || this.show();
  }
  onBlur(e3) {
    if (!this.preventcloseonblur) {
      if (e3.relatedTarget && this.isTargetInComponent(e3.relatedTarget) || this.latestClickTarget && this.isTargetInComponent(this.latestClickTarget) || this.latestMouseOverElement && this.isTargetInComponent(this.latestMouseOverElement) || e3.relatedTarget === this.triggerElement) {
        this.latestClickTarget = null;
        return;
      }
      this.hide();
    }
  }
  onDocumentMousedown(e3) {
    var _a;
    if (this.latestClickTarget = e3.target, e3.target !== this.triggerElement && !((_a = this.contentElements) == null ? void 0 : _a.includes(e3.target)) && !e3.composedPath().includes(this)) {
      if (this.customtriggerelement && this.triggerElement && e3.composedPath().includes(this.triggerElement)) return;
      this.open && this.hide();
    }
  }
  onTriggerMousedown(e3) {
    e3 instanceof MouseEvent && e3.button !== 0 || this.trigger !== "contextmenu" && (this.open ? this.hide() : this.show());
  }
  onKeyDown(e3) {
    var _a;
    if (this.open && e3.code === "Escape") {
      e3.stopPropagation(), this.hide();
      return;
    }
    let t2 = this.actionkeys.includes(e3.code) && (e3.target === this.triggerElement || ((_a = this.triggerElement) == null ? void 0 : _a.contains(e3.target)));
    if (t2) {
      if (e3.preventDefault(), t2 && this.open) {
        this.hide();
        return;
      }
      t2 && !this.open && this.show();
    }
  }
  onMouseOver(e3) {
    if (this.latestMouseOverElement = e3.relatedTarget, this.hasTrigger("hover")) {
      let t2 = this.opendelay ?? y;
      clearTimeout(this.hoverTimeout), this.hoverTimeout = window.setTimeout(() => this.show(), t2);
    }
  }
  onMouseOut() {
    clearTimeout(this.hoverTimeout), this.hoverTimeout = window.setTimeout(() => this.hide(), j);
  }
  onContextMenu(e3) {
    if (e3.target === this.triggerElement || this.triggerElement && e3.composedPath().includes(this.triggerElement)) {
      e3.preventDefault(), this.latestContextmenuEvent = e3, this.open = true, this.reposition();
      return;
    }
    this.hide();
  }
  onScroll() {
    this.dispatchEvent(new Event("scroll"));
  }
  onDocumentScroll() {
    e() && this.open && this.hide();
  }
  reposition() {
    if (!this.open || !this.triggerElement) return;
    if (this.hasTrigger("contextmenu") && !this.contextmenuonside) {
      let t2 = { getBoundingClientRect() {
        return { x: 0, y: 0, top: 0, left: 0, bottom: 0, right: 0, width: 0, height: 0 };
      } };
      this.contentWrapper && computePosition2(t2, this.contentWrapper).then(() => {
        var _a, _b;
        this.contentWrapper && Object.assign(this.contentWrapper.style, { left: `${(_a = this.latestContextmenuEvent) == null ? void 0 : _a.clientX}px`, top: `${(_b = this.latestContextmenuEvent) == null ? void 0 : _b.clientY}px` });
      });
      return;
    }
    let e3 = [offset2({ ...this.getOffset(this.fit) }), this.dontflip ? null : flip2(), this.dontshift ? null : shift2({ limiter: limitShift2({ offset: ({ rects: t2, placement: n }) => n.startsWith("top") || n.startsWith("bottom") ? t2.reference.height / 2 : t2.reference.width / 2 }) }), this.dontadjustheight ? null : this.detectOverflowMiddleware, size2({ apply: ({ elements: t2, rects: n }) => {
      Object.assign(t2.floating.style, { maxWidth: this.maxwidth ? this.maxwidth : "none", maxHeight: this.maxheight ? this.maxheight : "none", width: this.width === "trigger" ? `${n.reference.width}px` : this.width });
    } }), this.arrow && this.arrowElement ? arrow2({ element: this.arrowElement, padding: this.getArrowPadding(this.fit) }) : null];
    this.contentWrapper && computePosition2(this.triggerElement, this.contentWrapper, { placement: this.mapPosition(this.position), middleware: e3, strategy: "fixed" }).then(({ x: t2, y: n, middlewareData: s, placement: a3 }) => {
      var _a, _b;
      let c = { top: "bottom", right: "left", bottom: "top", left: "right" }[a3.split("-")[0]];
      if (this.contentWrapper && (this.contentWrapper.setAttribute("data-position", a3), Object.assign(this.contentWrapper.style, { left: `${t2}px`, top: `${n}px` })), this.arrow && s) {
        let p2 = (_a = s.arrow) == null ? void 0 : _a.x, u2 = (_b = s.arrow) == null ? void 0 : _b.y, w2 = typeof u2 == "number" ? `${u2}px` : "", E = "", x = "", T3 = typeof p2 == "number" ? `${p2}px` : "";
        this.arrowElement && c && Object.assign(this.arrowElement.style, { top: w2, right: E, bottom: x, left: T3, [c]: `${this.getArrowMargin(this.fit)}` });
      }
    });
  }
  getArrowMargin(e3) {
    return { small: "-3px", medium: "-4px", large: "-5px" }[e3] || "5px";
  }
  show() {
    this.open = true, requestAnimationFrame(() => {
      this.dispatchEvent(new CustomEvent("show", { bubbles: true, composed: true }));
    });
  }
  hide() {
    this.open = false, requestAnimationFrame(() => {
      this.dispatchEvent(new CustomEvent("hide", { bubbles: true, composed: true }));
    });
  }
  addRemoveTriggerEventListeners() {
    var _a, _b, _c;
    ((_a = this.eventReferences) == null ? void 0 : _a.onTriggerMousedown) && (this.trigger === "manual" ? (_b = this.triggerElement) == null ? void 0 : _b.removeEventListener("mousedown", this.eventReferences.onTriggerMousedown) : (_c = this.triggerElement) == null ? void 0 : _c.addEventListener("mousedown", this.eventReferences.onTriggerMousedown));
  }
  setTriggerElement() {
    if (this.customtriggerelement) {
      if (typeof this.customtriggerelement == "string") {
        let t2 = document.getElementById(this.customtriggerelement);
        t2 || console.warn(`mc-popover: Could not find element with id "${this.customtriggerelement}"`), this.triggerElement = t2 || void 0;
      } else this.triggerElement = this.customtriggerelement;
      this.addRemoveTriggerEventListeners();
      return;
    }
    let e3 = this.querySelector('[slot="trigger"]');
    e3 instanceof HTMLSlotElement && (e3 = e3.assignedElements({ flatten: true })[0]), this.triggerElement = e3, this.addRemoveTriggerEventListeners();
  }
  hasTrigger(e3) {
    return this.trigger.split(" ").includes(e3);
  }
  getOffset(e3) {
    return { small: { mainAxis: 8, crossAxis: 0 }, medium: { mainAxis: 8, crossAxis: 0 }, large: { mainAxis: 8, crossAxis: 0 } }[e3] || { mainAxis: 0, crossAxis: 0 };
  }
  getArrowPadding(e3) {
    return { small: 6, medium: 7, large: 8 }[e3] || 7;
  }
  mapPosition(e3) {
    return { "bottom-center": "bottom", "bottom-left": "bottom-start", "bottom-right": "bottom-end", "left-bottom": "left-end", "left-center": "left", "left-top": "left-start", "right-bottom": "right-end", "right-center": "right", "right-top": "right-start", "top-center": "top", "top-left": "top-start", "top-right": "top-end" }[e3] || "bottom-start";
  }
  addEventListeners() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r;
    if (this.hasTrigger("manual")) return;
    let e3 = this.customtriggerelement && this.triggerElement ? this.triggerElement : this;
    ((_a = this.eventReferences) == null ? void 0 : _a.onDocumentMousedown) && document.addEventListener("mousedown", this.eventReferences.onDocumentMousedown, true), ((_b = this.eventReferences) == null ? void 0 : _b.onKeyDown) && e3.addEventListener("keydown", this.eventReferences.onKeyDown), ((_c = this.eventReferences) == null ? void 0 : _c.onBlur) && e3.addEventListener("blur", this.eventReferences.onBlur, true), ((_d = this.eventReferences) == null ? void 0 : _d.onScroll) && this.container && this.container.addEventListener("scroll", this.eventReferences.onScroll, true), ((_e = this.eventReferences) == null ? void 0 : _e.onDocumentScroll) && document.addEventListener("scroll", this.eventReferences.onDocumentScroll, true), ((_f = this.eventReferences) == null ? void 0 : _f.onMouseOver) && e3.addEventListener("mouseover", (_g = this.eventReferences) == null ? void 0 : _g.onMouseOver), this.hasTrigger("focus") && ((_h = this.eventReferences) == null ? void 0 : _h.onFocusIn) && e3.addEventListener("focusin", this.eventReferences.onFocusIn, true), "addEventListener" in e3 && (((_i = this.eventReferences) == null ? void 0 : _i.onKeyDown) && e3.addEventListener("keydown", (_j = this.eventReferences) == null ? void 0 : _j.onKeyDown), ((_k = this.eventReferences) == null ? void 0 : _k.onBlur) && e3.addEventListener("blur", (_l = this.eventReferences) == null ? void 0 : _l.onBlur, true), this.hasTrigger("focus") && ((_m = this.eventReferences) == null ? void 0 : _m.onFocusIn) && e3.addEventListener("focusin", (_n = this.eventReferences) == null ? void 0 : _n.onFocusIn, true), this.hasTrigger("hover") && ((_o = this.eventReferences) == null ? void 0 : _o.onMouseOut) && e3.addEventListener("mouseout", (_p = this.eventReferences) == null ? void 0 : _p.onMouseOut)), this.hasTrigger("contextmenu") && ((_q = this.eventReferences) == null ? void 0 : _q.onContextMenu) && document.addEventListener("contextmenu", (_r = this.eventReferences) == null ? void 0 : _r.onContextMenu);
  }
  removeEventListeners() {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    let e3 = this.customtriggerelement && this.triggerElement ? this.triggerElement : this;
    ((_a = this.eventReferences) == null ? void 0 : _a.onDocumentMousedown) && document.removeEventListener("mousedown", this.eventReferences.onDocumentMousedown, true), ((_b = this.eventReferences) == null ? void 0 : _b.onKeyDown) && e3.removeEventListener("keydown", this.eventReferences.onKeyDown), ((_c = this.eventReferences) == null ? void 0 : _c.onBlur) && e3.removeEventListener("blur", (_d = this.eventReferences) == null ? void 0 : _d.onBlur, true), ((_e = this.eventReferences) == null ? void 0 : _e.onFocusIn) && e3.removeEventListener("focusin", this.eventReferences.onFocusIn, true), ((_f = this.eventReferences) == null ? void 0 : _f.onMouseOver) && e3.removeEventListener("mouseover", this.eventReferences.onMouseOver), ((_g = this.eventReferences) == null ? void 0 : _g.onMouseOut) && e3.removeEventListener("mouseout", this.eventReferences.onMouseOut), ((_h = this.eventReferences) == null ? void 0 : _h.onContextMenu) && document.removeEventListener("contextmenu", this.eventReferences.onContextMenu), ((_i = this.eventReferences) == null ? void 0 : _i.onDocumentScroll) && document.removeEventListener("scroll", this.eventReferences.onDocumentScroll, true);
  }
  isTargetInComponent(e3) {
    return this.contentElements ? this.contentElements.includes(e3) : false;
  }
  getAllChildren(e3) {
    let t2 = [];
    return e3.forEach((n) => {
      if (n instanceof HTMLSlotElement) {
        let s = n.assignedElements({ flatten: true });
        t2 = [...t2, ...this.getAllChildren(s)];
      } else if (t2.push(n), n.children) {
        let s = Array.from(n.children);
        t2 = [...t2, ...this.getAllChildren(s)];
      }
    }), t2;
  }
};
r([query('slot[name="footer"]')], o2.prototype, "footerSlot", 2), r([state()], o2.prototype, "footerSlottedElements", 2), r([property({ type: String })], o2.prototype, "trigger", 2), r([property({ type: String })], o2.prototype, "customtriggerelement", 2), r([property({ type: Boolean, reflect: true })], o2.prototype, "open", 2), r([property({ type: String })], o2.prototype, "position", 2), r([property({ type: Boolean })], o2.prototype, "arrow", 2), r([property({ type: Array })], o2.prototype, "actionkeys", 2), r([property({ type: Boolean })], o2.prototype, "preventcloseonblur", 2), r([property({ type: String })], o2.prototype, "fit", 2), r([property({ type: String })], o2.prototype, "width", 2), r([property({ type: String })], o2.prototype, "maxwidth", 2), r([property({ type: String })], o2.prototype, "maxheight", 2), r([property({ type: String })], o2.prototype, "modalmode", 2), r([property({ type: Boolean })], o2.prototype, "dontflip", 2), r([property({ type: Boolean })], o2.prototype, "dontshift", 2), r([property({ type: Boolean })], o2.prototype, "dontadjustheight", 2), r([property({ type: Number })], o2.prototype, "opendelay", 2), r([property({ type: Boolean })], o2.prototype, "contextmenuonside", 2), r([property({ type: Number })], o2.prototype, "zindex", 2), r([query(".content")], o2.prototype, "contentWrapper", 2), r([query(".container")], o2.prototype, "container", 2), r([query(".arrow")], o2.prototype, "arrowElement", 2);
customElements.get("mc-popover") || customElements.define("mc-popover", o2);

// node_modules/@maersk-global/mds-components-core-select/index.js
var g3 = Object.defineProperty;
var O = Object.getOwnPropertyDescriptor;
var o3 = (d, r2, e3, t2) => {
  for (var s = t2 > 1 ? void 0 : t2 ? O(r2, e3) : r2, p2 = d.length - 1, c; p2 >= 0; p2--) (c = d[p2]) && (s = (t2 ? c(r2, e3, s) : c(s)) || s);
  return t2 && s && g3(r2, e3, s), s;
};
var b = css`:host([readonly]) *{pointer-events:none;touch-action:none;user-select:none}.mc-select .vanity input.input,.mc-select input.input{color:transparent;cursor:pointer;display:block}.mc-select .vanity input.input::selection,.mc-select input.input::selection{background:0 0}.mc-select .vanity input.input::-moz-selection,.mc-select input.input::-moz-selection{background:0 0}.mc-select .affix::part(icon){fill:var(--mds_brand_appearance_neutral_weak_text-color)}.mc-select .mc-input.vanity .affix.type-suffix{padding-left:0}.mc-select .options-empty{padding:0 0 0 11px}.mc-select .field{min-width:fit-content;width:100%;cursor:pointer;position:relative}.mc-select .field .input-container{overflow:hidden}.mc-select .field .input-container .selected-option-label{align-items:center;color:var(--mds_brand_appearance_neutral_default_on-background-color);display:flex;pointer-events:none;min-width:0;position:absolute;width:100%;z-index:2;top:-1px}.mc-select .field .input-container .selected-option-label div{flex:1;overflow:hidden;white-space:nowrap;text-overflow:ellipsis}.mc-select .field .input-container .selected-option-label mc-option{background-color:transparent}.mc-select .field .input-container .selected-option-label mc-option::part(button){padding:0;background-color:transparent}.mc-select mc-popover::part(container){overflow-x:hidden}.mc-select mc-option.placeholder::part(text-and-icon){color:var(--mds_brand_appearance_neutral_weakest_text-color)}.mc-select.open .container .inner .field{outline:0;border-color:var(--mds_brand_appearance_state_focus_border-color);box-shadow:var(--mds_brand_appearance_state_focus_default_shadow_offset-x) var(--mds_brand_appearance_state_focus_default_shadow_offset-y) var(--mds_brand_appearance_state_focus_default_shadow_blur-radius) var(--mds_brand_appearance_state_focus_default_shadow_spread-radius) var(--mds_brand_appearance_state_focus_default_shadow_color)}.mc-select.open .vanity .container .inner .field{outline:0;border-bottom-color:var(--mds_brand_appearance_state_focus_border-color);box-shadow:var(--mds_brand_appearance_state_focus_vanity_shadow_offset-x) var(--mds_brand_appearance_state_focus_vanity_shadow_offset-y) var(--mds_brand_appearance_state_focus_vanity_shadow_blur-radius) var(--mds_brand_appearance_state_focus_vanity_shadow_spread-radius) var(--mds_brand_appearance_state_focus_vanity_shadow_color)}.mc-select .small .selected-option-label{height:32px;padding:0 0 0 7px;font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);font-size:var(--mds_brand_typography_text_small_mobile_font-size);line-height:var(--mds_brand_typography_text_small_mobile_line-height)}@media screen and (min-width:1025px){.mc-select .small .selected-option-label{font-size:var(--mds_brand_typography_text_small_desktop_font-size);line-height:var(--mds_brand_typography_text_small_desktop_line-height)}}.mc-select .small .selected-option-label{font-style:var(--mds_brand_typography_text_small_normal_font-style);font-weight:var(--mds_brand_typography_text_small_normal_font-weight)}.mc-select .small .field .input-container{min-width:20px}.mc-select .small .field .input-container .input{padding-right:0}.mc-select .small.vanity .selected-option-label{padding-left:0;padding-right:0}.mc-select .small.prefix .selected-option-label{padding-left:0;margin-left:11px}.mc-select .small.prefix.vanity .selected-option-label{margin-left:0}.mc-select .medium .selected-option-label{height:40px;padding:0 0 0 11px;font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);font-size:var(--mds_brand_typography_text_medium_mobile_font-size);line-height:var(--mds_brand_typography_text_medium_mobile_line-height)}@media screen and (min-width:1025px){.mc-select .medium .selected-option-label{font-size:var(--mds_brand_typography_text_medium_desktop_font-size);line-height:var(--mds_brand_typography_text_medium_desktop_line-height)}}.mc-select .medium .selected-option-label{font-style:var(--mds_brand_typography_text_medium_normal_font-style);font-weight:var(--mds_brand_typography_text_medium_normal_font-weight)}.mc-select .medium .field .input-container{min-width:24px}.mc-select .medium .field .input-container .input{padding-right:0}.mc-select .medium.vanity .selected-option-label{padding-left:0;padding-right:0}.mc-select .medium.prefix .selected-option-label{padding-left:0;margin-left:11px}.mc-select .medium.prefix.vanity .selected-option-label{margin-left:0}.mc-select .large .selected-option-label{height:48px;padding:0 0 0 15px;font-family:var(--mds_brand_typography_text_font-family),var(--mds_brand_typography_text_font-family-fallback);font-size:var(--mds_brand_typography_text_medium_mobile_font-size);line-height:var(--mds_brand_typography_text_medium_mobile_line-height)}@media screen and (min-width:1025px){.mc-select .large .selected-option-label{font-size:var(--mds_brand_typography_text_medium_desktop_font-size);line-height:var(--mds_brand_typography_text_medium_desktop_line-height)}}.mc-select .large .selected-option-label{font-style:var(--mds_brand_typography_text_medium_normal_font-style);font-weight:var(--mds_brand_typography_text_medium_normal_font-weight)}.mc-select .large .field .input-container{min-width:30px}.mc-select .large .field .input-container .input{padding-right:0}.mc-select .large.vanity .selected-option-label{padding-left:0;padding-right:0}.mc-select .large.prefix .selected-option-label{padding-left:0;margin-left:11px}.mc-select .large.prefix.vanity .selected-option-label{margin-left:0}.mc-select mc-input.option-search{padding:8px 12px 8px 36px}`;
var y2 = [i, t, unsafeCSS(b)];
var u = { en: { ...e2.en }, de: { ...e2.de }, es: { ...e2.es }, fr: { ...e2.fr }, it: { ...e2.it }, pt: { ...e2.pt }, tr: { ...e2.tr }, ru: { ...e2.ru }, zh: { ...e2.zh }, "zh-TW": { ...e2["zh-TW"] }, ko: { ...e2.ko }, ja: { ...e2.ja } };
var i3 = class extends T(i2) {
  constructor() {
    super(...arguments);
    this.preventInputBlurEvent = false;
    this.closureDueToItemSelection = false;
    this.placeholderOption = true;
    this.pressedAlphanumeric = "";
    this.actionkeys = ["Enter", "Space", "ArrowDown"];
    this.currentGroup = null;
    this.data = null;
    this.ariaExpanded = "false";
    this.open = false;
    this.listsearch = false;
    this.listsearchplaceholder = u.en.searchPlaceholder;
    this.filtertype = "contains";
    this.matchlabelonly = false;
  }
  get computedLabels() {
    return m({ translations: u, deprecatedPropsMap: { listsearchplaceholder: "searchPlaceholder" }, component: this, labelsOverride: this.labels });
  }
  render() {
    var _a;
    let e3 = { open: !!((_a = this.mcPopover) == null ? void 0 : _a.open), listsearch: this.listsearch };
    return html`<div data-cy="mc-input-container" class="mc-select ${classMap(e3)}">${super.render()}<mc-popover exportparts="content: popover-content" data-cy="options-popover" .customtriggerelement="${this.readonly ? null : this.inputField}" trigger="click" position="bottom-left" width="${this.optionswidth ? this.optionswidth : "trigger"}" maxheight="${ifDefined(this.optionsheight)}" .open="${this.open}" .actionkeys="${this.actionkeys}" @show="${this.onPopoverOpen}" @hide="${this.onPopoverClose}" preventcloseonblur role="presentation" aria-hidden="${this.open ? "false" : "true"}">${this.renderListOptions()}</mc-popover></div>`;
  }
  renderListOptions() {
    return html`<mc-list id="listbox" aria-labelledby="label" class="content" noborder scrolltofocused .type="${this.listType}" .fit="${this.fit}" .value="${this.value}" ?listsearch="${this.listsearch}" .listsearchplaceholder="${this.computedLabels.searchPlaceholder}" .filtertype="${this.filtertype}" .customfilter="${this.customfilter}" ?matchlabelonly="${this.matchlabelonly}" @listchange="${this.onOptionSelected}">${this.renderNoOptions()} ${this.renderPlaceholderOption()} ${this.renderOptionsFromData()}<slot @slotchange="${this.handleSlotChange}"></slot></mc-list>`;
  }
  renderOptionsFromData() {
    return this.loading === false && this.data && this.data.length > 0 ? this.data.map((e3) => html`${this.renderGroupLabel(e3)}<mc-option .value="${e3.value}" .icon="${e3.icon}" ?disabled="${e3.disabled}">${e3.label}</mc-option>`) : null;
  }
  renderGroupLabel(e3) {
    return e3.group && e3.group !== this.currentGroup ? (this.currentGroup = e3.group, html`<small>${e3.group}</small>`) : null;
  }
  renderSelectedOptionLabel() {
    var _a;
    return this.value || ((_a = this.value) == null ? void 0 : _a.toString()) === "false" ? html`<div part="selected-option" class="selected-option-label" aria-labelledby="label" aria-controls="listbox" aria-haspopup="listbox" aria-owns="listbox" aria-expanded="${this.ariaExpanded}" role="${ifDefined(this.listType === "multiple" ? "listbox" : "list")}" @focusin="${() => this.focus()}"><div role="${ifDefined(this.listType === "multiple" ? "option" : void 0)}" ?inert="${this.listType === "single"}">${this.selectedOptionLabel}</div></div>` : html``;
  }
  renderPlaceholderOption() {
    return this.placeholderOption && this.placeholder !== "" ? html`<mc-option class="placeholder" value="null" label="${this.placeholder}"></mc-option>` : null;
  }
  renderNoOptions() {
    let e3 = !this.mcOptions || this.mcOptions.length === 0, t2 = !this.data || this.data.length === 0;
    return e3 && t2 ? html`<div class="options-empty">${this.computedLabels.noResultsFound}</div>` : null;
  }
  connectedCallback() {
    super.connectedCallback(), this.addEventListener("keydown", this.onKeyDown.bind(this));
  }
  async firstUpdated(e3) {
    super.firstUpdated(e3), this.inputElement.setAttribute("readonly", "true");
  }
  async updated(e3) {
    super.updated(e3), e3.has("value") && e3.get("value") !== this.value && (await this.mcList.updateComplete, this.getSelectedOption(), this.selectedOptionLabel = this.getDisplayLabel());
  }
  onKeyDown(e3) {
    var _a, _b;
    if (this.clearbutton && e3.code === "Backspace") {
      if (this.listsearch && ((_a = this.mcPopover) == null ? void 0 : _a.open) && e3.composedPath().some((c) => {
        var _a2, _b2, _c;
        let h = c;
        return ((_a2 = h.classList) == null ? void 0 : _a2.contains("list-search")) || ((_b2 = h.tagName) == null ? void 0 : _b2.toLowerCase()) === "input" && ((_c = h.closest) == null ? void 0 : _c.call(h, ".list-search"));
      })) return;
      this.onClearButtonClick(e3);
      return;
    }
    if (((_b = this.mcPopover) == null ? void 0 : _b.open) && this.mcPopover.hide && e3.code === "Tab") {
      e3.stopPropagation(), this.mcPopover.hide();
      return;
    }
    (e3.keyCode >= 48 && e3.keyCode <= 57 || e3.keyCode >= 65 && e3.keyCode <= 90) && !this.listsearch && (this.pressedAlphanumeric += e3.key, this.handleAlphanumericKey(), this.pressedAlphanumericTimeout = window.setTimeout(() => {
      window.clearTimeout(this.pressedAlphanumericTimeout), this.pressedAlphanumeric = "";
    }, 500));
  }
  onInputBlur(e3) {
    super.onInputBlur(), this.preventInputBlurEvent && (e3.stopPropagation(), e3.preventDefault());
  }
  blur() {
    super.blur(), this.dispatchEvent(new FocusEvent("blur"));
  }
  onPopoverOpen() {
    if (this.preventInputBlurEvent = true, this.trailingicon = "chevron-up", this.ariaExpanded = "true", this.mcPopover && this.mcPopover.contentWrapper && this.mcList && this.mcList.setFocusedItem) {
      let { y: e3, height: t2 } = this.mcPopover.contentWrapper.getBoundingClientRect();
      if (this.mcList.mousemovevisiblearea = { min: e3, max: t2 + e3 }, this.listsearch) this.mcList.listSearchInput.focus();
      else {
        let s = this.getFocusedOption();
        s && this.mcList.setFocusedItem(s, true);
      }
    }
    this.dispatchEvent(new CustomEvent("opened", { detail: true }));
  }
  onPopoverClose() {
    this.closureDueToItemSelection || this.blur(), this.closureDueToItemSelection = false, this.preventInputBlurEvent = false, this.trailingicon = "chevron-down", this.ariaExpanded = "false", this.mcList && (this.mcList.clearFocusedItem && this.mcList.clearFocusedItem(), this.listsearch && this.mcList.clearListSearch && this.mcList.clearListSearch()), this.dispatchEvent(new CustomEvent("closed", { detail: true }));
  }
  onOptionSelected(e3) {
    this.closureDueToItemSelection = true, this.setOptionSelected(e3.detail.item);
  }
  onClearButtonClick(e3) {
    var _a;
    e3.stopPropagation(), (_a = this.mcPopover) == null ? void 0 : _a.hide(), super.onClearButtonClick(e3);
  }
  getOptions() {
    var _a;
    if (this.slotElements) if (Array.from(this.slotElements).filter((t2) => t2.nodeName.toLowerCase() !== "input").length === 0) {
      let t2 = (_a = this.shadowRoot) == null ? void 0 : _a.querySelector("mc-list");
      t2 && (this.mcOptions = Array.from(t2.querySelectorAll("mc-option")));
    } else this.mcOptions = Array.from(this.slotElements).filter((t2) => t2.nodeName.toLowerCase() === "mc-option");
  }
  async handleSlotChange() {
    this.getOptions(), await this.updateComplete, requestAnimationFrame(() => {
      this.getSelectedOption(), this.handleValueLabelSlotChange();
    });
  }
  handleAlphanumericKey() {
    var _a, _b, _c, _d;
    if (this.mcOptions) {
      let e3 = (_a = this.mcOptions) == null ? void 0 : _a.find((t2) => {
        var _a2, _b2, _c2, _d2;
        return (_d2 = (_c2 = t2.innerText ? t2.innerText : (_b2 = (_a2 = t2.shadowRoot) == null ? void 0 : _a2.querySelector(this.labelSelector)) == null ? void 0 : _b2.innerText) == null ? void 0 : _c2.toLowerCase().trim().replace(/\n/g, "")) == null ? void 0 : _d2.startsWith(this.pressedAlphanumeric);
      });
      ((_b = this.mcPopover) == null ? void 0 : _b.open) && e3 && ((_c = this.mcList) == null ? void 0 : _c.setFocusedItem) ? this.mcList.setFocusedItem(e3, true) : !((_d = this.mcPopover) == null ? void 0 : _d.open) && e3 && this.listType === "single" && this.setOptionSelected(e3);
    }
  }
};
o3([property({ type: Object })], i3.prototype, "labels", 2), o3([state()], i3.prototype, "placeholderOption", 2), o3([state()], i3.prototype, "selectedOptionLabel", 2), o3([queryAssignedElements({ slot: "", flatten: true })], i3.prototype, "slotElements", 2), o3([state()], i3.prototype, "currentGroup", 2), o3([property({ type: Array })], i3.prototype, "data", 2), o3([query("mc-popover")], i3.prototype, "mcPopover", 2), o3([query("mc-list")], i3.prototype, "mcList", 2), o3([state()], i3.prototype, "ariaExpanded", 2), o3([property({ type: String })], i3.prototype, "optionsheight", 2), o3([property({ type: String })], i3.prototype, "optionswidth", 2), o3([property({ type: Boolean })], i3.prototype, "open", 2), o3([property({ type: Boolean })], i3.prototype, "listsearch", 2), o3([property({ type: String })], i3.prototype, "listsearchplaceholder", 2), o3([property({ type: String })], i3.prototype, "filtertype", 2), o3([property({ attribute: false })], i3.prototype, "customfilter", 2), o3([property({ type: Boolean })], i3.prototype, "matchlabelonly", 2);
var _2 = class extends i3 {
  constructor() {
    super();
    this.listType = "single";
    this.labelSelector = ".labels";
    this.trailingicon = "chevron-down", this.keepclearbuttonvisible = true;
  }
  static get styles() {
    return [super.styles, y2];
  }
  willUpdate(e3) {
    super.willUpdate(e3), e3.has("clearbutton") && (this.placeholderOption = !this.clearbutton), e3.has("value") && (this.selectedOption = null);
  }
  async updated(e3) {
    super.updated(e3), e3.has("data") && (this.getOptions(), this.getSelectedOption(), this.selectedOptionLabel = this.getDisplayLabel());
  }
  async setOptionSelected(e3) {
    var _a;
    this.selectedOption = e3, this.selectedOption && (this.value = this.selectedOption.value === "null" ? "" : this.selectedOption.value, this.selectedOptionLabel = this.getDisplayLabel(), this.dispatchEvent(new InputEvent("input")), this.dispatchEvent(new CustomEvent("optionselected", { detail: { value: this.value, label: this.selectedOption.innerText }, composed: true, bubbles: true })), this.mcPopover && this.mcPopover.hide && this.mcPopover.hide(), await this.updateComplete, (_a = this.inputElement) == null ? void 0 : _a.focus());
  }
  getDisplayLabel() {
    if (this.selectedOption) {
      if (this.placeholderOption && this.placeholder && this.selectedOption.label === this.placeholder) return "";
      let e3 = this.selectedOption.childNodes, t2 = null;
      return e3.length > 0 && (t2 = this.selectedOption.cloneNode(true), t2.fit = this.fit), this.selectedOption.label || !t2 ? this.selectedOption.label : t2;
    }
    return "";
  }
  getSelectedOption() {
    var _a;
    if (this.mcOptions && this.mcOptions.length > 0) {
      let e3 = JSON.stringify(this.value);
      this.mcOptions.forEach((t2) => {
        t2.selected = false;
      }), this.selectedOption = (_a = this.mcOptions) == null ? void 0 : _a.find((t2) => e3 === JSON.stringify(t2.value)), this.selectedOption && (this.selectedOption.selected = true);
    }
  }
  getFocusedOption() {
    return this.selectedOption ? this.selectedOption.value === "null" && this.placeholderOption && this.placeholder ? this.mcOptions ? this.mcOptions[0] : null : this.selectedOption : this.mcOptions ? this.mcOptions.filter((e3) => !e3.disabled)[0] : null;
  }
  handleValueLabelSlotChange() {
    this.selectedOption && (this.value = this.selectedOption.value, this.selectedOptionLabel = this.getDisplayLabel());
  }
};
customElements.get("mc-select") || customElements.define("mc-select", _2);

export {
  y2 as y,
  i3 as i,
  _2 as _
};
//# sourceMappingURL=chunk-DOHDNVNG.js.map
